# Changelog

## 2026-06-02

- Consolidated generated source folders into a Netlify-ready static website.
- Removed original generated folders after extracting content and design direction.
- Corrected the photographer identity to Jonathan Peter Surette.
- Replaced fragile inherited remote imagery with local generated assets.
- Added Netlify form support for project inquiries.
- Removed preview screenshots after verification.
- Split combat, architecture, about, and contact into dedicated pages.
- Added Markdown project memory files in `docs/`.
- Updated the wordmark to read as a single `JPSURETTE` mark.
- Restored the original top-left wordmark treatment with `JPS`, separator line, and `SURETTE`.
- Revised the top-left wordmark to read `JP Surette`, keeping `J`, `P`, and `S` in the bold JPS type and `URETTE` in the lighter Surette type.
- Combined `Inquire` and `Contact` in the desktop navigation by removing the center `Contact` link and renaming the right-side button to `Contact`.
- Added `Photography` to the top-left wordmark as a blue technical lockup while preserving the `JP Surette` mark.
- Restyled `Photography` as a subtler second-line lockup matching the Surette type treatment, and added hero parallax for the central `JPS` mark and split hero images.
- Tuned the hero parallax so the central `JPS`, hero headlines, and background images each scroll on separate rates.
- Increased homepage hero text parallax and softened the hero image hover zoom with a springier transition.
- Added unique combat portfolio images so the Combat page does not reuse the same asset across Muay Thai, Jiu-Jitsu, Boxing Light Work, and Judo cards.
- Added a homepage `Most Recent Gallery` section after the hero and documented the future event-gallery/login/download/purchase direction.
- Added `events.html` and `everything-else.html`, linked them in the navigation, and pointed the homepage gallery section to Events.
- Reverted lossy WebP image conversion and restored original PNG assets to preserve image quality.
