# Current Status

Last updated: 2026-08-27





## Product line: websites only (2026-08-27)

- **Packaged apps and stores are withdrawn.** `app-catalog.html`, `app-pricing.html`,
  `app-checkout.html` and `multichannel-commerce-website.html` are deleted, with 301s in
  `netlify.toml`. The six app profiles remain as **case studies**; `apps.html` is now a
  case-study index, not a sales page (repurposed rather than deleted because 17 pages
  linked to it).
- **Every sales path was chased down, not just the pages.** The Software nav pill (desktop
  + hamburger), `data-active="software"` on 7 pages, purchase CTAs, the `websites.html`
  "Custom business software" card and multichannel card, `pricing.html`'s "Software, not a
  website" route and its enterprise bullets (custom e-commerce, CRM/ERP/API), the
  `index.html` meta description (3 occurrences), and **the contact form's project-type
  options** — the form was still collecting leads for retired work.
- **One CTA was percent-encoded** (`account.html?next=%2Fapp-checkout...`) and survived a
  plain href sweep. Search encoded forms too when retiring a funnel.

## Nav breakpoint is 1024px (2026-08-27)

Four pills again after Software was retired. Measured on an interior page: **+76px at
1120, +41px at 1024, +9px at 960, 0px at 920 and below.** 880 is **not** safe to return
to — the row abuts the CTA there. The breakpoint lives in 6 `@media` blocks in `nav.css`
plus `isMobileNavViewport()`, and `.page`'s vertical padding follows it while its
horizontal gutters stay at 880.

## Self-Hosting Setup and Handoff is live in the docs and on the site (2026-08-27)

- `website_pricing_plan.txt` **§29b**: $450 single/multi-page, $750 with stored form
  records. Flat per size — setup effort does not scale with page count.
- Accounts created **in the customer's name**; 14 days of setup support, then $125/hr.
  §29 was amended, since it previously excluded server configuration outright.
- Surfaced on `pricing.html` (new route + FAQ + rewritten ownership FAQ),
  `services/website-design-hosting.html` (full panel) and `websites.html`.

## Light mode had five real defects; the auditor could not see them (2026-08-19)

- **The rule that keeps producing these: a colour LITERAL cannot flip, so anything written as one must be given a light counterpart explicitly.** Three separate mechanisms broke this — a Tailwind arbitrary gradient (`bg-[linear-gradient(...,#050505_84%)]`, 15 pages), two inline `style="background:#050505 / #0a0010"` layers (privacy, terms), and three theme tokens calibrated only against pure paper.
- **All of them are `position: fixed` or siblings, which is exactly what an ancestor-walk auditor cannot see.** `getComputedStyle` climbs parents; a fixed veil is nobody's parent. That is why 2026-08-18 reported the whole site clean while `auction-house` was rendering its headings at 1.05:1 in light.
- **Fixes:** `.sds-cosmic-veil` (+ `--tinted` for seansads) in `cosmic-web.css`; `privacy.html` uses `rgb(var(--t-void-black))` and drops its nebula blobs to 0.12 in light; `terms.html` moved its backdrop to `.sds-page-backdrop`; `--t-electric-cyan` light `0 104 121 → 0 94 109`, `--t-ink-39ff14 → #1a5e0e`, `--t-ink-18d96e → #0e612f`; `account.html`'s phone auth panel keeps its glass fill; `.transfer-badge` → `#14490a`.
- **Dark mode is deliberately unchanged everywhere.** Every fix is either light-scoped or a class hook whose dark branch restates the original literal.
- **`account.html`'s phone login was a design decision that cost legibility.** The panel fill was dropped below 880px so the wormhole showed through; the plume runs across the login copy. It now uses the same glass it already had above 880px — the animation still reads through and around it.
- **Signed-in portal views remain unaudited** — the sweep only ever sees the signed-out shell.

## Card visuals are inline SVG, token-driven (2026-08-17)

- `websites.html`'s "What you get" cards use `.wb-viz` — an inline `<svg>` at 16/9, not an image. **Inline keeps them off this page's image budget**, which matters here: its proof cards were once 3.2 MB and were deliberately cut.
- **Every stroke and fill reads one of four custom properties on `.wb-viz`** — `--wb-line`, `--wb-dim`, `--wb-accent`, `--wb-accent2` — and nothing else. **Never put a hex inside one of these SVGs**: it freezes the illustration to a single theme with no error, exactly like a hardcoded palette colour anywhere else. Verified resolving in both themes.
- Use `--t-nebula-purple-on`, not raw `--t-nebula-purple`, for SVG *lines*. `#7000FF` is a fill colour; as a stroke on the site's near-blacks it measures 2.91:1.
- **All three cards must keep the same shape: visual → title → one paragraph → a three-item checklist.** The list is pinned to the card bottom by `.wb-card-list { margin-top: auto }`. The section previously had a list on only the middle card, which left the third ending in a large void because the grid still equalised heights. Tail gap is now a uniform 29px across the row — that is the check to re-run after editing.
- The "dedicated WhatsApp account" bullet belongs to **Changes handled for you**, not the hosting card. It is a change-request channel.

## Hero scrim is TWO layers (2026-08-17)

- `#hero-tint` on `index.html` stacks, topmost first: the **black gradient** (0.9 / 0.55 / 0.55 / 0.85 top-to-bottom) and beneath it a flat **neutral grey**, `rgb(var(--t-surface-container-highest) / 0.22)`.
- **The grey is what makes white hero copy readable over the galaxy's bright particles.** Adding more black would not: black scales every pixel proportionally, so bright spots stay bright relative to white text. Grey compresses the range from the top. Measured: white-on-background goes 5.04 → 6.67 at the brightest, 6.61 → 8.31 at bright, while dark areas move only 20.11 → 19.63.
- The grey reads strongest through the gradient's transparent **35–65% mid band** — which is exactly where the hero copy sits. That is deliberate; do not flatten the gradient to a uniform alpha.
- **Both layers live in the element's INLINE style.** A stylesheet rule for `background` loses to it without `!important` — same trap as `#nova-bg`'s inline `background:#160016`.
- **No theme guard is needed.** `#hero-tint` and `#nova-bg` are both `display: none` in light and Nova is never built there, so this is dark-only by construction.
- One element serves both duplicated trees: it is `position: fixed; inset: 0` at `z-index: 2`, above Nova (1) and below page content (10).

## Featured work = live sites only, on BOTH pages (2026-08-17)

- **Two pages carry the same three featured cards** — `index.html`’s "Recent work" (both duplicated trees) and `websites.html`’s "Recent work" section. They are **Naples Estate Jewelry · SpotCalc.com · Deepfield Gallery**, every one currently live.
- **Match the SITES across the two pages, not the wording.** `websites.html`’s copy is deliberately longer than the homepage’s — even the Naples entry differs. Keep the trio in sync; let each page keep its own voice. Elite Yacht Detailing and AuctionBuddha were removed because they have been offline since 2026-08-08 and were sending homepage visitors to a dead end.
- **Rule going forward: this section is proof, so it may only show sites a visitor can actually open.** If one of these three goes offline, swap it out rather than marking it offline — that is what the `casestudies.html` gallery is for.
- Deepfield's card is the **only external link** in the section (`target="_blank" rel="noopener"`) because it has no detail page.
- **Image budget is enforced here.** These three were once 3.2 MB and were cut to ~566 KB; they now total **383 KB**. Deepfield uses a purpose-shot `card.jpg` (1200x750, q82, 157 KB) from `scripts/capture-deepfield.js --card`, **not** the 787 KB full capture. Never point this section at a raw `assets/portfolio/**` PNG without checking its weight.
- Lives in **both** duplicated `index.html` trees — parity check is that each card title appears exactly twice.

## Websites gallery is SEVEN cards (2026-08-17)

- **Deepfield Gallery is No. 007**, `deepfieldgallery.com`, **live**. Palette `.tcg-card--gallery` (acid chartreuse `#d9f97a`). Estimate `EST 110-150 HRS`.
- **It is the one card with no detail page.** Owner chose card-only, so its foot has a single `Live Site` button instead of the `Live` + `Case Study` pair. If a `portfolio-deepfieldgallery.html` is ever built, add the second button to **both** grids.
- **Three live sites now**: `naplesestatejewelry.com`, `spotcalc.com`, `deepfieldgallery.com`. The other four gallery entries remain offline with `Ask to View`.
- `portfolio.html` reads **7 Websites** and a **7-card set** chip. The Apps stat stays 6; the `Est. 15-440 hrs` range still covers everything.
- **Adding an eighth card:** it goes in both grids, needs a new `--tcg-*` palette distinct from the seven in use, a `tcg-mobile-row--even` wrapper (8 is even), a `collectible-card.css` cache bump, and a re-run of the never-scroll sweep.

## Card-back measurement — what actually detects an over-budget back (2026-08-17)

- **A "true slack" sum of back-face children reads negative on every shipped card** (AuctionBuddha -106, Elite -55, JPSurette -52, Naples -49) because `.poke-back-inner` is a flex column with `margin-top: auto` on `.poke-links` — children heights do not sum to the container. **Do not treat a negative value from that formula as a defect.**
- **Trust these instead:** `plate.scrollHeight - plate.clientHeight` and the inner-vs-plate height delta. Across all seven cards these read **0** and **-4** identically.
- **`.poke-plate` is `overflow: hidden`.** An over-budget back therefore **clips silently rather than scrolling**, so a scroll check alone can pass while content is cut off. Check the height delta too.
- **Lazy images never load in an offscreen iframe.** Set `loading="eager"` and wait before measuring anything involving a front face, or the art reports 0x0 and the layout is not the real one.

## Canvases must be themed in JS — and screenshots DO work (2026-08-17)

- **Every `<canvas>` is invisible to contrast auditing.** `apps.html`'s hero wordmark painted `#ffffff` on the light ground at **1.12:1** (subline `#AAFF00` at 1.1:1) while every sweep reported the page clean, because `getComputedStyle` cannot see inside a canvas. It was found by **taking a screenshot**.
- **Screenshots work in the preview pane.** They returned reliably this session. The older note that the pane "never composites frames" is wrong as of 2026-08-17 — it fails only when the pane is hidden. **Look at the page as well as measuring it**; measurement alone cannot catch canvas, WebGL, or image-backed text.
- **The canvas theming pattern:** declare `colorLight` / `introTintsLight` beside the dark values, stash the authored dark values on first apply (`_colorDark`/`_tintsDark`), and swap on the **`sds-themechange`** event that `standard-site-nav.js` fires. `fractal.js` and now `surette-physics-wordmark-canvas.js` both do this. Colours are read at draw time, so a swap needs no rebuild and preserves physics state.
- **Wordmark light values are measured, not picked:** `#0b0e10` (17.32:1) and `#3f6212` (6.33:1). **Do not lighten the subline** — `#4d7c0f` is the next step toward the dark-mode lime and already fails at 4.47:1.
- Verified by sampling rendered canvas pixels in both themes; dark still paints `#ffffff` / `#AAFF00` exactly as before.

## `process.html` retired (2026-08-17)

- **The page is deleted.** Netlify 301s `/process.html` and `/es/process.html` → `/websites.html#how-it-works`.
- **Its content split by audience, not by convenience.** The migration story it carried was explicitly software (`BUSINESS SOFTWARE` tag, "when the project is custom business software"), so it went to **`apps.html#software-process`** — putting it on `websites.html` would have been wrong. Only the *Maintain* detail list was website content, and that went into `websites.html`'s "Hosted, backed up, monitored" card. The rest was duplicate and dropped.
- **`websites.html#how-it-works` is now a load-bearing anchor** — it is the redirect target, both service pages' "View Our Process" CTAs, and `rail.js`'s `launch` step. Do not rename that id.
- **`apps.html`'s "View Our Process" button is a same-page anchor now** (`#software-process`), not a cross-page link.
- **The process ladder is now 3 → 4 → 8**: homepage (hook), `websites.html` (detail), `services/website-design-hosting.html` (full reference). The fourth surface is gone.
- **Validator baseline dropped to 68** (was 69) — one fewer English page, one fewer expected missing-`es`-mirror.
- **`assets/rail.js` is deleted (2026-08-17).** It was dead code — no page carried the `#rail-popover` element or `[data-step]` triggers it needed, so it returned immediately on all 13 pages that loaded it. Removed with its 13 script tags and 76 lines of orphaned `.rail-popover-*` CSS across 9 pages.
  - **There are two unrelated "rails" in this repo. Never sweep on the bare word.** The live one is the Admin Center sidebar under the `client-admin-*` prefix (`client-admin-tabs-rail__brand`, `client-admin-rail-link`, styled in `client-portal.css`); `nav.css`'s "rail" mentions are layout comments about desktop padding. Only `.rail-popover-*` and `rail.js` belonged to the dead process rail.
  - `[id^="step-"] { scroll-margin-top: 6rem; }` in `index.html` sat directly after the deleted CSS block and was **kept on purpose** — it serves the three homepage process cards.

## Homepage "How it works" — funnls process structure (2026-08-17)

- **Three steps, not four:** `01 Discovery call` → `02 Design & build` → `03 Launch & care`. Modelled directly on funnls.com's block. The retired `Manage` and `Support` cards were never process steps — they were the ongoing service, already covered by the "what we build" cards and the WhatsApp section below. Their content folded into `03`. **Do not add a fourth card**; that reintroduces the overlap.
- **The headline carries the commitment**, which is what makes the funnls block work: *"From first call to live in about 1–2 weeks."* over an anti-friction subhead.
- **The 1–2 week figure is the owner's own published One Page estimate** (`websites.html:167`), not an invention, and **its qualifier is load-bearing** — "A One Page site is usually live in 1–2 weeks once content and access are in hand; larger builds run longer." A delivery timeline is a factual claim about the business, the same class as a star rating or a "trusted by N businesses" counter. **Never restate it unqualified, and never raise it without the owner.** The owner was offered the unqualified version on 2026-08-17 and chose the qualified one.
- Step copy is outcome-focused ("more calls, quotes, and booked work"), per funnls, not deliverable-focused.
- **Lives in both duplicated trees.** Parity check: `HOW IT WORKS`, `Discovery call`, `Design &amp; build`, `Launch &amp; care`, `live in about 1` must each appear **exactly twice** in `index.html`.
- Desktop grid is `md:grid-cols-3`. Verified 1920→320px: one row, equal heights, zero overflow; contrast 0 failures in both themes.
- **The site now has a 3 → 4 → 8 process ladder** — homepage (3, the hook), `websites.html` (4, the detail), `services/website-design-hosting.html` (8, the full reference). This is deliberate progressive disclosure. `process.html` is a fourth surface and is in no nav.

## Navigation & IA — condensed (2026-08-17, supersedes the 2026-08-16 section below)

- **Top-level nav is `Websites · Software · Pricing`** + the icon-only Client Login pill and the "Get started" CTA. No Services dropdown.
- **`Home` is shown on every page EXCEPT the homepage (2026-08-17)** — as the first desktop pill *and* as the first hamburger-dropdown row. The logo still links home everywhere; this makes the route explicit once you have navigated away, without spending a slot on a self-link. `isHomePage()` in the generator checks **both** `data-active === "home"` **and** the URL, because a page that forgets `data-active` would otherwise render a Home link pointing at itself — and because **Netlify Pretty URLs serve the homepage as `/`, not `/index.html`**, the pattern accepts a bare directory while still not matching extensionless Pretty URLs like `/account`. Homepage dropdown is **5 rows**, every other page **6**.
- **The phone bottom tab bar keeps Home on every page, on purpose.** It is a tab bar, not a menu: it holds a constant set and marks the current one. Removing a tab on one page would shift the other three under the user's thumb, so the same tap position would hit different destinations depending on the page. Do not "fix" this inconsistency.
- **Nav width is now the constraint to watch.** The row carries four pills plus the login icon, theme toggle and CTA. Verified fitting with zero overflow down to **880px**, the narrowest width the desktop bar renders at. Adding a fifth pill would need re-measuring there first.
- **The `Work` item was retired.** It resolved to `portfolio.html`, a two-card interstitial whose cards led to `apps.html` — *already the Software item* — and `casestudies.html`. Three of four nav items showed built work and none predicted which. **Do not reintroduce a portfolio/work/case-studies item as a fourth sibling.**
- **Galleries hang off their parent service page:** Websites → `websites.html` → `casestudies.html` → 6 detail pages; Software → `apps.html` → `app-catalog.html` → 6 app profiles. Both links already existed (`websites.html:230`, `apps.html:201`), so no content edits were needed.
- **Pricing covers both product lines.** `pricing.html:466` routes to `app-pricing.html` under "Software, not a website". `app-pricing.html` is still in no menu directly, which is fine — Pricing is its front door.
- **`portfolio.html` stays live with `data-active="none"` and is deliberately NOT redirected.** It has 6 inbound in-page links (`apps.html`, `index.html` ×2, `pricing.html`, `websites.html`, self) and legitimately serves anyone searching "portfolio". A redirect would strand those links.
- **Valid `data-active` values are now** `websites`, `software`, `pricing`, `home`, `account`, `contact`, `none`. `work`, `portfolio`, `apps`, and `services` still resolve via `LEGACY_ACTIVE` in the generator — note its `portfolio` entry was repointed from `work` to `websites`.
- The bottom tab bar was untouched (Home · Websites · Pricing · Contact) — it never carried Work or Software.
- **funnls.com is the named design reference.** Its four items carry no portfolio at all; the nav follows the visitor's decision path and proof lives inside the pages. Consult it before any IA change. Full rationale and the rejected alternatives are in `project-docs/NAV_RESTRUCTURE_PLAN.md`.
- → `standard-site-nav.js?v=20260817-nav-condense` (31 pages). Verified 12 pages × 5 widths (1280/1024/880/879/390): zero overflow, desktop row fits at every width, exactly one active pill on all 15 pages checked, zero nav links to `portfolio.html`, no duplicate dropdown destinations. Validator at the 69 baseline.

## Bulk-edit encoding rule (2026-08-17)

**Never bulk-edit `.html` with Windows PowerShell `Get-Content`/`Set-Content`/`Out-File`.** PowerShell 5.1 decodes UTF-8-without-BOM as Windows-1252, so a read-modify-write round-trip mojibakes every curly quote, em-dash and middot. It happened this session across 31 files during a cache-buster bump; it is **silent** (the intended edit lands correctly) and was caught only by the validator jumping 69 → 271. Repaired losslessly by re-encoding UTF-8 → CP1252. Use `[IO.File]::ReadAllText/WriteAllText` with `UTF8Encoding($false,$true)`, the editor's edit tooling, or git-bash `sed` — and **always re-run the validator after a bulk pass**. See DECISIONS.

## Layout system — use it on every new page (2026-08-16)

`assets/nav.css` owns the shared page vocabulary. **Do not invent another page prefix**; that is exactly how `.pr-*` and `.wb-*` drifted apart on nine of twelve shared values before being consolidated.

| Class | Use |
|---|---|
| `.sds-wrap` | page container, `min(1240px, 100% - 3rem)` |
| `.sds-section` | vertical rhythm between sections (88px) |
| `.sds-eyebrow` | small-caps mono label above a headline |
| `.sds-h1` / `.sds-h2` / `.sds-h3` | heading scale |
| `.sds-lead` / `.sds-body` / `.sds-fine` | copy scale; add `.sds-lead--center` to centre |
| `.sds-card` / `.sds-panel` | surfaces (card has `position: relative` for badges) |
| `.sds-btn` / `.sds-btn--solid` / `.sds-actions` | buttons and button rows |
| `.sds-grid-2` / `-3` / `-4` | responsive grids, breaking at the site's 880 line |
| `.sds-media` | card art, 16/10 cover, top-anchored |
| `.sds-hero-secondary` | outlined hero action sized to the newsletter row |

Page-specific components still belong in the page. The test: if a second page would want it, it goes in `nav.css`. Current page-local survivors are `pricing.html`'s billing toggle / badge / price block / feature list / route bands / FAQ, and `websites.html`'s proof card / step numeral / icon chip / list.

## Homepage structure (2026-08-16)

- **Order:** hero (with proof strip) → what we build → agentic AI → recent work → local services → process → support → testimonials → newsletter.
- **All three key pages carry a hero proof strip, each with a different quote** matched to that page's claim — index (managed service), websites (responsiveness), pricing (update turnaround). All three of the site's testimonials are now doing work above the fold instead of only appearing in one section near the bottom of the homepage. Use `.sds-proof--start` on left-aligned heroes (websites) and plain `.sds-proof--hero` on centred ones (index, pricing).
- **The hero proof strip carries only already-published, attributed proof.** It is a verbatim quote from this page's own testimonials section. **Do not add a star rating or a "trusted by N businesses" counter** unless the owner supplies a verified figure — those are factual claims about the business, not copy.
- It must stay **above the fold on a 1280×768 laptop**. It was first placed in the hero's bottom band, which put it at `top: 870` — visible only after scrolling, which defeats its purpose. It now sits directly under the hero lead.
- **Homepage `<title>` and meta are website-first** ("Websites for Southwest Florida Businesses"). They led with "Agentic AI-Powered Business Software" until 2026-08-16 — reordering the page body did not change what search and social shares displayed, so metadata needs updating alongside any positioning change.
- **The hero stays centred over the Nova WebGL background.** A left-copy/right-visual hero was considered and rejected — it fights a full-bleed animated background. Hero is two-button: `Get started` + `See pricing`.
- **"What we build" is 2-up (Websites, then Software).** It was a flat 4-card grid; the two local-service cards moved into their own compact "Also local" strip.
- **That strip is load-bearing for discoverability.** `services/in-home-services.html` and `services/office-network-setup.html` have had **no nav entry** since the Services dropdown was retired in Phase 1. The homepage strip and the footer are their only routes. Do not remove it without giving them a nav home first.
- **index.html keeps duplicated desktop and mobile trees.** Every content edit must land in both or they drift. After editing, assert parity by counting shared strings (e.g. "WHAT WE BUILD" should appear exactly twice).
- **Auditing the mobile tree:** `querySelector('.md\\:hidden')` returns the **nav's** injected mobile shell, not page content, and an audit using it silently reports zero cards. Use `document.querySelector('#hero-mobile').closest('.md\\:hidden')`.

## Primary CTA (2026-08-16)

- **"Get started" is the sitewide primary CTA verb** — 21 anchors, including the nav's gradient button. Use it for any primary conversion action on a new page.
- **Do not convert these**, they mean something specific: `Request Access` (access-by-request apps, decided 2026-08-08), `Request Custom Version`, `Request account deletion`, form submit buttons, and **every collectible-card foot button** (`Ask to View`, `Case Study`, `Open Profile`, `Live Site`, `View Live`) — those are width-constrained and anything past ~105px scrolls AuctionBuddha's card back at 320px.
- The phone bottom tab bar's **Contact** stays a destination label, like Home/Websites/Pricing. It is a tab, not a call to action.

## Websites story page (2026-08-16)

- **`websites.html` (root)** is the Websites nav destination: what you get, how it works (4 steps), recent work, two ways to buy, also-available strip, closing CTA. **No plan tables** — pricing lives on `pricing.html` (summary) and `services/website-design-hosting.html` (full reference).
- Its proof cards are **plain links, not the collectible-card component**, on purpose: that component has a fixed 2.5/3.5 ratio and a back face under a documented never-scroll budget, none of which this page needs.
- Page-scoped CSS uses the `.wb-*` prefix, mirroring `.pr-*` on pricing.html. Phase 5 should promote the shared pieces into `nav.css` rather than letting a third prefix appear.
- **Watch image weight on new pages.** The obvious portfolio screenshots run 1–2 MB each; this page's three proof cards were 3.2 MB before being swapped to 567 KB of equivalent shots. Check file size before referencing anything from `assets/portfolio/`.

## Website Pricing — two pages (2026-08-16)

- **`pricing.html` (root) is the front door**, linked from the nav. Four cards: Starter $125 · Lead Capture $250 · **Local Business $375–500 (most popular)** · Growth $750, plus a monthly/annual toggle, three escape-hatch bands, and six FAQs.
- **`services/website-design-hosting.html` keeps its URL** and is now the full-detail reference (H1: "Full Website Pricing Details"), reached from the nav's Websites item and from "Full pricing details" links. Nothing was removed from it.
- **The two pages must stay consistent.** `website_pricing_plan.txt` is still the single source of truth; a price change there has to land in **both** pages.
- **The fifth managed plan (Mini, $185) is intentionally not on the front page.** It is still sold and fully documented on the detail page, and the front page's fine print points at it. Dropping it is what produces the four-tier ladder; do not treat its absence as a bug.
- **Annual prices are real prepaid figures, not monthly × 12** ($1,250 / $2,500 / from $3,750 / from $7,500). Prepaying is discounted, so never compute the annual column — each price element carries its own `data-monthly` / `data-annual` string.
- Page-scoped CSS uses the `.pr-*` prefix and is entirely token-driven; it was built from `app-catalog.html`'s head (a single-responsive-layout page) so its scaffolding matches a known-good page exactly.

## Navigation & IA (2026-08-16)

- **Top-level nav is `Websites · Software · Work · Pricing` + the Contact CTA.** Client Login is an icon-only pill. There is **no Home item** — the logo carries it, following funnls.com.
- **No Services dropdown.** Do not reintroduce one; if a local-services entry is ever needed, add a plain item. In-home tech and office network setup are reached from the footer (and, once Phase 4 lands, a homepage strip) — their `data-active` is `none`.
- **Valid `data-active` values are now** `websites`, `software`, `work`, `pricing`, `home`, `account`, `contact`, `none`. The old `apps`/`portfolio`/`services` still resolve via the `LEGACY_ACTIVE` map in the generator, so an un-remapped page degrades gracefully rather than losing its highlight.
- **Active state is a filled pill**, `.sds-nav-pill.is-active` in `nav.css`. All pills share a `min-height` so the icon-only Client Login pill does not sit 1.2px short.
- **`WEBSITES_HREF` and `PRICING_HREF`** at the top of `assets/standard-site-nav.js` are the single place to repoint the two items whose dedicated pages are still being built. They currently resolve to the live pricing page so the nav cannot 404.
- **`assets/mobile-services-nav.js` is unreferenced and must stay that way.** It identifies the Services tab by regex-testing hrefs for the string "services" — the Websites tab's href (`services/website-design-hosting.html`) matches, so re-adding the script would attach the retired popout to the wrong tab.

## Theming (2026-08-16)

- **The site has two themes.** Dark is the default and is visually unchanged; light is opt-in through a toggle in the shared nav. Choice persists in `localStorage` as `sds-theme` and syncs across open tabs. A visitor with no stored choice gets **dark regardless of `prefers-color-scheme`** — deliberate, see DECISIONS.
- **How it works: values flip, selectors never do.** `assets/nav.css` defines the whole Tailwind palette as space-separated RGB triplets (`--t-void-black: 5 5 5`) with light values under `:root[data-theme="light"]`. Every page's inline `tailwind.config` reads them as `rgb(var(--t-NAME) / <alpha-value>)`. **The triplet form is mandatory** — `<alpha-value>` only works inside `rgb()`, so storing the palette as hex silently breaks every `/10` `/60` opacity variant.
- **Never hardcode a palette colour.** A literal defeats the theme with no error. This bit hardest in `nav.css`'s hand-pinned utility fallbacks (`.text-starlight-white` etc.): they exist because injected markup can't rely on the Tailwind CDN, but `nav.css` loads *after* the CDN's style block, so a literal there overrides the correct variable-backed rule. Fourth confirmed bug from the injected-markup/Tailwind interaction.
- **Pre-paint resolver is load-bearing.** `<script id="sds-theme-resolver">` is the first script in all 34 heads and stamps `data-theme` before anything paints; it also overwrites the `<html>` inline `style` background, which outranks every stylesheet. `#dm-critical-dark-baseline` keeps hex literals on purpose — it runs before `nav.css` defines any variable. **Any new page needs both**, or light mode flashes black on every load.
- **`starlight-white` does two jobs.** As body/heading text (301 uses) it must flip to ink; on the cyan→purple gradient CTAs (3 uses + the nav CTA) it must stay white. Use `.sds-on-accent` / `--c-on-accent` for the latter.
- **Veils use channel variables.** `--c-veil-rgb` (white→near-black) and `--c-shadow-rgb` carry each call site's own alpha: `rgba(var(--c-veil-rgb), 0.07)`. Property matters — black used as a *panel fill* must invert, black used as a *shadow* must not; sweeping them together is what left mid-grey panels on the pricing page.
- **Light text is sized against the darkest light surface** (`--t-surface-container-highest`, `#e5e2db`), not the page ground. Sizing against the ground passes at 4.56:1 there but lands at 4.28:1 on cards, which is where most small label text sits.
- **The homepage has NO animated background in light mode (2026-08-16).** Nova is gated off, not just hidden: `index.html`'s module wraps the engine in `if (SHOW_NOVA)`, so in light the galaxy is never built and no render loop runs. `#nova-bg` and `#hero-tint` are `display: none !important` — `!important` is required because `#nova-bg` carries an inline `background:#160016` attribute, which outranks stylesheets. Toggling *into* dark on the homepage reloads once, because the engine only initializes at module evaluation. Dark is untouched; the engine text was wrapped, never edited.
- **Tailwind's stock `white` is overridden in every config** (`"white": "rgb(var(--t-starlight-white) / <alpha-value>)"`). It is a built-in, not part of the custom palette, so it was missed by the first tokenisation pass — 138 usages stayed literally white, including the homepage `<h1>` at 1.12:1 on paper. **Do not try to fix stock colours with a `nav.css` pin:** the Tailwind CDN injects its `<style>` *after* nav.css and emits a hard literal for built-ins, so the CDN wins at equal specificity. The config is the only place that works. (Custom colours seem to pin fine only because both sides resolve to the same variable.)
- **Backgrounds have a light "blueprint" variant.** `cosmic-web.css` inverts its existing hero photo (`filter: invert(1)`), so every overlay colour in that block is written as a **pre-inversion complement** — the salmon and lime values render as teal and violet. `fractal.js` re-derives fog and particle colours on the `sds-themechange` event, since WebGL can't read CSS variables.
- **Portal utility pages** (`account-admin/-users/-settings/-created`, `app-checkout`) have no nav, so no toggle — by design. They still honour the stored theme through the head resolver.
- **`multichannel-commerce-website-test-4.html` is outside the theme system** — it's the unlinked noindex experiment and is already a light cream page. Excluded from every sweep.
- **Verification is by measurement**, not screenshots (the preview pane doesn't composite frames). `scripts/audit-contrast.js` composites translucent ancestors to resolve real backgrounds. Current: homepage 0 failures / 99 checked, casestudies 0 / 182, pricing 0 / 1,246, dark mode 0 with probes byte-identical to pre-change values.

## Site State

- Static Dark Matter site with English root pages only; Spanish `es/` mirrors are absent from this working copy.
- Main navigation: Home, Services (dropdown), Portfolio, Client Login, Contact Us. `portfolio.html` is the top-level Portfolio hub and links onward to the existing Apps (`apps.html`) and Websites (`casestudies.html`) pages. Process no longer appears as a top-level nav item — it is reachable through service-page CTAs.
- `apps.html` is the canonical Surette Data Systems app-library page; old `/downloads` URLs redirect here.
- Sean's Google Ads source is intentionally absent from this repo and must not be recreated here. **`seansads.com` itself is offline as of 2026-08-08**, so the site no longer links to it.

## Current Features

- **Portfolio: only Naples Estate Jewelry and SpotCalc are live (2026-08-08).** The other four Websites entries (Elite Yacht Detailing, JPSurette, Sean's Ads, AuctionBuddha) are offline. Their gallery cards show `status - offline` and an `Ask to View` button to `contact.html` in place of the live link; their detail pages use the new `.portfolio-live-widget--offline` variant (or, for Sean's Ads, rewritten hero/section copy). **`NaplesEstateJewelry.co` became `.com`** in the same pass, everywhere. **The card button label is "Ask to View" for a fit reason, not a style one** — `Request Viewing` wrapped the `.poke-links` row and put AuctionBuddha's back into a 24px scroll at 320px; see DECISIONS before changing it. Verified all 12 card backs at 320/880/1280px with zero overflow, and zero remaining links to the four dead domains sitewide.
- **Five of six apps are offline; access is by request (2026-08-08):** Auction House, SDMS, Benji, Third Street / Antique Mall, and MetalsPredictor no longer have public demo instances. Each profile page carries a shared `.app-access-notice` panel (amber, defined once in `assets/surette-data-systems-app-profile.css` — **do not copy it into a page's inline `<style>`**), a `Request Access` primary CTA to `contact.html`, and an eyebrow that says access is by request. Every outbound link to the retired hosts was removed sitewide, including a stray `benjipayroll.netlify.app` button on `app-pricing.html`. On `app-catalog.html` the `.tcg-stat-pill` now carries **availability, not delivery type**: five cards read `By Request`, SpotCalc reads `PWA`. Verified at 320px and 1280px: notice renders styled on all five, zero dead external links, zero page overflow. → `surette-data-systems-app-profile.css?v=20260808-access-notice` (6 pages).
- **MetalsCalc renamed to SpotCalc (2026-08-08):** site-wide brand rename across the app catalog, the app profile, both of `casestudies.html`'s duplicated grids, and `portfolio-metalscalc.html` — 44 brand strings. The app link is now `https://app.spotcalc.com` and the marketing link `https://spotcalc.com`. SpotCalc is the **only** app still live. **Filenames and asset directories intentionally keep the `metalscalc` spelling** (`metalscalc-buying-calculator.html`, `portfolio-metalscalc.html`, `assets/apps/metalscalc/`, `assets/portfolio/metalscalc/`) so live URLs don't move — see DECISIONS for why that case split makes the rename scriptable. The card palette class became `.tcg-card--app-spotcalc`, so `collectible-card.css` was cache-busted to `?v=20260808-spotcalc` (2 pages) or returning visitors would lose the card's purple. Re-verified the never-scroll rule on all 18 card backs (12 casestudies + 6 app-catalog) at 320px: zero vertical and zero horizontal overflow.
- **Directional mobile bottom-nav reveal (2026-08-01):** the shared Home / Services / Portfolio / Contact bar now slides and fades out after 24px of downward travel and returns after 6px of upward travel. It is always visible at the top, ignores small direction jitter and height-only mobile-browser chrome resizes, resets outside the <880px mobile range, and closes the Services popout before leaving. Keyboard input reveals it immediately and keeps it available; `prefers-reduced-motion` removes the transition while preserving the behavior. The motion is deliberately gentle: 460ms ease-in/out while leaving and 520ms spring-like ease-out while returning, with slower opacity fades. Implemented once in `standard-site-nav.js` / `nav.css`; 29 nav-loader references use `standard-site-nav.js?v=20260801-scroll-hide3` and 34 stylesheet references use `nav.css?v=20260801-scroll-hide3`. Verified on a root page and a nested `services/` page at 390px, plus the 879/880px boundary, with zero overflow. Validator: 67 known baseline items.
- **Spatial concept hero-copy contrast fix (2026-08-01):** the explanatory paragraph in `multichannel-commerce-website-test-4.html` now sits on a deep navy translucent panel with a lime edge, subtle shadow, and fully white text. The spatial grids remain visible around it but no longer cross the copy. Verified at 320 and 636px with zero horizontal overflow.
- **Unlinked spatial multichannel concept (2026-08-01):** `multichannel-commerce-website-test-4.html` is a noindex, standalone visual test that clones the complete multichannel product story into the cream/cobalt/coral/lime editorial style of the requested Treasure Island reference. It includes the live eBay, Etsy, Instagram, and Facebook destinations plus the image-analysis and natural-speech AI Listing Assistant workflow. It has no inbound site links and does not replace the current sales page. Verified at 320, 390, and 1440px with zero document overflow and all three local showcase images loaded. Validator: 67 known items (the documented 66 plus this test page's expected missing Spanish mirror).
- **Multichannel mobile gallery containment fix (2026-08-01):** the first customer-experience gallery card no longer overflows narrow phones. Its 16:10 aspect ratio had combined with the 250px mobile minimum height to force a 400px-wide card inside a ~327px container. At 560px and below, gallery figures are now explicitly constrained to `width:100%; min-width:0`, and the first figure drops the conflicting aspect ratio while retaining its intended 250px image height.
- **Multichannel page header logo repair (2026-08-01):** `multichannel-commerce-website.html` now carries the established forced-flex styling for both `#sds-logo` and `#sds-logo-mobile` and explicitly initializes both mounts. The mobile brand no longer stacks its canvas above the wordmark. Header geometry and the 879/880px menu switch were verified at 320, 375, 414, 767, 768, 879, 880, 1024, 1280, 1440, and 1920px; the account/hamburger controls and all desktop nav items remain clear.
- **Complete Multichannel Commerce Website sales page (2026-08-01):** `multichannel-commerce-website.html` packages the Naples Estate Jewelry commerce foundation for jewelry sellers, dealers, galleries, antique/vintage businesses, collectors, resale/consignment, and specialty retail. Positioning is "List it once. Sell everywhere."—the AI Listing Assistant analyzes uploaded product images, accepts a naturally spoken description, and fills the listing form for human review and approval; that one approved record then powers the branded store, eBay, Etsy, Instagram, and Facebook without repetitive listing work. Marketplace publishing and Instagram/Facebook product and advertising publishing are presented as live. The hero uses the current live-store capture `assets/portfolio/naples-estate-jewelry/home-live-20260801.png`; the matching 1200x630 sharing card is `assets/multichannel-commerce-og-v2.png`. The page is linked from a featured commerce banner on `apps.html`; it is not yet in the shared header navigation, and pricing remains undecided.
- **Homepage repeated objects are symmetric at every layout tier (2026-08-01):** the Services heading/copy and all other section-heading groups are centered; the four wide-layout service cards render 2x2 from 880-1279px and one 4-card row from 1280px upward; each service card is itself a full-surface link; and repeated cards/quote boxes use equal-height grid tracks on both desktop and phone layouts. A 320-1920px sweep confirmed no orphan rows, uneven repeated-object heights, misaligned section headers, incomplete card hitboxes, or horizontal overflow.
- Homepage hero background is the Nova particle-galaxy WebGL widget (`nova/nova-widget.html`), mounted as a fixed full-viewport `#nova-bg` div at `z-index:1`. A fixed `#hero-tint` scrim at `z-index:2` anchors the gradient overlay so it doesn't scroll away. The homepage integration promotes `#nova-bg`/canvas to a composited layer and ignores height-only mobile viewport resize events so browser toolbar changes do not reallocate WebGL during scroll. The old black-hole Three.js scene is gone. Only the homepage integration and the `CONFIG` block should be changed; leave the Nova engine copy alone unless explicitly needed.
- Non-home pages use the shared cosmic-web background system (`assets/cosmic-web.css`).
- Services pages: Website Design &amp; Hosting (`services/website-design-hosting.html`, merged from the former separate Website Design, Managed Hosting, and Website Care Plans pages; now a complete public pricing page implementing `website_pricing_plan.txt` — managed plans, one-time builds, care plans, policies, add-ons, FAQ), Custom Business Web Apps (links to `apps.html`), In-Home Tech Services, Office Network Setup. Custom Apps, In-Home, and Office Network pages include "View Our Process" CTAs.
- Service pages share the Surette Data Systems footer block with legal/resource links and `info@SuretteSystems.com`; their separate mobile markup path now includes the standard fixed mobile logo/header, promoted mobile nav row with Services popout, and a compact mobile footer inside the `md:hidden` wrapper.
- **Navigation single source of truth (2026-07-15):** every public marketing/site page now renders its entire nav (desktop header, mobile header + hamburger dropdown, mobile bottom tab bar) from `assets/standard-site-nav.js` via one script tag: `<script src="assets/standard-site-nav.js?v=20260801-scroll-hide3" data-active="...">` (version current as of 2026-08-01). Valid `data-active` keys: `home`, `services`, `portfolio`, `apps` (maps to portfolio highlight), `account`, `contact`, `none` (no item highlighted — legal/utility pages). Pages under `services/` use `../assets/...` and the loader auto-prefixes all emitted links with `../`. `assets/unified-mobile-menu.js` (the legacy runtime retrofit shim) was deleted; no page carries inline nav markup anymore. **Do not hand-copy nav HTML into a page — add the loader tag instead.** The shared mobile pattern is a fixed top logo/header with icon-only account control before the hamburger, an accessible dropdown, and a fixed bottom nav; the desktop header is used from 768px upward. Exception: portal utility pages (`account-admin`, `account-users`, `account-settings`, `account-created`, `app-checkout`) intentionally keep their own minimal chrome.
- **The site's mobile/desktop breakpoint is 880px — one line for everything.** Below 880: hamburger, bottom tab bar, mobile page layout, mobile footer. At 880+: full header with Contact CTA, wide layout, wide footer. Two halves keep this true and must stay in step:
  1. **Tailwind `md:`** is retargeted to 880 via `"screens": {"md": "880px"}` in each page's inline `tailwind.config` (and in `assets/tailwind-nav-config.js`). **A new page must include it**, or its layout will flip at Tailwind's stock 768 while the nav flips at 880 — which is exactly the bug that produced a mobile bar over a squeezed desktop grid.
  2. **Hand-written media queries** use `max-width: 879.98px` / `min-width: 880px`. Never write 767/768 in site CSS.
  Exceptions, on purpose: `account-admin/-settings/-users` + `app-checkout` (no `md:` utilities, no nav loader), and `client-portal.css` (700/860px are its own component reflow points).
- Homepage wide hero (`#hero`) is `min-height: min(100vh, 1000px)`; mobile hero (`#hero-mobile`) is `min-height: min(74vh, 700px)`. The 74vh ratio is tuned for phones; the 700px cap stops the flex stage from opening a ~450px void above the Contact CTA on tall tablet-sized windows that still get the mobile layout (<768px). Reach for the cap, not a width breakpoint — a breakpoint jumps the hero the moment it is crossed. The `min-h-[100vh]` class on the section is overridden by this rule.
- Favicon (2026-07-16) is the Material Symbols `blur_on` glyph in electric cyan on the void-black rounded square — the same mark the shared footer uses. Source of truth is `assets/favicon.svg`; the PNGs (`favicon-32/192/512.png`, `apple-touch-icon.png`) are rasterised from it, so **regenerate them from the SVG rather than editing them directly**. apple-touch-icon is intentionally an opaque square (iOS masks and rounds it itself); the others keep transparent corners. All favicon links carry `?v=20260716-blur-on` — bump it when the icon changes or browsers will keep the old one.
- **Footer single source of truth (2026-07-16):** every public page injects its footer from `assets/standard-site-footer.js` via one tag — `<script src="assets/standard-site-footer.js?v=20260716-shared-footer">` (add `data-variant="desktop"` / `"mobile"` inside each wrapper on the two-layout pages; `../assets/...` under `services/`). No page carries inline footer markup, and the old `.app-footer` / `.service-mobile-footer` / `.portfolio-hub-footer` variants are gone. **Do not hand-copy footer HTML into a page — add the loader tag.** Two variants match the homepage: `.dm-footer` (≥768px, full link list + tagline) and `.dm-footer-mobile` (<768px, centered, short labels, brand mark). Styled as plain CSS in `nav.css`, deliberately: the Tailwind CDN only builds utilities present in a page's *static* HTML, so injected markup cannot rely on them (`text-on-primary-container/60` rendered white when tested that way). Same reason `nav.css` pins the hamburger line colours.
- Horizontally-scrollable tables self-announce: `assets/table-scroll-hint.js` nudges any overflowing table ~52px right and back **and repeats every ~4s of stillness, up to `MAX_NUDGES = 6`, while the table is in view and the reader hasn't engaged** (2026-07-16 — one nudge was easy to miss if it fired while they were looking elsewhere; the cap keeps ~30s of intermittent motion clear of WCAG 2.2.2). Tablet/mobile only, skipped under `prefers-reduced-motion`. It finds the scroll container by walking up from every `<table>`, so new tables are covered automatically — do not add a per-table class or opt-in. Loaded on `services/website-design-hosting.html`, `account-admin.html`, `account-users.html`. **Because it loops rather than firing once, the stop conditions are the load-bearing part** — any interaction (`pointerdown`/`touchstart`/`wheel`/`keydown`) ends it permanently, a table the reader already scrolled is never touched, leaving the viewport pauses and rewinds it (so a half-finished nudge isn't later misread as the reader having scrolled), and switching to reduced-motion or a desktop width mid-session stops/parks it. The cap is checked at the top of `run()`, not only where the next nudge is queued, because a table that goes offscreen mid-cycle re-enters via `evaluate()` and would otherwise walk past a spent budget.
- The fixed mobile bottom tab bar (5rem) covers the end of the page, so `nav.css` gives every footer `margin-bottom: calc(5rem + env(safe-area-inset-bottom, 0px))` under 768px — one rule owns this. Use a **margin on the footer, not padding on the body/wrapper**: the footer must stay flush against the bar so the bar's blur sits over the footer's own background; padding lifts it and exposes the fixed starfield behind. Do not add per-page bottom padding for bar clearance (that double-counts — `index.html`'s `mb-20` and the removed service-wrapper `pb-24` were the old per-page way).
- The fixed nav measures ~101px at ≥768px and 64px for the mobile header. Anything relying on an anchor offset must clear its own nav: the pricing page uses `section[id] { scroll-margin-top: 145px }` with a `110px` override under 768px. A single shared value does not work for both.
- **Wide footer legal block is a three-line stack (2026-07-16):** `© 2026 Surette Data Systems` / `Call or text (239) 404-8505` / `Southwest Florida`, right-aligned from 1024px up. Deliberately three short lines rather than two long ones — the location used to share the copyright line, which made it 282px wide; alone it drops the column to 162px. The phone footer keeps its own single centred paragraph (no phone number) and is a separate format.
- **Footer legal/tagline text is phrase-atomic (2026-07-16):** the shared footer builds its legal line, phone line, and tagline from `<span>` phrases via the `phrases()` helper in `standard-site-footer.js`, each `white-space: nowrap` in `nav.css`, so they wrap *between* phrases and never inside one. **Do not collapse these back to a plain string** — as one string the phone split as "404-" / "8505" (at 1440px, not just on phones) and "SOUTHWEST FLORIDA" stranded "FLORIDA". `.dm-footer__legal` also has `flex-shrink: 0` at ≥1024px: `space-between` had squeezed it to 159px against the 282px it needs, and the link column is the one that should give (it already wraps). The phone is a `tel:+12394048505` link; the phone footer deliberately carries no phone number (it mirrors the homepage's). Note `.footer-meta` / `.footer-meta-sep` in `nav.css` are leftovers from the pre-consolidation footers and now style nothing.
- **Fixed nav bar tint (2026-07-16):** the desktop header, phone header, and phone bottom tab bar get `background: rgba(8, 10, 14, 0.9)` from `assets/nav.css`, keyed to `.dm-standard-desktop-nav` / `.dm-unified-mobile-header` / `.dm-standard-bottom-nav`. **Do not move this back to a Tailwind utility** — it was `bg-surface/10` on injected markup, a class no page carries in static HTML, and at 10% the bar labels measured 1.17:1 against the galleries' light cards. The tint nearly matches the page background, so dark pages look identical at any alpha; it only matters where light content scrolls under. If a gallery's card colours change, re-check these.
- **Fixed nav bar hairline borders (2026-07-21):** the same three bars' 1px borders (`border-bottom` on both headers, `border-top` on the bottom tab bar) are pinned in `nav.css` right below the tint rule — `rgba(255,255,255,0.1) !important`. The injected markup's `border-white/10` / `border-starlight-white/10` utilities exist in no page's static HTML and rode the CDN's DOM observer, which missed on portfolio.html, leaving Tailwind's preflight fallback (`#e5e7eb`) — a solid light-gray line under the mobile header and above the bottom bar. Third confirmed instance of the injected-markup Tailwind failure (see DECISIONS). The utility classes remain in the generator markup; the CSS pin always wins.
- **Apps-page wordmark has no knock-loose interaction at ANY width (2026-07-21; mobile-only earlier the same day, then owner removed it everywhere):** `#wm-canvas { pointer-events: none !important; }` in `apps.html`'s base styles removes hover-drop, click/tap release, and the module's dblclick release-all at every viewport; the dead rect-corrected handler re-registrations were deleted from the page's inline script. The load intro (light-up → letters fall → settle) is frame-driven and unaffected. The original mobile motivation also holds: the engine's `touch-action: none` + `preventDefault()` on a canvas covering the top ~430px of the phone viewport was eating swipes. `terms.html`'s wordmark keeps its interactions (page-scoped CSS; shared module untouched). The apps loader's brief `overflow: hidden` while its overlay is up was reviewed and intentionally kept.
- **Mobile dropdown has one contact entry (2026-07-21):** the gradient "Contact Us" CTA only (mirrors the desktop header's plain-links-plus-gradient-CTA pattern). The redundant plain "Contact" row above it was removed from the generator; the bottom tab bar's Contact item carries the active-page highlight for contact.html. All injected menu surfaces (desktop nav, dropdown, bottom bar, services popout, both footers) audited zero duplicate destinations within any single menu.
- **All fixed background canvases now survive mobile scroll without stutter (2026-07-21):** `fractal.js` (shared Three.js background on `apps.html`, `process.html`, `account.html`) previously resized its renderer on every window resize — mobile browser-chrome collapse fires height-only resizes mid-scroll, and each `renderer.setSize()` WebGL reallocation was a visible stutter. Now: debounced 180ms, mobile height-only changes skipped (mirrors `shouldResizeNova()` in index.html), and the `#canvas-container`/canvas on all three pages carry the `#nova-bg` compositing treatment (`translate3d`, `backface-visibility: hidden`, `contain: strict`). `surette-physics-wordmark-canvas.js` (`apps.html`, `terms.html`) got the same-class guard — its `_resize()` now skips when width/height/DPR are unchanged (a same-value `canvas.width` set still blanks the bitmap). See DECISIONS "Fixed Background Canvases". Current versions: `fractal.js?v=20260721-scroll-stutter`, `surette-physics-wordmark-canvas.js?v=20260721-resize-guard`.
- Shared `assets/nav.css` anchors fixed headers to the viewport, normalizes the Portfolio and client-login header gutters, and blocks accidental horizontal overflow on phone widths. The public-nav sweep passed 30 routes at 1440px, 834px, 768px, 767px, and 390px with no detected header, navigation, or overflow failures.
- Public website forms are dual-routed. Contact, Apps consultation, and App Checkout submit to Supabase `client_messages` through `assets/site-message-forms.js` for the Admin Center Message Center, then post the same form to Netlify Forms so Netlify can send owner notification emails. Contact and Checkout support optional photo/screenshot attachments for the private Supabase `portal-message-attachments` storage bucket. **supabase-js is lazy-loaded (2026-07-21):** the module no longer statically imports the bundle (that fetch+parse at DCL made form pages load choppy during the reveal animations) — it dynamic-imports on first submit with a post-load idle warm-up. Do not reintroduce a top-level supabase import here.
- Homepage desktop/mobile hero now includes a newsletter subscriber email field wired to Supabase `homepage_email_signups` through `assets/newsletter-signup.js`. The email input uses an account-page-inspired transparent field with a thin cyan outline over the Nova background.
- Global page reveal still uses `html` opacity controlled by `surette-logo.js`, with a sitewide no-white-flash baseline: critical inline head CSS on HTML pages, black `html`/`body` and true app-root backgrounds in `assets/nav.css`, dark route/app loader surfaces, and `theme-color` set to `#050505`. **As of 2026-07-21, every page that loads `surette-logo.js` also stamps `sds-logo-loaded` via an inline `<head>` script before first paint** — without it, nav.css's 0.2s no-logo-script fallback fires on heavy pages before the bottom-of-body script stamps the class, producing a visible flash → hide → reveal double blink (that's what apps + the pricing page were doing). **Any new page adding surette-logo.js must include the head stamp** (copy it from any page — it sits just above `#dm-critical-dark-baseline`). Apps.html additionally pre-hides all content except its loader from first paint until `is-apps-ready` (head CSS + a 5s CSS-only safety un-hide), closing the loader's 50ms slow-path race. The reveal also waits for `document.fonts.ready` capped at 500ms (2026-07-21) so `display=swap` fonts can't re-layout text mid-fade; the 850ms absolute safety is unchanged. Visual page shells such as the account portal remain transparent so animated/cosmic backgrounds can show through. Same-site link clicks briefly show the small black spinner overlay before navigation to avoid a frozen-page feel. Repeated cards/panels/tiles use a shared object-level reveal so their text and background treatment appear together after page-ready.
- Client portal pages are static HTML wired for Supabase auth. Auth email redirects use `assets/supabase-config.js` `siteUrl` (`https://surettesystems.com`) as the canonical base, so password reset emails generated during local preview should no longer point at localhost. Account-dashboard support/change requests submit to Supabase `client_messages` through `submit_portal_message()` for the Admin Center and also post to Netlify Forms for owner email notifications.
- Mobile page headers, including service pages, inject an icon-only account link via `assets/account-nav.js`. Signed-in state may change the accessible label/title to "Account", but the visible mobile header control should remain just the `account_circle` icon. The mobile icon is grey while signed out and cyan/blue while signed in, and sits before the hamburger so the menu stays right-aligned.
- Account-page nav now uses `assets/standard-site-nav.js` with `data-active="account"`, matching the homepage/shared header and mobile nav structure.

## Portal Pages

- Admin Center Subscribers and Account holders tables use explicit column sizing so name/email fields get more room, with the Subscribers source pill allowed to wrap in a narrower column.
- Admin Center Subscribers and Account holders are independent lists. New portal account signups mirror into `homepage_email_signups`, but deleting a subscriber row removes it from Subscribers even if the portal account remains.
- Client profile data is intentionally minimal: account settings and Admin Center no longer show or save business/company name or website URL profile fields, and the Supabase setup SQL drops those columns if they exist.

- `account.html` — Client dashboard. Supabase email/password auth. Super-admin dashboard now looks like a normal client account except for an owner-only "Admin Center" card linking to `account-admin.html`.
- `account-admin.html` — Owner-only Admin Center. Gated by Supabase super-admin role; organized as an ultra-wide left-anchored tab workspace with Overview, Messages, Subscribers, and Account holders sections. The Messages tab is the unified message center for public website forms and account-dashboard support/change requests. Messages, Subscribers, and Account holders render their tables directly inside the selected tab. The Subscribers table includes a copy/export email modal with line-list, comma-list, and CSV formats. Message, Subscriber, and Account-holder tables have Delete actions behind a confirmation modal; message attachments render as signed owner-only links.
- `account-users.html` — Owner-only newsletter subscribers table. Gated by Supabase super-admin role; shows newsletter subscriber emails only. Portal account signups count as newsletter subscribers too, through `homepage_email_signups` once the updated Supabase setup SQL is run.
- `account-settings.html` — Profile/password management. Gated by Supabase session; shows sign-in prompt when logged out.
- `account-created.html` — Email confirmation landing page. Supabase redirects here after new registration; instructs user to verify email before signing in.
- The internal Sean's Google Ads portal feature (`seans-google-ads-dashboard.html`, `account-ads-status.html`, the `sean_ads_admin` role) was removed 2026-07-15. The public SeansAds.com portfolio case study is unrelated and still lives at `portfolio-seansads.html`.
- Privileged portal access reads Supabase `app_metadata.role` first (`super_admin`, `sean_ads_admin`), with email allowlist fallback in `assets/supabase-config.js`.
- Auth signup trigger (`handle_new_portal_user()`) mirrors new user metadata into `client_profiles`.

## Surette Data Systems Apps

App/software brand is **Surette Data Systems**. **Six** app profiles exist (the docs said five until 2026-07-16 — `metalspredictor-gold-silver-analyzer.html` had been added without updating them):

1. **Auction House & Consignment Store Software** — `auction-house-consignment-store-software.html`. Screenshots under `assets/apps/auction/` (WebP).
2. **Secondhand Dealer Management System (SDMS)** — `secondhand-dealer-management-system.html`. Screenshots under `assets/apps/sdms/` (WebP).
3. **Benji Payroll Management System** — `benji-payroll-management-system.html`. Screenshots under `assets/apps/benji/` (WebP + SVG variants).
4. **Antique Mall Vendor Management System and Auction Platform** — `antique-mall-vendor-management-system-and-auction-platform.html`. **`thirdstreetauctions.com` is offline (owner-confirmed 2026-08-08)** — it is no longer a live client site, and all links to it were removed from the site. Earlier docs and CHANGELOG entries describe it as live; those are history, not current state. Screenshots under `assets/apps/antique-mall/` — newer `thirdstreet-*.png` captures are current; older `antique-mall-*.png` captures may be stale/unused.
5. **SpotCalc — Buying Calculator** (renamed from MetalsCalc 2026-08-08) — `metalscalc-buying-calculator.html` (filename deliberately unchanged). **The only app still live:** site at `https://spotcalc.com`, app at `https://app.spotcalc.com`. Live Au/Ag/Pt spot pricing, customizable buy percentages, coin library, and one-tap PDF invoices for precious-metal dealers. PWA. Screenshots under `assets/apps/metalscalc/` (PNG — path keeps the old spelling).

6. **MetalsPredictor — Gold & Silver Trend Analyzer** — `metalspredictor-gold-silver-analyzer.html`. Live Au/Ag spot prices, candlestick charts, RSI/MACD/Bollinger Bands, and an analytical forecast score. Screenshots under `assets/apps/metalspredictor/`.

App catalog (`app-catalog.html`, 2026-07-16) renders 6 **collectible cards** + a dashed "Let's build yours today" open slot, matching the Websites gallery's treatment — single responsive layout, **1-up on phone**, 2-up at md, 3-up at xl. Card CSS is shared: `assets/collectible-card.css` (+ `pokecard-dropin`). **Apps use their own palette** (`.tcg-card--app-*`: coral, lime, green, teal, periwinkle, magenta), deliberately distinct from the Websites hues so the two galleries don't read as one set. The art-corner `.tcg-stat-pill` carries **availability** here (2026-08-08 — five cards read `By Request`, SpotCalc reads `PWA`) where the Websites cards carry a build estimate; it previously carried delivery type (LIVE DEMO / PWA / WEB APP), which stopped being the useful thing to show once nothing but SpotCalc was self-serve. The foot has one button (Open Profile) because an app has one destination.

App profile pages share compact typography overrides (`assets/surette-data-systems-app-profile.css` / `assets/surette-data-systems-app-profile-compact.css`) and mobile CTA callout panels. No English/Spanish nav toggle on individual app profiles (removed when Spanish mirrors were absent).

Surette Data Systems brand assets:
- `assets/surette-data-systems-logo.webp`
- `assets/surette-data-systems-icon.webp`
- `assets/surette-data-systems-geometric-icon.webp` (transparent geometric, used site-wide as the icon)
- `assets/surette-data-systems-floating-icon.webp`
- `assets/surette-data-systems-floating-orange-blocks.webp` (apps hero drift mark)

`apps.html` hero uses an inline SVG/CSS interactive Surette Data Systems wordmark with a page-load intro (full word lights up, then each letter falls/materializes left-to-right, then hover activates). Hovered letters rumble, break apart, tumble off-page, vanish, and rematerialize. Hover-started animations complete even if the pointer leaves.

## Portfolio / Websites

Portfolio hub (`portfolio.html`) introduces the two work categories: Surette Data Systems apps/software and Websites/case studies. Header and mobile nav Portfolio items point here; Apps and Websites pages remain live as downstream pages. The page is a single responsive layout. **The two category cards are "collection folders" (2026-07-16)** — same visual universe as the galleries' trading cards (paper plate, ink lines, hue frame, hard offset shadow, VCR mono) but deliberately a different object: folder tab on top, landscape, a fanned card-stack peeking behind the screenshot, **no flip/tilt/JS** (plain links, page-scoped `.folio-set*` CSS, hover motion gated behind `prefers-reduced-motion: no-preference`). Hues quote the galleries: teal = apps set, gold = websites set. `vcr-osd-mono` is @font-faced from `pokecard-dropin/pokecard/fonts/` without loading the widget CSS. **`apps.html` carries a conditional "Back to Portfolio" link** (2026-07-16, `[data-portfolio-back]` + an inline referrer script at the end of the file): revealed **only** when `document.referrer` is `portfolio.html`, because apps.html is primarily a Services destination (nav `data-active="services"`) reached from the Services dropdown, the homepage, the footer, and app-catalog's back link — a permanent one would misdirect most visitors. Don't make it unconditional.

**The two folders' copy is deliberately asymmetric:** the Websites folder says "Open the set" (`casestudies.html` IS the gallery), but the Apps folder says "Open the hub" with a "Gallery inside" chip, because `apps.html` is the software hub and the six-card gallery (`app-catalog.html`) is one step inside — don't "fix" them to match. The hero stats row reads 6 apps / 6 websites / 1 builder. **Responsive shape (2026-07-16):** 2-up folders ≥1200px; **one full-width folder per row in the 880–1199.98px band** (a component reflow point, not the site's 880 line — two side-by-side plates stretch their screenshots into portrait slivers below ~1200); stacked plates <880. The art is a capped square stack (`aspect-ratio: 1/1`, `max-height: 20rem`, centred) so the text column can never stretch it portrait — keep that cap if the layout is retuned.

Websites gallery (`casestudies.html`) — current entries (in order):
1. Naples Estate Jewelry — `portfolio-naplesestatejewelry.html`. **LIVE** at `https://naplesestatejewelry.com/` (moved from `.co` 2026-08-08). Screenshots under `assets/portfolio/naples-estate-jewelry/` (PNG + WebP).
2. JP Surette — `portfolio-jpsurette.html`. **OFFLINE** (2026-08-08). Screenshots under `assets/portfolio/jpsurette/`.
3. Elite Yacht Detailing — `portfolio-eliteyachtdetailing.html`. **OFFLINE** (2026-08-08). Screenshots under `assets/portfolio/elite/`.
4. Sean's Ads — `portfolio-seansads.html`. **OFFLINE** (2026-08-08; the site is down, so the link was removed — this supersedes the link half of the "Sean's Ads Boundary" decision, though the source still must not be recreated here). Screenshots under `assets/portfolio/seansads/`. Copy reflects live production status.
5. AuctionBuddha.com — `portfolio-auctionbuddha.html`. **OFFLINE** (2026-08-08). Screenshots under `assets/portfolio/auction-buddha/` (PNG).
6. SpotCalc.com (renamed from MetalsCalc.com 2026-08-08) — `portfolio-metalscalc.html` (filename deliberately unchanged). Product landing and PWA install page. Screenshots under `assets/portfolio/metalscalc/` (PNG — path keeps the old spelling).
- Open "Your Project Here" tile.

**Websites gallery is collectible cards at every width (2026-07-16):** both the desktop grid (`.hidden md:block`, 2-up → 3-up at xl) and the phone grid (`.md:hidden`, **1-up**) render six themed cards built from the `pokecard-dropin` widget — one hue per project via `--tcg-face/--tcg-light/--tcg-deep/--tcg-tint` on `.poke-scene.tcg-card` (jewelry gold, yacht aqua, photo rose, ads blue, auction violet, metals steel). **The phone grid must stay 1-up:** the card is a fixed 2.5/3.5 ratio, so a 2-up phone column (162px → a 226px card) cannot hold the anatomy, which needs ~312px even with the art at its 64px floor. **The two grids are hand-duplicated copies (~330 lines) — edit BOTH or they drift.** Static HTML was kept over JS-rendering them because the card copy is this page's real SEO content, and the page has always duplicated its two layouts (hero, CTA, footer and template card are all already duplicated). **Neither gallery renders `.portfolio-tile` any more** — it survives on both pages only for the open ("Your Project Here" / "Let's build yours today") slots, and the tile media/img/badge/price rules were deleted from both. `assets/portfolio-mobile-fixes.css` still carries tile-era rules and may be partly dead (flagged in TASKS; it is shared with the portfolio detail pages, so audit before deleting). Each card front carries two buttons (live site, then Case Study), a `.tcg-stat-pill` build estimate on the art's bottom-right corner, and a `.tcg-flip-top` "Flip ⟳" cue in the top bar. Both overlays are `pointer-events: none` so they can't swallow the flip click; the flip cue is `aria-hidden` (the card's aria-label already says it flips) but **the stat pill is not** — it's real information.

**Phone gallery is a numbered zigzag (2026-07-17):** on `casestudies.html`'s `.md:hidden` grid only, each of the 6 cards sits in a `.tcg-mobile-row` (added in `assets/portfolio-mobile-fixes.css`) with a `.tcg-mobile-num` chip — odd cards (1/3/5) shrink and push toward the row's right edge with the chip on the card's left, even cards (2/4/6) push left with the chip on the right, reading as a staggered timeline. The card's own `.poke-scene.tcg-card` width is trimmed via `width: calc(100% - 2.7rem)` (reserving room for the chip) with the base `max-width: 340px` left in place, so the shrink only engages below ~423px viewport — wider phones/small tablets keep the historic full-width card. `app-catalog.html` and the desktop grid are untouched by this. **Do not shrink the reserved space (2.7rem) further, and do not add a smaller max-width cap here** — either one narrows the card (and its back face, since front/back share one bounding box) below what `collectible-card.css`'s back-fit budget assumes; see the next entry. **The front-face `.tcg-stat-pill` on this same grid is separately shrunk to 70%** (`.md\:hidden .tcg-stat-pill` in the same file — height/font-size/padding/border/letter-spacing all scaled, pull-up margin recalculated rather than scaled to keep its 8px art-corner gap). Dimensions only, never `transform: scale()` — same reasoning as the in-flow rule below. **The card title and front-face badges are also shrunk on this grid** (`.md\:hidden .tcg-card .poke-name` 0.9rem→0.7rem; `.md\:hidden .tcg-card .poke-badges .poke-badge` 0.58rem→0.46rem with tightened padding/border, since the badges' box had been sized for the larger font) — back-face badges are untouched, their size comes from the back-fit container query below and any change there needs the same full-range re-verify as back copy.

**Mobile card fronts are decluttered (2026-07-17, owner rule — "keep only what's needed"):** on this grid the `.poke-stage-line` ("STAGE · X / NO. 00N") and the foot's `.poke-tag` status are `display: none` (in `assets/portfolio-mobile-fixes.css`) — the zigzag chip already numbers the card, the badges already carry the type, and the status wrapped up to 3 lines while saying nothing the Live/Preview button doesn't. The two foot buttons are flex:1 full-width tap targets. **Hidden via CSS, not deleted from the markup**, so the phone/desktop grid copies stay byte-identical and both-grids edits keep working; desktop deliberately keeps both elements. The shared `@container tcg-card (max-width: 336px)` tier in `collectible-card.css` also tightens the FRONT on narrow cards (body 0.58rem, tighter badges/padding) so the flex-fill art — the card's hero — gets the reclaimed height instead of sitting at its 64px floor. If card content grows (longer body copy, more badges), the art is what silently shrinks: check art height at 320px, not just overflow.

**The Tech Business Site Template card gets the same mobile reformat, via its own rules (2026-07-17):** the template hero widget is NOT a `.tcg-card`, so neither the gallery declutter nor the `@container` tier reaches it — its back was silently overflowing 148px (scrolling!) at a 320px viewport until this pass. Mirrored rules live in `portfolio-mobile-fixes.css` under `.md\:hidden .lab-poke-section` (stage line + weakness tag hidden, full-width View Live, gallery-tier back values); viewport scoping is correct for THIS card because its width tracks the viewport (`min(360px, 86vw)`, no zigzag). Both desktop copies keep the full design. **If a new non-tcg `.poke-scene` is ever added to a page, it needs its own no-scroll audit** — the tcg container tier will not cover it. (Same pass fixed `â†’` mojibake in all three template-widget copies' back text — now `&rarr;`.)

**Card back faces must never scroll (2026-07-16, owner rule; tightening moved to a container query 2026-07-17):** the widget's `.poke-back-inner` is `overflow: auto`, so the guarantee is that back copy **fits**, deliberately not `overflow: hidden` (that would clip the Case Study button silently instead of showing a scrollbar). The tightening in `assets/collectible-card.css` is a `@container tcg-card (max-width: 336px)` rule keyed to `.poke-scene.tcg-card`'s own rendered width — **not a viewport media query** — because the zigzag above (previous entry) shrinks card width independently of viewport, down to 236.8px at a 320px viewport. It's one tier, not two: an earlier attempt split narrow/narrower tiers and left a gap where the milder tier wasn't tight enough just above the aggressive tier's cutoff, so it's a single tier sized for the tightest case (proven at 0px to spare there; more width only ever adds slack). **After editing any back copy, OR changing how much width the zigzag reserves for its chip, re-verify every back across BOTH galleries at a 320px viewport: `scrollHeight === clientHeight`.** AuctionBuddha's back is the tightest fit — it has the longest copy and a fourth badge.

**Back-face type bumped up within that same budget (2026-07-17, owner request, two passes — the first was too timid).** The `@container` tier's values are not fixed at "as small as fits," they're fixed at "as large as still fits." First pass raised `p` 0.54rem→0.6rem — barely perceptible, and the owner correctly called it out ("not even halfway filled... I don't even see any change") with a screenshot showing real empty space below the badges on Naples. **The `scrollHeight === clientHeight` check that gates every back-copy edit only proves "doesn't scroll" — it does NOT prove "fills the space,"** because `.poke-links` carries `margin-top: auto` (pokecard.css), which stretches the flex column to `clientHeight` regardless of how little text is in it; scrollHeight can never read below clientHeight once that auto-margin is present, even with 60px of visible empty space. **True slack is `clientHeight − (sum of children's own heights + gaps + padding)`, measured with the auto-margin's stretch subtracted back out** — use this, not the overflow check, to judge whether there's room to grow. Second pass measured true slack per card at a 320px viewport (Naples 45px, AuctionBuddha only 20px — confirms AuctionBuddha, not Naples, is the real ceiling) and pushed `p` to 0.68rem (line-height 1.18) and `h3` to 0.78rem, paid for by tightening `.poke-back-inner` gap (0.32rem→0.14rem), padding (0.5rem 0.55rem→0.35rem 0.4rem), badges (0.46rem→0.4rem), flip-hint (0.46rem→0.4rem), and button text/padding (0.58rem→0.54rem, 0.35rem 0.5rem→0.26rem 0.42rem) — leaving AuctionBuddha ~12px of true slack at 320px, not zero, since razor-thin margins are how the first regression happened. Re-verified 0px overflow (the scroll guarantee) across both galleries at the full viewport sweep after the change; button text at 0.54rem was judged the new floor — do not shrink it further to buy more paragraph size.

**Back-face titles enlarged (2026-07-17, later the same day):** `.tcg-card .poke-back-inner h3` is **1.15rem** at base (matches the front name's native size; the widget default was 0.95rem) and **0.95rem** inside the narrow-card tier (was 0.78rem). The base rule carries `overflow-wrap: anywhere` and it must stay: the back h3 markup has no `<wbr>`, domains are unbreakable tokens, and without the guard a long name at this size overflows the `overflow: auto` back panel **horizontally** — a scrollbar, which the never-scroll rule forbids in either axis. When verifying backs, check `scrollWidth − clientWidth` as well as the vertical pair; the horizontal side was only being asserted implicitly before this change.

**Any overlay on a card face must be IN-FLOW — never positioned, z-indexed, transformed, will-changed, or opacity-transitioned** (2026-07-16, final form after three device round-trips; supersedes the interim state-hide pattern). Mobile compositors promote such elements to their own layer, which escapes the face's 3D flattening: the promoted pill first painted **mirrored through the back** of flipped cards (`backface-visibility` is ignored on a promoted transform-less descendant), and once a state-driven hide fixed that bleed, the same promoted layer **flickered mid-rotation** on flip-back. In-flow content is rasterised into the face itself — like `.poke-badge`, which never glitched once across all three device tests — so it rides the card through the whole flip and is culled with the face at 90°, no state juggling or timing gates needed. `.tcg-stat-pill` is therefore fully static, placed over the art by layout: a zero-height flex row (`.tcg-stat-row`) that follows the art, with the pill pulled up by negative margin to the same 8px/8px corner insets. Two supporting facts: `.tcg-card .poke-art` is forced `position: static` (the drop-in's `relative` — needed only for the template card's `.art-ph` placeholder — made the image, a positioned box, paint above ALL in-flow content and hid the pill entirely; caught by hit-testing), and there are **no** `data-flip` rules or transitions on the pill anymore. The widget's `.poke-glare` is the one legitimate positioned overlay: it's a direct child of the preserve-3d card with its own transform, a first-class 3D participant. Testing notes: assert on the card copy with a non-zero rect (`display:none` grids fake failures — transitions don't run there and `transform` computes as `none`), and `app-catalog` scrolls on **body**, not `documentElement`. Rules: theme via `assets/collectible-card.css`, never by editing `pokecard-dropin/` (the hero's template card must stay green, and the drop-in stays reusable); `pokecard.js` auto-wires any `.poke-scene`, so new cards need no JS; and `.tcg-card .poke-name` is pinned to 0.9rem with a `<wbr/>` before each TLD because domains are unbreakable tokens that otherwise push the top-bar cue out of the plate.

**Contrast lesson worth keeping (2026-07-16):** anything sitting on a case-study screenshot needs its own opaque backing, not an accent tint. The gallery's old badge/price chips were accent-tinted and measured **1.8:1** against the near-white Naples storefront; the same reasoning later forced the nav bars' tint (see below). Several portfolio screenshots are near-white exactly where a corner chip lands — check contrast against the image, not against the page background.

**Build-time estimates in hours (2026-07-16, replaced the dollar labels the same day):** every Websites gallery card shows a `.tcg-stat-pill` hours estimate ("EST 120-160 HRS" — ASCII hyphen in pills on purpose, the VCR font's en-dash coverage is untrusted), card backs and detail pages carry the range with the rate ("~120–160 hours at our $125/hr shop rate"), and a rate line sits above each gallery grid (both layout copies). The published shop rate is **$125/hr — `website_pricing_plan.txt` §37 addendum**; hours were derived from the earlier package-based dollar ranges at that rate so hours × rate reconciles with the pricing page. Detail pages are hours-led but **each keeps its named package's dollar anchor** (e.g. "Online Store builds start at $4,500"), and every disclosure includes "hours × rate is not a quote". Current values: Naples ~120–160 (owner-raised 2026-07-16 above the derived 95–130; implies ~$15K–$20K), Elite ~70–80, JPSurette ~35–50, SeansAds ~60–80, **AuctionBuddha ~280–440+**, MetalsCalc ~15–20 hours.

**AuctionBuddha is not a marketplace website — do not describe it as one (2026-07-16).** It is a localized eBay for a single vendor mall: a complete drop-in commerce ecosystem where each vendor runs their own mini storefront inside the mall, can launch an immediate timed auction or schedule lots into the store's end-of-month house auction, backed by a full admin center with messaging, label printing, and integration/control of in-store operations (online catalog and shop floor share records). The public site shows almost none of this, which is why it was first underpriced at $15K–$25K, then repriced to $35K–$55K+, now expressed as **~280–440+ hours** at the published $125/hr rate (still Custom Software & AI Systems, from $10,000 — a multiple of that entry point, not a different tier). `portfolio-auctionbuddha.html` carries an "Under the hood" section explaining the ecosystem. If the rate or plan prices change in the spec, re-derive the hours everywhere: the 12 card pills + 12 card backs + 2 rate lines in `casestudies.html`, the six detail-page sections, and `portfolio.html`'s folder chip.

Tablet layout (768px–1279px): shared fixes in `assets/nav.css` reduce left-rail padding, stack contact/portfolio two-column layouts, and treat `#sds-logo` as a flex nav brand. `casestudies.html` hero stacks below 1024px; `portfolio-live-widget.css` constrains the live-site CTA on tablet. Playwright audit (`scripts/tablet-audit.js`) reports zero overflow/clipping issues across 16 key pages at 834×1194, 1024×768, and 768×1024.

## Known Cautions

- Supabase portal auth gates are browser-only; real data security requires RLS + server-side functions.
- `supabase/portal-role-setup.sql` must be run in the dedicated Dark Matter / Surette Data Systems Supabase project (not Naples Estate Jewelry) before the admin account-holder table and signup trigger work.
- The updated `supabase/portal-role-setup.sql` must be run in full before live homepage newsletter submissions, account-signup mirroring into `homepage_email_signups`, public form submissions into `client_messages`, optional message photo attachments, account request submissions into `client_messages`, the Admin Center Message Center, and Admin Center delete actions work. The script now drops/recreates `list_portal_messages()` before changing its return table shape.
- Frontend Supabase config points at project `axlszyssxyvehjatztwe` and queries `client_profiles`, `client_services`, `client_invoices`, `client_documents`, `client_messages`, and `homepage_email_signups`; the starter schema still includes some legacy `client_billing` / website status tables and should be reconciled before a fresh database bootstrap.
- Static validator reports pre-existing noise: missing `es/` mirrors and the `node_modules` HTML scan (node_modules remains on disk for the playwright tooling but is gitignored/untracked as of 2026-07-21 — it no longer deploys). Site-content mojibake was swept and repaired 2026-07-15; the only remaining validator mojibake hit is a `node_modules` vendor file. `_enum_out.txt` and its generator were deleted 2026-07-21.
- **Deploy hygiene (2026-07-21):** repo root is trimmed for as-is Netlify deployment — ~11MB of orphaned/stale assets and one-off scripts deleted (see CHANGELOG), `node_modules` untracked (restore via `npm install`; root `package.json` added), `.claude/settings.local.json` untracked. `pokecard-dropin/app-template/` is NOT dead weight — it's the "Tech Business Site Template" live demo linked from casestudies. `.env.local` (gitignored, never commit) holds the Supabase service-role key.
- Owner email notifications now rely on Netlify Forms notifications. Configure the form notification recipients in the Netlify site settings; no Resend API key or custom notification function is used.
- Old `antique-mall-*.png` screenshots may still be on disk alongside the current `thirdstreet-*.png` set; audit and remove if unused.
- ~~MetalsCalc app/portfolio OG meta URLs reference `darkmatterwebsites.com`~~ — **resolved/stale as of 2026-08-08:** a repo-wide grep finds zero `darkmatterwebsites.com` references in any HTML/JS/CSS. Both pages already use `surettesystems.com`. The doc note outlived the problem.
- SpotCalc's `https://spotcalc.com` and `https://app.spotcalc.com` links were set from the owner's rename instruction and have **not** been reachability-tested from this environment — confirm both resolve after deploy.
- ~~Admin-only ThirdStreetAuctions.com screenshots not yet captured~~ — **moot as of 2026-08-08:** the site is offline, so there is nothing left to capture. The existing `thirdstreet-*.png` public captures are now the permanent record for this app profile.
