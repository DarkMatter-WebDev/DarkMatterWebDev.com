# Changelog

This file is intentionally compact. Keep only high-signal recent changes and major milestones.

## 2026-08-27 (latest) — Self-hosting copy live, retired-offer claims purged

- **Self-Hosting Setup surfaced on the public pages.** `pricing.html` gained an "Own your own hosting" route ($450/$750, account-holder-from-day-one, 14 days then $125/hr) and a matching FAQ. **The ownership FAQ was rewritten** — it claimed "This is a fully managed website service" full stop, which stopped being true the moment a one-time customer could hold their own accounts; it now explains both routes and why the account ownership differs. `services/website-design-hosting.html` gained a full add-on panel in `#one-time-builds`; `websites.html` picked up the one-line version.
- **Stale claims for retired work removed** — these mattered more than the deleted pages, because they were still selling:
  - `pricing.html`: the "Software, not a website" route, plus the enterprise bullets *Custom e-commerce & checkout* and *CRM, ERP & API integrations*, and a lead paragraph advertising "e-commerce builds, custom integrations".
  - `index.html`: meta description sold "custom software" — **3 occurrences** (both duplicated trees plus OG tags).
  - `websites.html`: the "Custom business software — CRM, ERP, inventory, agentic AI" card linking to `apps.html`.
  - **`contact.html`: the project-type dropdown still offered "An online store" and "A custom app or tool"** — the form was actively collecting leads for work no longer sold. This is the one worth remembering: retiring an offer means auditing forms, not just pages.
- **Verified:** validator steady at 80 with zero issues in the site's own files; 16 records / 0 contrast failures across the edited pages in both themes; no mojibake.
- **Full post-removal sweep: 48 pages x 2 themes x 4 widths = 384 records per auditor, 0 failures, 0 errors** (36,524 elements computed-style; 35,392 line boxes rendered-pixel; `revealOk` false on 0). This also re-validated **1024**, which flipped back to the desktop nav when the breakpoint moved 1120 → 1024.

## 2026-08-27 (later) — App/software product line retired

- **Packaged business apps are no longer sold.** Owner decision. The offer is gone; the work survives as proof.
  - **Deleted (4):** `app-catalog.html`, `app-pricing.html`, `app-checkout.html`, `multichannel-commerce-website.html`. All four get **301 redirects** in `netlify.toml` (catalog → `apps.html`, pricing/checkout → `contact.html`, store → `websites.html`) because the URLs were public.
  - **Kept as case studies:** the six app profiles (auction house, secondhand dealer, Benji payroll, antique mall, MetalsCalc, MetalsPredictor). `apps.html` was **repurposed from a sales page into a case-study index** rather than deleted — that preserved **17 inbound links** (13 industry pages, index, portfolio, websites) instead of breaking them.
  - **Purchase paths removed, not repointed.** Checkout CTAs deleted outright; `app-pricing.html` links became `contact.html`. One CTA was **percent-encoded** (`account.html?next=%2Fapp-checkout...`) and survived the first pass — worth remembering that a plain href sweep misses those.
  - **Nav: the Software pill is gone** from both the desktop row and the hamburger. `data-active="software"` became `none` on 7 pages.
  - **Breakpoint moved 1120 → 1024.** Four pills instead of five. Measured on an interior page: **+76px at 1120, +41px at 1024, +9px at 960, 0px at 920 and below** — so 880 is **not** safe to return to even at four pills; the row abuts the CTA there. 1024 keeps real clearance and gives tablets the pill row back.
  - **E-commerce dropped too:** the multichannel store card removed from `websites.html`, and the *Online Store $4,500* and *Custom Software and AI $10,000* packages removed from `website_pricing_plan.txt`.
  - **Verified:** validator **84 → 80** (exactly the 4 deleted pages' missing-`es` mirrors; broken-link unchanged at 16, all vendored; **zero issues in the site's own files**). All 48 nav-bearing pages correct at 1024/1023 with positive clearance, no overflow, no Software pill. Encoding clean across 60 files.

- **New offering: Self-Hosting Setup and Handoff** (`website_pricing_plan.txt` §29b).
  - **$450** single/multi-page, **$750** multi-page with stored form records. Priced against the existing ladder (migration assistance $250+, local SEO $750–1,500) and the market's $200–1,500 band for a full migration incl. domain, hosting, DNS and testing. **Flat per size, not per build**, because the setup work does not scale with page count.
  - **Account ownership is now explicit and splits by option** (§ "Who owns the accounts"): on a managed plan SDS owns the hosting account; on a one-time build SDS **creates every account in the customer's own name** — hosting, domain, email, analytics — and hands over credentials, being added as admin only where needed. The customer is never locked in.
  - **Support boundary:** 14 days of setup-related fixes, then the published **$125/hr**. §29 was amended — it previously excluded "Configuring a new server" outright, which would have contradicted the new service.

- **New: §29c "Where a Website Ends and Custom Software Begins."** Written because the owner asked whether AI in a website makes it an app. The test: *if it switched off tomorrow, would they lose a feature or lose their operations?* Visitor-facing AI (answering, qualifying, routing, drafting, summarising, booking) is a website feature — data passes through and lands in email/CRM. Logins, roles, inventory/payroll/scheduling, or AI reading and writing the business's own records are custom software and are not offered. **This exists to stop tier 3 ("multi-page with backend tables") drifting back into the app business at website prices with no maintenance contract.**

## 2026-08-27

- **`apps.html`: the physics wordmark is switched off below 880px and replaced with a static one** (owner decision). This retires the last open contrast item on the site.
  - **Why it could not be fixed any other way.** The letters came to rest wherever the simulation put them, which at these widths was regularly on top of the hero copy. Measured and rejected, in order: dimming to `opacity: 0.45` (cut the common cases but still tripped **~1-2 loads in 10**); an opaque chip on the kicker lifted to `z-index: 60` (**still measured a covered ground of `#989896` — the canvas paints ABOVE the section whatever the stacking**); dimming enough to guarantee it (**needs ~0.22, which erases the wordmark anyway**); moving the next section down (**relocates the collision onto the lead paragraph**); clipping the arena (**erases the wordmark from the mobile hero**).
  - **The `<h1>` was nothing but the canvas**, so hiding it alone would have left the hero with no visible brand. A static wordmark now takes its place below 880px — SURETTE in Archivo Expanded over DATA SYSTEMS in JetBrains Mono, the same two lines the canvas paints, using the light-mode values already measured for it (`#0b0e10` 17.32:1, `#3f6212` 6.33:1; **do not lighten that subline**).
  - The dim and the kicker chip were removed with it — both existed only to survive falling letters that no longer fall here. **Desktop (>=880px) keeps the physics untouched.**
  - **Verified:** canvas `display:none` / static `flex` below 880 and the reverse at 1024; `apps.html` clean at 1280/1024/768/390 in both themes; and **0/10 repeat runs** at `light/768`, the width that previously failed intermittently. Because the canvas no longer renders there, this is deterministic rather than a lucky sample — the box count is stable at 68/68 where it used to fluctuate.

## 2026-08-20

- **The desktop nav had been overflowing since Industries was added — fixed by moving the hamburger switch 880px → 1120px.** Reported from a 923px preview: buttons were disappearing from the header.
  - **Cause.** `standard-site-nav.js` carried a comment saying the 5th pill "was measured at 880px and still fits". It does not. That holds only for the homepage, which drops the Home pill and carries 6 items — and even that collides at 960px. **Every other page carries 7** (Home · Websites · Software · Industries · Pricing + login + theme toggle, beside the Get started CTA) and needs **~1105px**.
  - **Why it looked like "lost" buttons rather than overflow.** The pill row is a flex child with `min-width: 0` and `overflow: visible`, so it did not wrap or scroll — it silently **spilled**, and the theme toggle and login icon slid underneath the Get started CTA. Measured clearance on an interior page: **-175px at 881, -142px at 923, -17px at 1080, +14px at 1120, +33px at 1160.**
  - This is precisely the "partway header" that `nav.css`'s switch rule was written to prevent ("the header is either whole or a menu and never partway"). 880 was right for **four** pills; nobody re-measured when the fifth landed.
  - **The breakpoint lives in 7 places** and all moved together: six `@media` blocks in `nav.css` and `isMobileNavViewport()` in the generator. **`.page`'s padding rule was split** — its vertical padding is nav clearance and follows the nav to 1120, its 20px horizontal gutters are page layout and stay at 880. Page layout (grids, `.sds-proof`, footer variants) deliberately left at 880.
  - Tightening spacing was tried and rejected: gap 8px + 10px pill padding only bought ~40px, moving the collision 1080 → 1040. The row cannot fit 7 items in the 880-1100 band.
  - **Verified:** switch is exact at 1120/1119; all 46 pages that use the shared nav render correctly on both sides with zero horizontal overflow and positive clearance; all 8 destinations plus the theme toggle reachable in the hamburger at 1000/900/390. (The 5 portal utility pages and the standalone concept page have no shared nav by design.) Contrast re-audited at 1024, the width whose nav changed: computed-style **104 records, 0 failures**.

## 2026-08-19

- **Light-mode contrast: five real defects found and fixed, and the auditor rebuilt to be able to see them.** The 2026-08-18 sweep reported "0 failures" across the whole site. It was measuring with an ancestor-walk auditor, and **`getComputedStyle` can only see ancestors** — so every defect below was invisible to it while being obvious on screen.
  - **The big one: a hard-coded dark veil on 15 pages.** `<div class="absolute inset-0 bg-[linear-gradient(to_bottom,rgba(5,5,5,0.48),#050505_84%)]">` — a Tailwind arbitrary value written in dark literals, with no light variant. It is `position: fixed`, so it is nobody's ancestor and the old auditor scored it as absent. In light mode it dropped a near-black wash over pages whose ink stays dark: `auction-house` measured **1.01:1** on its eyebrow and **1.05:1** on its section headings; `portfolio-seansads` lost its whole lead paragraph. Fixed with a **`.sds-cosmic-veil`** hook + a light rule in `assets/cosmic-web.css` restating the same fade in paper tones. **Dark mode is untouched** — the inline gradient still wins there; the light rule beats the utility on specificity alone (0,3,0 vs 0,1,0), so no `!important`. `portfolio-seansads` keeps its two accent blooms via `--tinted`.
  - **`privacy.html` had an opaque `#050505` layer** in an inline style (`<div class="fixed inset-0 z-0" style="background:#050505">`). In light the page ground stayed black while headings flipped to dark ink — **"1. Who We Are" and every other `h2` were literally invisible** at ~1.03:1. Now `rgb(var(--t-void-black))`, which already flips to paper. Its two `nebula-blur` blobs then held the ground at ~#c5d6d7 (links 4.29:1), so light mode drops them 0.3 → **0.12** (measured: 0.16 is break-even).
  - **`terms.html` had an inline `#0a0010` backdrop**, which dragged the paper to mid-grey and left the contents links at **2.67:1**. Moved to a `.sds-page-backdrop` class so it can be themed at all — an inline style cannot be overridden without `!important`. Dark keeps the exact original colour.
  - **Three light ink tokens were calibrated against paper, not against the site's own panels.** `--t-electric-cyan` (light) measured 5.77:1 on `#f4f2ee` but **4.07:1** on a `#cccecb` panel, which is where it actually gets used — pricing check rows, portfolio meta labels, legal contents links. Darkened `0 104 121 → 0 94 109`, `--t-ink-39ff14 #1f6f10 → #1a5e0e`, `--t-ink-18d96e #10723c → #0e612f`. Darkening only ever helps text contrast; the full sweep confirms nothing inverted.
  - **`account.html`'s phone login sat directly on the wormhole canvas.** The `[data-auth-panel]` rule deliberately dropped the panel fill below 880px so the animation showed through — but the bright plume runs straight across the copy, and "Sign in with your verified email." had a word-sized run of its line **fully** on failing ground. It now keeps the same glass fill it already uses above 880px; the wormhole still reads through it and around it.
  - **`.transfer-badge`** light ink `#185d0d → #14490a` (3.72 → 4.91 against the spine visual's worst gradient stop).
  - **The phone width had its own systemic cause: `cosmic-web.css`'s mobile override hard-coded `opacity: 0.52` on `body::before`.** That is dark mode's 0.66 scaled for the phone, but the rule is not theme-scoped, so it also overrode **light mode's 0.26** and put dark's heavier veil on the light page. The inverted cosmic photo then held the paper at ~`#bfbdc3` instead of `#f4f2ee`, and a dozen labels across the portfolio, contact, privacy, multichannel and app pages measured 4.0–4.5:1 at 390px while passing at 1280px. Now `opacity: calc(var(--cosmic-opacity) * 0.79)` — dark is bit-identical (0.66 × 0.79 = 0.52), light drops to 0.21.
  - **A second round of ink tokens, once the phone width was measurable at all.** Nested glass surfaces compound their tint (`--c-veil-rgb` is a *dark* wash in light mode, so a card inside a panel darkens paper twice, to ~`#c4c1c9`). Rather than restyle glass in 14 definition sites, the inks were taken down one more step: `--t-electric-cyan` `0 94 109 → 0 83 96`, `--t-on-surface-variant` `77 84 89 → 71 77 82`, `--t-outline` `93 99 102 → 86 91 94`, `--t-ink-ecdfc8` `#7a5d2a → #705627`, `--t-ink-8f98a6` `#5b6472 → #545c69`, `--t-ink-39ff88` `#107237 → #0f6933`. Dark's `--t-on-primary-container` went the other way, `121 119 119 → 128 126 126`, for footer meta over the purple bloom on `services/in-home-services` at 390px.
  - **`--t-nebula-purple-on` (dark) lightened `163 92 255 → 176 118 255`.** Same story as the cyan: calibrated against the page black (5.37:1), but used as ink on tinted panels — the 13 `web-design-for-*` pages' mistake-list marks, `services/in-home-services`' BEST FIT label, `built-by`'s card eyebrow — where it measured **4.32–4.49:1**. Now 5.43:1 there, 6.68:1 on black. It is only ever ink (text and `--sds-list-mark`), never a fill, so lightening cannot cost contrast anywhere.

- **Widths 1024 and 768 swept afterwards (the documented scope is four widths, not two) — and the settle time was wrong.** 10 pixel-pass failures appeared; **6 were measurement error**. The physics wordmark intro on `apps.html` runs ~3s, but the driver measured at 900ms and caught letters mid-flight across the hero copy. Settled, that copy is clear. **This is the ~800ms object-reveal trap again, just 3x longer** — both drivers now settle **2800ms**, not 900ms.
  - Four were real, same pattern as everything above: `--t-outline` (dark) `142 145 146 → 151 154 155` (index's proof line over the Nova galaxy and privacy's footer links over the cyan bloom both read ~4.4:1 at 1024px), `--t-ink-a7ffcb` (light) `#107138 → #0f6834` (multichannel's "Live now" pill, 4.39:1), `--t-ink-aeb4bf` (light) `#575f6e → #505765` (multichannel body copy, 4.40:1).
  - **`apps.html` hero z-order fixed.** Its `@media (max-width: 879.98px)` block says "Lead text and catalog stay above the falling letters" and sets `z-index: 5`/`6` — but `surette-physics-wordmark.css` puts the wordmark at **`z-index: 12`**, so the intent lost and the canvas letters painted over the lead paragraph during the fall. Now 14/15.
  - **`apps.html`'s mobile hero needed three changes, and two structural fixes were tried and rejected first.** The physics letters come to rest wherever the sim puts them, so *which* element they land on varies per load — the "Software Catalog" kicker failed on **3 of 5 loads** at 390px. Moving the section down (`margin-top: 0`) just moved the collision onto the lead paragraph; clipping the arena fixed contrast but **erased the wordmark from the mobile hero entirely**. What shipped: the kicker gets its own chip ground, and `#wm-canvas` is dimmed to **`opacity: 0.45` at <=879.98px only**, which keeps the effect as a watermark and leaves every heading readable. **Desktop is untouched; revert is deleting that one rule.**

    **CORRECTION (2026-08-27): this reduced the problem, it did not eliminate it.** The "15/15 runs clean" recorded here was a sample that missed the tail. A 416-record four-width sweep caught it once (`light/768`, "Software Catalog", chunk 0.918), and targeted repeats reproduce it at roughly **1-2 loads in 10**. **The letters paint ABOVE the section regardless of stacking** — an opaque chip lifted to `z-index: 60` still measured a ground of `#989896`, i.e. covered. So no background/z-index trick fixes it, and dimming enough to guarantee it (worst case, a letter fully covering the label) needs **opacity ~0.22**, which erases the wordmark. Structural fixes were also tried and rejected: clipping the arena erases the wordmark from the mobile hero, and moving the section relocates the collision onto the lead paragraph. **This is now a design decision about the physics hero at narrow widths, not a CSS defect** — left as-is deliberately.
  - **`.sds-btn-secondary` had no fill**, so on `apps.html` it sat directly on the fractal canvas and its cyan icon measured **3.2-4.2:1 on 3 of 3 runs** over the fractal's bright core. Given `background: rgb(var(--t-void-black) / 0.6)` — dark on dark, paper on light. The class is used on that one page only.
  - **Auditor caveat worth knowing: a `<canvas>` with `pointer-events: none` is invisible to the occlusion probe**, because `elementFromPoint` skips it. Paint order and hit order diverge there, so text overlaid by canvas art can still be measured as if unobstructed. Judge canvas heroes by eye as well.
  - **`multichannel-commerce-website-test-4.html` fixed too (owner asked for everything).** Its closing section's coral circle is 440px until the page's own `@media(max-width:760px)` drops it to 230px — which left a **761-800px gap** where the full-size circle slid behind the heading, putting white "One product entry." at 2.6:1 and lime "Your whole selling space." at 1.9:1, whole words on coral. Added an intermediate `@media(max-width:860px)` step (`width:300px;right:-110px;top:-90px`) so the circle steps 440 -> 300 -> 230 instead of jumping. Verified clean at 1280/1024/900/860/800/768/761/390 in both themes.

- **Verified after: 52 pages x {light,dark} x {1280,1024,768,390} = 416 page-audits, run through BOTH auditors.** Computed style: 40,620 elements, **0 failures**. Rendered pixels: 38,955 line boxes, **0 failures on every routable page** (the only 4 are on `multichannel-commerce-website-test-4.html`, the unlinked `noindex` concept page left as-is by owner decision). 0 errors either pass. Validator at its expected noise level, all CSS brace-balanced, zero mojibake across the site's 64 files. **Signed-in portal views remain unaudited** — the sweep only ever sees the signed-out shell.

- **`scripts/audit-contrast.js` rebuilt.** The old version **bailed out on any element with a `background-image`**, i.e. skipped most buttons and every gradient panel, and could not see sibling, fixed, or canvas paint at all.
  - It now runs **two** passes and both must be clean: the ancestor walk (composites gradients **with** stop alpha, worst stop, drops ~0-alpha stops, skips `background-clip: text`, bottoms out on the real body fill) and a **rendered-pixel pass** that hides every glyph, screenshots the viewport, and samples the exact line boxes via `Range.getClientRects()`.
  - Five traps in the pixel pass, each of which produced false failures before being handled — **occlusion** (the collectible cards keep their back face laid out behind the front: 58 phantom hits), **smooth scrolling** desyncing rect from screenshot, **keeping the worst sample across scroll steps** instead of the best-centred one, and **scoring by percentile** instead of by contiguity. That last one matters: `apps.html`'s hairline fractal fails ~30% of the pixels under its hero copy and reads perfectly, while `account.html`'s plume covered whole words. Scoring is now per word-sized chunk, failing at `maxChunk >= 0.75`, calibrated by eye against those two pages.
  - **The fifth trap: occlusion has to be judged per LINE, not per element.** A paragraph scrolled so its first lines sit behind the fixed header is still a visible element, and those lines then get sampled against the nav's opaque fill — `portfolio.html`'s cream card reported **1.13:1** that way while rendering perfectly. `__sdsRectsNow` now drops any individual line box that is not the topmost paint at its own coordinates.
  - Forcing the object reveal needs a **stylesheet with `!important`** — the page's own script re-sets `html.style.opacity` after load, so an inline assignment is silently undone and every element then reads as invisible. And `.reveal-up` needs the page **scrolled**: without it, 64 elements on the homepage alone were never measured.

## 2026-08-18

- **Full-site contrast sweep and fixes (51 pages x 2 themes x 2 widths).** 5 real defects found and fixed; everything now passes AA at 1280 and 390 in both themes.
  - **The big one: `portfolio-live-widget` was near-invisible in light mode (1.21:1)** on four portfolio pages. Root cause is a rule worth remembering: **the component is built from colour literals (#39ff88 / #8dffb4) that deliberately do not flip, but three theme tokens layered on top of it did.** In light, `--c-veil-rgb` turned the 0.9 radial highlight into a near-black blob over the eyebrow and title; `--t-electric-cyan` turned the gradient centre dark teal (2.96:1); and `--t-ink-39ff88` turned the orb icon dark-green on a dark orb (2.43:1). Fixed by scoping two component-local tokens (`--lw-light` / `--lw-dark`) plus literal gradient stops. **When a surface does not flip, nothing layered on it may flip either.** An earlier pass had already fixed this component's text colour for the same reason and missed everything else.
  - Four marginal fixes, all accent-on-tinted-panel where the light accent is calibrated against the standard surfaces but sits on something darker: `.eyebrow` 4.31 -> 4.63 (one override in the shared app-profile CSS, covering all six app pages), `.transfer-badge` 4.19 -> 4.61, `.mc-roadmap-card span` 4.39 -> 4.65, `.client-muted` 4.42 -> 4.70 (alpha 0.62 -> 0.64).
  - **The auditor itself had a bug worth recording.** It counted fully-transparent gradient stops as candidate grounds, so a decorative overlay fading to `rgba(0,0,0,0)` made it score text against the page body — inventing 14 phantom failures on the live widget in BOTH themes. Near-zero-alpha stops are now dropped. Recorded in the contrast-audit memory.
  - Verified after: 51 pages x {light,dark} x {1280,390} = 204 page-audits, **0 failures**. Validator 84, all CSS brace-balanced, zero mojibake.


- **Blog structure built (funnls parity, Gap C).** Two files: `blog.html` (index) and `blog-post-template.html` (copy-me, `noindex`, unlinked). Authoring guide: `project-docs/BLOG.md`.
  - **One filtered index instead of funnls' 8 category pages.** funnls runs `/blog` plus 8 `/blog/category/*` routes; on a static site with no build step that means 8 real files to keep in sync and 8 empty ones until posts exist. Categories filter one grid client-side, so the URL count stays at 1 and **without JS every post is simply visible** — the correct degradation. Four categories (`websites`/`software`/`local`/`process`), not eight, until there are posts to justify more.
  - **Not added to the nav, deliberately** — an empty blog in the header is worse than no blog. Measured for later: a **5th nav pill fits at 880px**, the narrowest desktop width, with no overflow.
  - Empty state is driven by the same filter pass, so it also covers a category that matches nothing.
  - **Validator trap found and worked around:** `scripts/validate-site.ps1` regexes for `href=` **without parsing HTML comments**, so the copy-paste card example in a comment registered as a real broken link (validator hit 71 instead of 70). The pattern moved to `project-docs/BLOG.md` and the count returned to exactly **70 = 68 + 2 new pages**. Recorded in BLOG.md — do not put example `href=` markup in a page comment.
  - **Verified:** index at 1280/880/390 in both themes; 5 chips, exactly one pressed at a time; filter exercised with injected cards (all→3, websites→2, software→1, process→0 **with the empty state correctly revealed**, back to all→3); template page renders in both themes (dark body `196 199 199`, light `77 84 89`); nav and footer inject on both; zero overflow; zero mojibake.

## 2026-08-17

- **`websites.html`'s "What you get" section rebuilt with inline-SVG visuals and balanced cards.** The row was visibly broken: only the middle card carried a checklist, so the third card ended in a large void while the grid held all three to equal height.
  - **Root cause was content shape, not CSS.** All three cards now share one structure — visual → title → one short paragraph → a **three-item** checklist pinned to the bottom by the new `.wb-card-list` (`margin-top: auto`). Measured tail gap is now **29/29/29px** across the row, i.e. uniform card padding with no dead space. Keep the three-item symmetry; an uneven list count is exactly what caused the original problem.
  - **A content bug fixed in passing:** the "dedicated WhatsApp account" line was filed under *hosting*. It is a change-request channel, so it moved to the "Changes handled for you" card.
  - **New visuals are inline SVG** (`.wb-viz`, 16/9): a browser wireframe being composed, a server rack with an uptime trace and shield, and a message bubble → arrow → approved page. Inline means **no extra request and nothing against this page's image budget** — the page whose proof cards were once 3.2 MB.
  - **All SVG colour comes from four custom properties on `.wb-viz`** (`--wb-line`, `--wb-dim`, `--wb-accent`, `--wb-accent2`) and nothing else — **zero hex literals**, verified. A hex inside an SVG would freeze the illustration to one theme with no error, the same failure mode as any hardcoded palette colour here. `--t-nebula-purple-on` is used rather than raw `#7000FF`, which is a fill colour, not a line colour, on the site's near-blacks.
  - Copy tightened: each card's prose dropped to one sentence, with the specifics moved into the checklists where they scan.
  - **Verified at 1280 / 880 / 390:** 3 cards, 3 list items each, equal heights at every width, uniform tail gap, zero overflow. SVG variables resolve correctly in **both themes** (dark: line `196 199 199/0.38`, accent `0 240 255`, accent2 `163 92 255`; light: `77 84 89/0.38`, `0 104 121`, `90 0 204`) — nothing falls back to black. Section contrast: **27 elements checked, 0 failures in each theme.** Validator at 68.

- **`websites.html`'s "Recent work" cards brought in line with the homepage.** Same swap: Elite Yacht Detailing → **SpotCalc.com**, AuctionBuddha → **Deepfield Gallery**, Naples unchanged. Both pages' featured slots now show only sites a visitor can open.
  - **Matched the SITES, not the wording.** This page's copy is deliberately longer than the homepage's — its Naples entry already differed — so SpotCalc and Deepfield got new copy in this page's fuller voice rather than a paste of the homepage lines.
  - Deepfield links out (`target="_blank" rel="noopener"`) since it has no detail page; SpotCalc keeps `portfolio-metalscalc.html`.
  - **Image budget:** 566 KB → **383 KB**, reusing the same lean assets as the homepage (`metalscalc/home.png` 149 KB, `deepfield-gallery/card.jpg` 157 KB). This page is the one the docs flag as having been cut from 3.2 MB, so it stays under watch.
  - Verified at 1280 / 880 / 390: 3 cards, all images decode, equal-height rows, correct hrefs and rel, zero overflow, zero mojibake. Validator at 68.
  - **`casestudies.html` deliberately still links both retired sites' detail pages** — that gallery is the archive and shows them as `status - offline` with `Ask to View`. Only the two *featured* surfaces were changed.

- **Homepage hero scrim gained a neutral-grey layer so white copy reads better over the Nova galaxy (dark theme).** `#hero-tint` now stacks two layers — the original black gradient on top, `rgb(var(--t-surface-container-highest) / 0.22)` (52 53 53 at 22%) beneath it.
  - **Why grey and not more black:** the black gradient scales everything proportionally, so the galaxy's bright particles stayed bright *relative* to the white headline. A flat grey compresses the range from the top, pulling bright pixels down while barely lifting the darks. Modelled across the brightness range, white-on-background contrast improves where it was worst and is essentially unchanged where it was already fine: galaxy 240 → **5.04 to 6.67**, 200 → **6.61 to 8.31**, 120 → **11.59 to 12.82**; dark areas move only 20.11 → 19.63 and 18.30 → 18.11.
  - The grey lands hardest exactly where the hero copy sits, because the black gradient is most transparent through its 35–65% mid band and near-opaque at top and bottom.
  - **Edited the inline style, not a stylesheet rule.** `#hero-tint`'s background is authored inline, so a CSS rule would lose to it without `!important` — the same trap `#nova-bg`'s inline `background:#160016` already documents.
  - **Dark-only by construction, no theme guard needed:** the whole element is `display: none` in light mode and Nova is never built there. Verified in light — tint and nova both `display: none`, no canvas, h1 still ink-on-paper.
  - Token-driven, zero hex literals. One `#hero-tint` element serves both duplicated trees because it is `position: fixed; inset: 0` at `z-index: 2` (above Nova's 1, below content at 10) — verified active and covering the full client area at 1280 and 390. Validator at 68.

- **Homepage "Recent work" cards now show only LIVE sites.** Elite Yacht Detailing → **SpotCalc.com**, AuctionBuddha → **Deepfield Gallery**. Naples Estate Jewelry stays. The two replaced entries have been offline since 2026-08-08, so the homepage's proof section was showcasing two dead sites — a visitor clicking through hit an `Ask to View` dead end instead of working proof. All three cards are now reachable and live.
  - Deepfield has no detail page, so its card links **straight to `https://deepfieldgallery.com/` with `target="_blank" rel="noopener"`** — the only external card in the section. SpotCalc keeps its internal `portfolio-metalscalc.html`.
  - **Landed in both duplicated trees** (identical markup, so one `replace_all` each). Verified: each new title/image appears exactly **2x**, and `Elite Yacht Detailing` / `AuctionBuddha` / their two image paths are at **0** occurrences.
  - **Image budget respected.** This section was deliberately cut from 3.2 MB to ~566 KB once before. The full Deepfield capture is 787 KB on its own, so `scripts/capture-deepfield.js --card` was added to shoot a **1200x750 JPEG at q82 → 157 KB**, matching the card's own `width`/`height` attributes. New three-card total is **383 KB, lighter than the 566 KB it replaced**.
  - Verified at 1280 and 390: 3 cards per tree, all images decode, equal-height rows, zero overflow. Validator at 68.
  - **Note the gallery card and this card differ by design:** `casestudies.html`'s Deepfield card is the collectible-card component; this is the homepage `glass-card` proof tile. Editing one does not touch the other.

- **Deepfield Gallery added to the Websites gallery as No. 007 — the seventh card.** `deepfieldgallery.com` is **live**, so the card carries `status - live` and links straight out. A curated Naples gallery selling art, jewelry, antiques and decorative objects: five collection categories, artist profiles, accounts, saved items, EN/ES, local pickup beside insured shipping.
  - **Card-only entry (owner call).** No `portfolio-deepfieldgallery.html` was created, so the foot carries a single `Live Site` button rather than the `Live` + `Case Study` pair the other six use. This is the one structural difference between it and the rest of the set.
  - **Landed in BOTH grids** — desktop (`.hidden md:block`) and phone (`.md:hidden`, with `tcg-mobile-row--odd` since 7 is odd, card right / chip left). 14 `.poke-scene` blocks total = 7 + 7. Both carry a comment pointing at the other.
  - New palette `.tcg-card--gallery` (acid chartreuse `#d9f97a`) in `assets/collectible-card.css`. Chosen because the six existing website hues already cover gold / aqua / rose / periwinkle / violet / steel — a pink or violet face would have collided with `--photo` and `--auction`. → `collectible-card.css?v=20260817-deepfield` (2 pages).
  - **Estimate: `EST 110-150 HRS`** at the $125/hr rate — derived by comparison to Naples Estate Jewelry (120–160), which has the same commerce depth plus the spot-pricing engine, while Deepfield adds artist profiles. Owner-approved. ASCII hyphen in the pill per the VCR-font rule.
  - Screenshots captured with a new `scripts/capture-deepfield.js` (mirrors `capture-metalscalc.js`). **The site's cookie notice is HIDDEN via `addStyleTag`, never clicked** — dismissing it would be consenting on the owner's behalf, and the capture only needs it out of frame. Card art `home.png` is 787 KB, in line with the existing 631 KB Naples capture.
  - `portfolio.html` updated: Websites stat 6 → 7 and the folder chip `6-card set` → `7-card set`. The Apps stat stays 6 and the `Est. 15-440 hrs` range still holds (110–150 sits inside it).
  - **Verified: 63 card backs across 9 phone widths (320-879) — zero vertical scroll, zero horizontal scroll, zero foot-row wrapping.** Plus 320/390/880/1280: 7 visible cards each, palette resolving, art loading, correct live href and pill, no page overflow, no mojibake. Validator unchanged at 68.
  - **Measurement note worth keeping:** a "true slack" formula summing back-face children reports *negative* on every shipped card (AuctionBuddha -106, Naples -49) because `.poke-back-inner` is a flex column with `margin-top: auto` on `.poke-links` — children heights do not sum to the container. **The trustworthy signals are `plate.scrollHeight - plate.clientHeight` and the inner-vs-plate height delta**, which read 0 and -4 identically on all seven cards. Also: `.poke-plate` is `overflow: hidden`, so an over-budget back would clip silently rather than scroll — do not rely on a scroll check alone. And lazy images never load in an offscreen iframe; force `loading="eager"` before measuring a front face.

- **Conditional `Home` pill added to the desktop nav.** Shows on every page except the homepage, first in the pill row, generated in `standard-site-nav.js`. Amends the earlier "no Home item, the logo carries it" rule without reversing it — the homepage still spends no slot on a self-link.
  - `isHomePage()` checks **two** things: `data-active === "home"` (fast path) and the URL (so a page missing `data-active` cannot render a Home pill pointing at itself). The URL pattern accepts a bare directory because **Netlify Pretty URLs serve the homepage as `/`**, and deliberately does not match extensionless Pretty URLs like `/account`. **Both `/index.html` and `/` were verified to suppress the pill.**
  - Uses the existing `u()` prefixer, so `services/` pages correctly emit `../index.html` — verified.
  - **Width was the real risk**: the row is now four pills + login icon + theme toggle + CTA. Verified at 1280 / 1024 / 880 / 879 across 12 pages — bar fits everywhere, zero overflow, Home never renders active, exactly one active pill on pages that have one. Validator at 68, no console errors. → `standard-site-nav.js?v=20260817-home-pill` (30 pages).
  - **The hamburger dropdown now hides its Home row on the homepage too** (follow-up the same day), so the desktop pill and phone menu agree. Verified: homepage dropdown is **5 rows** at both `/index.html` and `/`, every other page **6**, zero duplicate destinations. → `standard-site-nav.js?v=20260817-home-pill2` (30 pages).
  - **The phone bottom tab bar deliberately keeps Home everywhere.** It is a tab bar, not a menu — a constant set with the current item marked. Dropping a tab on one page would shift the other three under the user's thumb, so identical tap positions would hit different destinations page to page. This asymmetry with the dropdown is intentional; do not "fix" it.

- **Homepage hero subtext shortened, 28 words → 14 (two passes).** Was "Modern websites for Southwest Florida businesses — designed, hosted, updated, and supported by one team. When the work outgrows a website, we build the software behind it too."; now "Websites for Southwest Florida businesses — designed, hosted, updated, and supported by one team. When you outgrow one, we build the software too."
  - First pass trimmed it to 23 words. **The owner then removed the closing sentence "When you outgrow one, we build the software too." entirely**, leaving: *"Websites for Southwest Florida businesses — designed, hosted, updated, and supported by one team."* The hero is now purely websites-first; software is still introduced by the "what we build" section further down, so the Software nav item is not orphaned.
  - **This buys fold clearance, measured at 1280×768:** lead height **160px → 128px → 96px**, moving the proof strip's bottom **572px → 540px → 508px** — 64px further clear of the fold that CURRENT_STATUS requires it to stay above. Phone (390): lead 72px, proof bottom 582px, no overflow.
  - Landed in **both duplicated trees** (identical strings, so one `replace_all`); verified the new copy appears exactly twice and renders identically at 1280 and 390.
  - **Meta descriptions deliberately NOT changed.** Lines 19/28/36 carry their own longer copy ("small businesses… plus custom software and on-site tech") tuned for the ~155-char search/social snippet. This was a length trim, not a positioning change, so the metadata rule in CURRENT_STATUS does not trigger. The separate body sentence at L611 is likewise untouched.

- **Light-theme contrast fixed on `apps.html`: the hero wordmark was invisible.** The physics wordmark is a **2D canvas**, so no computed-style audit could ever see it — every previous sweep reported the page clean while the brand name sat at **1.12:1** and its subline at **1.1:1** on the light ground. Found by taking an actual screenshot.
  - `assets/surette-physics-wordmark-canvas.js` hardcoded `#ffffff` (wordmark) and `#AAFF00` (subline) with **zero theme awareness** — no `sds-themechange` listener, no CSS-variable reads. `fractal.js` had already solved this exact problem for its WebGL scene; the wordmark never got the same treatment.
  - Added `colorLight` / `introTintsLight` to both presets plus an `applyThemeToGroups()` that swaps them on the `sds-themechange` event. **Authored dark values are stashed in `_colorDark`/`_tintsDark` on first apply**, so toggling is lossless and dark mode is provably unchanged. Colours are read from `group.color` at draw time, so no rebuild is needed and the physics state survives a toggle.
  - Light values, chosen by measurement: wordmark **`#0b0e10` → 17.32:1** (the site's heading ink); subline **`#3f6212` → 6.33:1**, which keeps the lime hue. **Do not lighten the subline toward the dark-mode lime** — `#4d7c0f` is the next step up and already fails at 4.47:1.
  - **Verified by sampling the rendered canvas pixels, not by eye:** light mode paints rgb(11,14,16) at 17.32:1 and rgb(63,98,18) at 6.33:1; dark mode still paints rgb(255,255,255) and rgb(170,255,0), identical to before. Live toggle repaints without a reload. → `surette-physics-wordmark-canvas.js?v=20260817-wordmark-theme` (2 pages).
  - **Screenshots DO work in the preview pane** (they returned fine this session), contradicting the long-standing note in these docs that they never do. The canvas bug was invisible to every computed-style method and only a screenshot exposed it — **look at the page, don't only measure it.**
  - Also found: **`terms.html` loads the wordmark script but never instantiates it** — dead weight, same class as the deleted `rail.js`. Logged in TASKS, not removed.

- **`assets/rail.js` deleted — dead code removed from 13 pages.** The process-rail popover had lost its markup at some point: no page contained the `#rail-popover` element, any `[data-step]` trigger, or a `[data-process-rail]` root, so the script hit `if (!pop) return;` and did nothing on every page that loaded it. Removed in full:
  - the file `assets/rail.js`,
  - **13 `<script>` tags** (11 root pages + 2 under `services/`),
  - the orphaned `.rail-popover-*` CSS — **10 rules each** from 7 root pages and **3 each** from the 2 service pages, 76 lines total, all CSS-only with zero markup referencing them.
  - **Not touched: the portal's own rail**, which is a completely different component under the `client-admin-*` prefix (`client-admin-tabs-rail__brand`, `client-admin-rail-link` in `account-admin.html` + `client-portal.css`). The bare-`rail` mentions in `nav.css` are layout comments. Checking that these were separate systems was the load-bearing step — a naive `rail` sweep would have gutted the Admin Center sidebar.
  - **`[id^="step-"] { scroll-margin-top: 6rem; }` was deliberately preserved** in `index.html` — it sits immediately after the deleted block and still serves the three homepage process cards.
  - Verified: zero residual references to `rail-popover` / `rail.js` / `data-process-rail` / `data-step=` anywhere; zero invalid-UTF-8 files; zero mojibake; **every inline `<style>` block still brace-balanced**; a fresh tab records no request for `rail.js` and no console errors; 26 page-loads across 13 pages × 2 widths render with no overflow. Validator unchanged at 68. Backup in the session scratchpad as `rail.js-retired-20260817.bak`.

- **`process.html` retired — folded out to two destinations, not one.** Owner asked to fold it into `websites.html` and kill the page. Reading it first showed its unique content was **not all website content**, so folding it wholesale would have put software copy on the websites page:
  - Its **"Replacing the software you already run — without the risk"** section (5 steps: assess → trim → prototype → run on real data → switch) was tagged **`BUSINESS SOFTWARE`** on the page and opens "when the project is custom business software". It moved to **`apps.html#software-process`**, the Software page — a different audience from websites.html. New `.app-swap-steps` rules in `surette-data-systems-apps.css`, fully token-driven; the numerals use the `-on` purple variant because raw `#7000FF` measures 2.91:1 as a foreground.
  - Its **Maintain** detail (automated backups and quick restore, security patches and uptime monitoring, performance tuning and speed checks, dedicated WhatsApp account) was the only genuinely additive *website* content and went into **`websites.html`**'s "Hosted, backed up, monitored" card as a `.wb-list`.
  - Its **Design / Build / Launch / Maintain** block and its **"Send updates the easy way"** section were dropped as duplicates — `websites.html`'s own four-step section and the homepage WhatsApp section already carry them.
  - **All 6 inbound links repointed**: `apps.html`'s "View Our Process" button now anchors to `#software-process` on its own page; both service pages' CTAs (2 each) go to `websites.html#how-it-works`; `rail.js`'s `launch` step and its `activeKeyFromPath` check repointed to `websites.html`. Added `id="how-it-works"` to `websites.html`'s process section as the anchor target.
  - **Netlify 301s added** for `/process.html` and `/es/process.html` → `/websites.html#how-it-works`.
  - **Validator baseline is now 68**, down from 69 — one fewer English page means one fewer expected missing-`es`-mirror. Verified: 5 steps render styled on `apps.html`, contrast 0 failures in **both themes** on the new section, 4 care items on `websites.html`, `#how-it-works` present, zero remaining `href`/`src` to `process.html` anywhere, zero mojibake, zero overflow. A copy of the deleted file is in the session scratchpad as `process.html-retired-20260817.bak`.
  - **Discovered in passing: `assets/rail.js` is dead code on all 14 pages that load it.** It returns immediately because no page contains the `#rail-popover` element or any `[data-step]` trigger it needs — only orphaned `.rail-popover-ghost` CSS survives in 9 pages' inline styles. Left in place; logged in TASKS.

- **Homepage "How it works" rebuilt on funnls.com's process structure — four steps to three, with the time promise moved into the headline.** funnls' block is `From kickoff to live in ~14 days.` over an anti-friction subhead ("No drawn-out timelines. No back-and-forth.") and exactly three steps: discovery call → design & build → launch & iterate. Ours was a generic 4-card block headed "From discovery to ongoing support" with no commitment in it.
  - **Steps 03 Manage and 04 Support were not process steps** — they described the ongoing service, which the "what we build" cards and the WhatsApp section directly below already cover. Their content folded into a single `03 Launch & care`. This is the same overlap pattern as the nav problem: sections that each partly told the same story. **Keep it at three; a fourth card reintroduces it.**
  - New headline: **"From first call to live in about 1–2 weeks."** The figure is **the owner's own published One Page estimate**, lifted from `websites.html:167` — nothing was invented. **Its qualifier must travel with it**: the subhead keeps "A One Page site is usually live in 1–2 weeks once content and access are in hand — larger builds run longer." Owner explicitly chose the qualified framing over the punchier unqualified one. A delivery timeline is a factual claim about the business, in the same class as a star rating or a client counter — do not restate it anywhere without the qualifier, and do not raise it without the owner.
  - Step copy is outcome-focused per funnls ("more calls, quotes, and booked work") rather than deliverable-focused.
  - **Landed in both of `index.html`'s duplicated trees.** Parity asserted by string count: `HOW IT WORKS`, `Discovery call`, `Design &amp; build`, `Launch &amp; care`, and `live in about 1` each appear **exactly twice**; zero occurrences of the retired `>Manage<` / `>Support<` / `From discovery`.
  - Desktop grid went `md:grid-cols-4` → `md:grid-cols-3`. Verified at **1920/1440/1280/1024/880/879/414/390/320px**: 3 cards, always one row, always equal height, zero overflow at every width. Contrast measured on the section in **both themes: 0 failures** (all colours are existing tokens — no literal was introduced). Validator at the 69 baseline.
  - **Not changed:** `websites.html` (4 steps), `process.html`, and `services/website-design-hosting.html` (8 steps). The 3 → 4 → 8 ladder is progressive disclosure and was left deliberately; see TASKS if it should be reconciled.

- **Header nav condensed to `Websites · Software · Pricing` — the "Work" item is gone.** Owner reported that Websites, Software, and Work all read as "showing work I've done", and chose Option A from `NAV_RESTRUCTURE_PLAN.md`. Confirmed by mapping the actual page content: "Websites we've built" existed on three pages, the app list on three pages, and `portfolio.html` was a pure interstitial whose two cards led to `apps.html` (**already the Software nav item**) and `casestudies.html`. Following funnls.com, the nav now names what a visitor can buy and proof lives inside those pages.
  - `assets/standard-site-nav.js`: removed the desktop Work pill (and its `portfolio-nav-button` class, referenced nowhere else) and the mobile dropdown's Work row. Bottom tab bar needed no change — it never carried Work or Software. `copy.work` kept in both language blocks.
  - `LEGACY_ACTIVE` gained `work: "websites"` and its `portfolio` entry was repointed from `work` to `websites`, so any un-remapped page still highlights something.
  - Remapped `data-active`: `casestudies.html` + the 6 `portfolio-*.html` detail pages → `websites`; `portfolio.html` → `none`.
  - **`portfolio.html` stays live and is deliberately NOT redirected.** It has 6 inbound in-page links and is a legitimate landing page for anyone searching "portfolio"; a redirect would strand those links.
  - Steps 7 and 8 of the plan turned out to be **already satisfied**: `pricing.html:466` already routes to `app-pricing.html` ("Software, not a website"), and both service pages already link their galleries (`apps.html:201` → `app-catalog.html`, `websites.html:230` → `casestudies.html`). No content edits were needed.
  - → `standard-site-nav.js?v=20260817-nav-condense` (31 pages). Verified 12 pages × 5 widths (1280/1024/880/879/390): zero overflow, desktop row always fits, exactly one active pill on all 15 checked pages, zero `portfolio.html` links in any nav surface, no duplicate dropdown destinations. Validator at the 69 baseline.

- **Sitewide mojibake introduced and repaired the same session — read `DEVELOPER-NOTES.md` §3 before any bulk edit.** The cache-buster bump was applied with Windows PowerShell `Get-Content -Raw` + `WriteAllText`, which decodes UTF-8-without-BOM as Windows-1252 and re-encodes the result. Every curly quote, em-dash and middot in **31 files** became `â€™`/`â€”`/`Â·`. It is silent — the intended edit lands correctly — and was caught only because the validator jumped **69 → 271**.
  - Repaired losslessly by reversing the transform (decode UTF-8 → re-encode CP1252 → those bytes are the original), with encoder/decoder exception fallbacks so an unmangled file throws rather than being corrupted, looping while the marker count strictly decreased. Files touched by two passes (`portfolio.html`, `casestudies.html`, the 6 `portfolio-*`) were double-mangled and needed two reversals.
  - After repair: 0 invalid-UTF-8 files, 0 mojibake markers across 60 iframe page-loads, validator back to 69, and both intended edits intact.
  - **Rule: bulk `.html` edits must use `[IO.File]::ReadAllText/WriteAllText` with explicit `UTF8Encoding($false,$true)`, the Edit tool, or git-bash `sed` — never the PowerShell text cmdlets.** Always re-run the validator after a bulk pass; the jump off the 69 baseline is the only cheap signal.

- **`DEVELOPER-NOTES.md` rewritten** — it was stamped 2026-06-10 and had drifted badly: wrong brand, claimed no `package.json` (one exists, dev-only for Playwright), claimed Spanish `es/` mirrors were present, described Tailwind as "precompiled" (it is the runtime CDN, which is the root of four separate bugs here), said "38 pages" where the real count is 31, and said none of these copies is a git repo. Added the missing subsystems: nav/footer single-source, the 880px breakpoint, the whole theme system, the Tailwind CDN trap, the verification constraints, and the 69-item validator baseline. Recorded funnls.com as the named design reference.

## 2026-08-16

- **Proof strip extended to `websites.html` and `pricing.html`, one quote each.** All three testimonials on the site are now working rather than sitting in a single section nobody scrolls to, and **each page carries the quote that evidences its own claim**:
  - `index.html` — "It feels like having our own website department." (Business Owner) → the managed-service promise.
  - `websites.html` — "We send a message, and it's done — no back-and-forth, no confusion." (Practice Manager) → proves the page's "a website you don't have to babysit" headline.
  - `pricing.html` — "We needed changes before a busy season — everything was updated within a day." (Office Administrator) → proves the monthly update allowance the plans are sold on, and sits directly above the price cards.
  - Added `.sds-proof--start` for left-aligned heroes: `websites.html`'s hero copy is left-aligned while the other two are centred, and an auto-centred pill under left-aligned text reads as a mistake. Verified the strip's left edge now matches the lead's to within 2px there, and stays centred on the other two.
  - All quotes verbatim from the testimonials section with their published attributions. Above the fold at 1280×768 and 390×844 on every page.

- **Hero proof strip + website-first metadata.**
  - **Proof strip** (`.sds-proof` in `nav.css`) sits directly under the hero lead in both layouts — a pill on desktop, squared and stacked below 880px. It carries a **verbatim client quote already published in this page's own testimonials section** ("It feels like having our own website department." — Business Owner, Local Service Business, SWFL) with its existing attribution. **Deliberately no star rating and no "trusted by N businesses" counter:** those are factual claims about the business that nobody has verified, and inventing them was not on the table. The full testimonials section stays where it is.
  - Placement was corrected during verification: first attempt put it in the hero's bottom band at `top: 870`, which is below the fold on a 1280×768 laptop — exactly defeating the point of a proof strip. Moved under the lead; now above the fold at 390×844, 880×768, 1280×768 and 1440×900.
  - **The homepage `<title>` and meta description were still software-first** — "Agentic AI-Powered Business Software" — which is what Google and social shares were showing even after Phase 4 reordered the page. Title is now "Websites for Southwest Florida Businesses | Surette Data Systems"; description, `og:*` and `twitter:*` updated to match. The hero lead was tightened in the same pass.
  - 36 pages × 2 themes × 2 widths: 0 contrast failures, 0 overflow. Validator 69, unchanged.

- **Shared `.sds-*` layout system — Phase 5, completing the layout upgrade.** `pricing.html` and `websites.html` had each hand-rolled the same twelve patterns under different prefixes, and **nine of the twelve had already drifted** on values meant to match: 2.1rem vs 2.25rem headings, 84px vs 88px section gaps, 26px vs 28px card padding, 46rem vs 44rem lead width. None of it deliberate — just what happens when the second page is copied from the first by hand.
  - The system now lives in `assets/nav.css`: `.sds-wrap` `.sds-section` `.sds-eyebrow` `.sds-h1/h2/h3` `.sds-lead` (+`--center`) `.sds-body` `.sds-fine` `.sds-card` `.sds-panel` `.sds-btn` (+`--solid`) `.sds-actions` `.sds-grid-2/3/4` `.sds-media` `.sds-hero-secondary`. Canonical values split the drifted differences.
  - Both pages migrated: **15 duplicated rules dropped from pricing, 22 from websites**, markup renamed onto the shared classes. `index.html`'s two orphans folded in as well — `.sds-work-art` became `.sds-media`, `.sds-hero-secondary` moved out of the page and into `nav.css`.
  - **Only genuinely page-specific components stayed behind**: pricing keeps 16 rules (billing toggle, most-popular badge, price block, feature list, route bands, FAQ), websites keeps 6 (proof card, step numeral, icon chip, list). Down from 38 and 29.
  - `.sds-lead` centring is a **modifier**, not baked in — pricing centres its hero lead, websites left-aligns the same element. That difference was real and is preserved.
  - Verified the migration is visually inert: `.sds-card`, `.sds-eyebrow`, `.sds-btn` and `.sds-h2` now compute **identically on both pages**, and the page-specific components still layer correctly on top — `.pr-badge`'s absolute positioning depends on `.sds-card` providing `position: relative`, which it does.
  - 36 pages × 2 themes × 2 widths: 0 contrast failures, 0 overflow, 0 nav issues. Validator **69**, unchanged. Zero remaining duplicate definitions of any shared pattern.

- **Homepage restructured websites-first — Phase 4 of the layout upgrade.** Section order is now hero → what we build → agentic AI → recent work → local services → process → support → testimonials → newsletter. **The Nova hero is untouched and stays centred** — moving to a left-copy/right-visual hero would fight the full-bleed WebGL background for little gain.
  - **Hero leads with websites.** The lead copy became "Modern websites for Southwest Florida businesses — designed, hosted, and supported by one team. Plus the custom software and on-site tech to run everything behind them." Websites first, software second, local third, matching the chosen positioning.
  - **Two-button hero** (`Get started` + `See pricing`), mirroring the reference site.
  - **The flat 4-card services grid became a 2-up "What we build"** (Websites, then Software) plus a **compact "Also local" strip**. This is the important fix: retiring the Services dropdown in Phase 1 left `in-home-services` and `office-network-setup` with **no nav entry at all** — the strip is their route back, and they now appear on the homepage rather than only in the footer.
  - The Websites card repoints from `services/website-design-hosting.html` to the new `websites.html`.
  - **New "Recent work" band** — three case studies linking to their detail pages, plus "See all work" → `portfolio.html`. Reuses the same deliberately-light assets as `websites.html` (567 KB for all three, lazy-loaded, below the fold).
  - **Both layout trees changed together.** index.html keeps duplicated desktop and mobile markup, so every edit landed twice and parity was asserted afterwards (2× each of "WHAT WE BUILD", "ALSO LOCAL", "RECENT WORK", "See all work", `websites.html`). Section count went 14 → 18: exactly the 4 added, none lost.
  - Verified at 390/700/879/880/1280/1440px: correct tree active at each width, equal-height "what we build" grid preserved (the documented 2026-08-01 symmetry work still holds), 0 overflow. 36 pages × 2 themes × 2 widths — 0 contrast failures, 0 nav issues. Validator **69**, unchanged (no new pages this phase). Console clean.
  - Measurement note: `querySelector('.md\\:hidden')` returns the **nav's** injected mobile shell, not the page's content wrapper — an audit using it reports zero mobile content. Reach the content tree via `document.querySelector('#hero-mobile').closest('.md\\:hidden')`.

- **New `websites.html` + "Get started" everywhere — Phase 3 of the layout upgrade.**
  - **`websites.html`** is the Websites nav item's destination and the websites story: what you get (3 cards), how it works (4 numbered steps), recent work (3 case studies), the two ways to buy, an "also available" strip covering software / multichannel commerce / on-site services, and a closing CTA. It carries **no plan tables** — `pricing.html` is the summary and `services/website-design-hosting.html` remains the full reference.
  - Content is condensed from the detail page and `website_pricing_plan.txt`; nothing invented. Timelines are quoted as the spec states them (about 1–2 weeks for Starter, 5–8 for Growth) and explicitly labelled estimates that begin once content and access are received.
  - Proof cards are **plain links, deliberately not the collectible-card component** — that carries a fixed 2.5/3.5 ratio and a back face with a documented never-scroll budget, none of which this page needs.
  - **Image payload caught during verification: 3.2 MB → 567 KB.** The obvious homepage screenshots were 2.1 MB and 996 KB for cards rendered ~380px wide. Swapped to `screenshot-compare.jpg` (226 KB) and `store.png` (264 KB), which are also the shots the copy actually describes — the drag-to-compare slider and a vendor storefront.
  - `WEBSITES_HREF` in the nav generator now points here. Both new-page constants have served their purpose: the nav never shipped a 404 across three repointings.
  - **Primary CTA unified on "Get started" — 21 anchors, up from 5.** Converted: `Schedule a Free Consultation` (5), `Contact Us` links (5), `Schedule a Visit` (2), `Start a project`, `Request a Quote`, and the nav's gradient CTA. **Deliberately not converted:** `Request Access` (13 — the documented 2026-08-08 access-by-request decision), `Request Custom Version` (10), every collectible-card foot button (width-constrained; >105px scrolls AuctionBuddha's card back at 320px), form submit buttons, and legal actions like "Request account deletion". The phone bottom-tab-bar's **Contact** stays a destination label, not a CTA verb.
  - One anchor escaped the scripted pass because it wraps an icon `<span>` before its text (`portfolio.html`'s hub CTA) — caught by tallying the result rather than trusting the replace count.
  - Verified: 36 pages × 2 themes × 2 widths — 0 contrast failures, 0 overflow, 0 nav issues, no broken internal links from either new page. One self-inflicted failure fixed en route: the step numerals were cyan at 0.55 alpha, 2.45:1 on paper against the 3:1 bar they need at 2rem bold. Validator **69** = 67 baseline + 2 new English pages. Console clean.

- **New `pricing.html` — Phase 2 of the layout upgrade.** A simple four-card front door over the existing detail page, mirroring funnls.com's pricing page structure with this site's own visual identity (void-black, cyan, mono — no cream/serif/coral).
  - **Four plans, mirroring funnls' four-tier shape**: Starter $125 · Lead Capture $250 · **Local Business $375–500 (most popular)** · Growth $750. The fifth managed plan (**Mini, $185**) is deliberately omitted from the front page — it sits 60 dollars from Starter with a weaker differentiator, and dropping it produces funnls' pricing rhythm (roughly 2× / 1.75× / 1.7× steps). Mini is still sold and still fully documented on the detail page, and the front page says so in the fine print.
  - **Prices are this business's real numbers from `website_pricing_plan.txt`.** "Mirror funnls plans" was read as its four-tier *structure*, not its prices — funnls charges $65–$449, this site charges $125–$750. They are different businesses.
  - **Monthly / annual toggle** backed by the real prepaid figures ($1,250 / $2,500 / from $3,750 / from $7,500), not monthly × 12 — prepaying is discounted, so deriving them would have printed wrong prices. Each price element carries its own `data-monthly` / `data-annual` string and the script only swaps text.
  - **Three escape-hatch bands** for the paths that do not fit the monthly ladder — one-time builds, care plans, software pricing — mirroring how funnls handles "One Page" and "Enterprise & Franchise".
  - **Six FAQs, every answer traced to the spec** (setup fees, 12-month term, ownership after 12 months, time-based update allowances, cancellation and early-cancellation terms, annual prepay). Nothing invented.
  - **`services/website-design-hosting.html` keeps its URL** and becomes the full-detail reference: H1 reframed to "Full Website Pricing Details", with a "Simple pricing overview" link back. Nothing was deleted from it.
  - Built by script from `app-catalog.html`'s head so the pre-paint resolver, critical baseline, head-stamp and variable-backed tailwind config are byte-identical to a known-good page. Modelled on app-catalog because it is a **single responsive layout** — no duplicated desktop/mobile markup. Both idempotent scaffolding scripts report zero changes needed on it.
  - Nav's `PRICING_HREF` now points here; `WEBSITES_HREF` still resolves to the detail page until Phase 3.
  - Verified: 35 pages × 2 themes × 2 widths — 0 contrast failures, 0 overflow, 0 nav issues. One self-inflicted failure caught and fixed en route: the active toggle button was cyan-on-cyan-tint at 4.44:1 on paper, the same pattern already corrected for `.pill`/`.badge`. Validator **68** = the 67 baseline plus this page's expected missing `es/` mirror. Console clean.

- **Navigation and IA reworked — Phase 1 of the funnls.com-inspired layout upgrade.** The owner referenced his other site (funnls.com), specifically its focus on websites and its simple top-level **Pricing** item. This is the nav half; scope decisions are in the session plan file.
  - **Desktop nav is now `Websites · Software · Work · Pricing` + the Contact CTA**, replacing `Home · Services▾ · Portfolio · Client Login · Contact`.
  - **Pricing is top-level for the first time** — it was buried two levels deep inside a 2,534-line services page and appeared in no menu at all.
  - **The Services dropdown is retired.** Its two "online" entries became the Websites and Software items; the two local-service pages (in-home tech, office network setup) now rely on the footer plus a coming homepage strip, and their `data-active` is `none`.
  - **Client Login is icon-only on desktop**, matching what the phone header always did — that is what freed the slot Pricing took.
  - **Active page is a filled pill** (`.sds-nav-pill.is-active`) instead of a cyan underline; a 2px rule under one of five same-coloured words was too weak a "you are here". Plain CSS in `nav.css`, never Tailwind, per the injected-markup rule. `min-height` pins all pills equal — the icon-only one otherwise rendered 1.2px short and skewed the bar.
  - **`data-active` keys changed**: `apps`→`software`, `portfolio`→`work`, `services`→`websites`/`software`/`none` by page; 10 pages remapped by script. The generator carries a `LEGACY_ACTIVE` alias map so any page not yet remapped still highlights correctly rather than highlighting nothing.
  - **`assets/mobile-services-nav.js` removed from all 29 pages.** Not merely dead: it finds the Services tab by regex-testing hrefs for "services", and the new Websites tab points at `services/website-design-hosting.html` — it would have silently attached the old popout to the wrong tab. File left on disk, unreferenced.
  - **New nav items point at existing live pages until their own pages land** (`WEBSITES_HREF` / `PRICING_HREF` constants at the top of the generator), so the nav cannot ship a 404 mid-rollout.
  - Verified: 34 pages × 2 themes × 2 widths — 0 contrast failures, 0 overflow, exactly one nav per page, zero stale popout markup, no duplicate destinations within a menu, correct `../` prefixes on `services/` pages. Bar fits without wrapping at 880/960/1024/1280/1440/1920. Validator 67 baseline; console clean.

- **Extended the contrast sweep to phone width (390px) and found 169 more failures — in BOTH themes.** Every previous sweep ran at 1280px only, where the site's entire mobile markup path never renders: `.dm-footer-mobile`, the phone header's injected account icon, the bottom tab bar. Two shared-chrome causes, hitting nearly every page:
  1. **Phone footer legal block** (`.dm-footer-mobile__legal`, `rgba(121,119,119,0.6)`) — the 10px copyright/phone/location text. **2.28:1 on dark and 2.16:1 on paper**: it had been failing AA on the phone since the footer consolidation and had simply never been measured at a width where it renders. 140 elements across 28 pages. Now the `on-primary-container` token at full opacity — 4.58:1 dark, 6.66:1 light.
  2. **Phone header account icon** (`account-nav.js`, `rgba(196,199,199,0.55)`) — 1.28:1 on the light bar (effectively invisible) and 4.09:1 on dark, under AA at that icon size. 29 pages. The colour is applied as an **inline style**, which outranks any stylesheet, so it could not be themed from nav.css; it now reads `var(--sds-acct-out)` / `var(--sds-acct-in)` with the tokens defined per theme. Still grey-vs-cyan for signed-out vs signed-in — that distinction is what carries meaning, and it is preserved.
- **Final coverage: 34 pages × 2 themes × 2 widths (1280 and 390) — 0 contrast failures, 0 horizontal overflow.** 3,871 elements at desktop, 3,705 at phone. Validator at the 67-item baseline.
- **Lesson recorded in TASKS:** a desktop-only audit silently skips this site's entire phone layout. Both of these were long-standing bugs, not theme regressions — the light theme is just what finally caused anyone to measure.

- **Fixed `multichannel-commerce-website.html`'s 92px horizontal overflow** (pre-existing, present in both themes). The cause was `.mc-browser::before` — the hero mockup's decorative cyan halo at `inset:-18%`, which on a 613px mockup is a box hanging 110px past each side. The mockup sits only ~21px from the viewport edge at 1280px, so that empty box pushed the document to 1357px and the page genuinely scrolled 92px sideways onto nothing (`window.scrollX` reached 92). It was worse on tablet — 243px at 880px wide.
  - **Why `body{overflow-x:hidden}` never caught it:** pairing `hidden` with `overflow-y:visible` makes BODY its own scroll container rather than propagating the value to the viewport, so `html` kept scrolling.
  - **Fix: `overflow-x: clip` on `.mc-page`**, which spans the full viewport width — so it clips exactly at the screen edge, where the remaining box was already off-screen. Nothing visible is lost: the gradient is `transparent 62%`, so its painted radius only reaches ~40px past the mockup. Shrinking the inset instead was ruled out — the bleed needed exceeds the 21px of room available. `clip` rather than `hidden` deliberately: it is the only value that pairs with `overflow-y:visible` without silently creating a scroll container. Safari <16 ignores it and keeps the old behaviour.
  - Verified at 390/880/1280/1440px in both themes: overflow 0, `scrollX` pinned at 0, glow still rendering at every width.
  - **Diagnostic note for next time:** no element's `getBoundingClientRect()` exceeded the viewport, because the culprit was a **pseudo-element** — invisible to `querySelectorAll`. Bisecting by hiding subtrees pointed at the wrong node (hiding it changed grid sizing). What actually found it was disabling all `::before`/`::after` in the hero and watching `scrollWidth` drop to exactly the viewport width.
- Tokenised the last two logo literals on that page (`text-shadow:0 0 12px rgba(0,190,255,.45)` and `opacity:.7`, both written in compact form and so missed by the spaced-form sweep). Light mode there now drops the cyan bloom like every other page.

- **Dark-mode contrast fixed: 49 failures → 0.** These were all long-standing, pre-dating the theme work; the light-mode sweep is simply what finally measured dark properly. **Six root causes, and several change how dark mode looks — see the visible-change list below.**
  1. **`text-nebula-purple` (#7000FF) as a foreground colour — 31 of the 49, across 8 pages.** A saturated blue-violet measures 2.91:1 on the site's near-blacks: under AA for the small-caps labels and under even the 3:1 large-text bar for the 36–48px feature icons. The token could not simply be lightened because it is also the end stop of every cyan→purple gradient CTA, so text/icons now use a separate `--t-nebula-purple-on` (dark `#a35cff`, 4.65:1; light unchanged at `#5a00cc`). Overridden as `:root .text-nebula-purple` — specificity (0,2,0) — because the Tailwind CDN loads after nav.css and a plain pin would lose.
  2. **App-credit pill label** was 58% of on-surface-variant: 4.43:1 dark, 2.6:1 light. Now full opacity (11.4:1 dark).
  3. **App-credit brand name** `rgba(168,85,247,.82)` at 3.69:1 → `#ab5cf7` full opacity (4.71:1).
  4. **`text-outline/40 /50 /60`** measured 2.36:1 on black as well as on paper — the alpha was failing in *both* themes and had only ever been checked on one. The full-opacity override now applies to both.
  5. **`#fractal-gui-toggle` "grain" control** at 0.18 alpha (1.59:1 dark / 1.49:1 light). It is a real button with an aria-label, not decoration, so it is now 0.6 alpha — 7.25:1 dark, 5.00:1 light.
- **Visible changes to dark mode** (all deliberate, all contrast-driven): the purple feature icons on 8 pages are a lighter violet; the app-credit pill's label and brand name are brighter; small `text-outline` print is no longer dimmed; and the fractal settings toggle is now clearly visible rather than nearly hidden. Nothing else about dark mode moved — gradients, fills, surfaces and body copy are untouched.
- **Final state: both themes, all 34 pages, 3,871 elements, 0 contrast failures.** Validator at the 67-item baseline.
- **Still open (not contrast):** `multichannel-commerce-website.html` overflows 92px horizontally at 1280px, identically in both themes — a pre-existing layout bug.

- **Full light-mode sweep of all 34 pages.** Audited every in-system page with an iframe harness (same-origin, computed-style based) rather than page-by-page: **3,871 elements checked, down from 100+ failures to 3.** Five systematic causes were found and fixed, none of which the earlier five-page spot-check had surfaced:
  1. **`.primary` lime button, 7 pages** — `background:#39ff14` with `color: void-black`. The lime does not flip but the text did, giving light-on-lime at **1.21:1**. Added `--c-on-bright` (always dark): a fill that does not flip needs a label that does not flip. Mirror of `--c-on-accent`.
  2. **Translucent dark panels, 30 across 22 values** — `rgba(8,9,13,.96)`-style bespoke panels, missed because they are neither a palette triplet nor a plain hex. Left dark while their text flipped to ink; this alone was 40 failing elements at 1.03:1 on `multichannel-commerce-website.html`. Now 17 paired `--t-surf-*` tokens, dark side byte-identical, colored status panels keeping chroma.
  3. **Opacity-modified text utilities** — `text-outline/50` (2.03:1), `text-on-surface-variant/70` (3.22:1 on all six portfolio detail pages). Alpha that reads fine knocking back a light colour on black washes a dark colour into the paper instead. Light mode restores full opacity for these; `text-starlight-white/70` is exempt at ~5.5:1.
  4. **`multichannel-commerce-website.html`'s private `--mc-*` palette** — predates the token layer, so the sitewide sweep never touched it; `--mc-muted` alone was the body copy of the page at 1.59:1 across 30 elements. Overridden in the page, not in nav.css.
  5. **`.pill` / `.badge` cyan-on-cyan-tint** at 4.29–4.35:1, plus the app-credit pill's 58%-alpha label and 82%-alpha purple.
- **Accent tokens re-sized once more.** `electric-cyan` / `tertiary` were sized against `#e5e2db`, but the sweep turned up a real card surface at `#dddcd8`; cyan measured 4.37:1 there. Now `0 104 121` / `0 105 119`, clearing 4.70:1 on the darkest surface actually observed. This is the second re-sizing — the lesson is to pick the worst case from measurement, not from the token table.
- **One genuinely pre-existing fix while in there:** the `#page-loader` "Loading" label was 0.4 alpha, failing in **both** themes (2.9:1 dark, 1.86:1 light). Now full opacity. Note the governing value was an inline `style` attribute on the span, not the `nav.css` rule.
- **Remaining light-mode failures: 3**, all the same `#fractal-gui-toggle` "grain" dev control at 0.18 alpha (account, apps, process) — deliberately near-invisible and failing identically before this work.
- **Dark re-verified across all 34 pages: 58 failures, every one pre-existing.** Confirmed by value comparison against the pre-change backup rather than by inference — `#7000FF` decorative icons (2.91:1), the app-credit purple `rgba(168,85,247,0.82)` (3.69:1), `text-outline/50` (2.36:1), and the grain toggle all resolve to byte-identical values before and after. **These are open dark-mode issues worth a separate pass**; they were not introduced here.
- **`multichannel-commerce-website.html` has a 92px horizontal overflow at 1280px — identical in both themes, so pre-existing** and out of scope for the theme work, but it is a real layout bug.

- **Homepage animated background removed in light mode.** In light, the Nova particle galaxy is not rendered *and not initialized* — the module's engine is wrapped in a `SHOW_NOVA` gate, so 150,000 particles are never built and no rAF loop runs. `#nova-bg` (which carries an inline `background:#160016`) and the `#hero-tint` darkening scrim are hidden with `!important`, since an inline style attribute outranks stylesheets. Dark mode is unchanged: the engine text itself was not modified, only wrapped, per the standing "leave the Nova engine alone" rule. Switching *into* dark on the homepage triggers a one-time `location.reload()` — the engine only initializes at module evaluation, and the reload is scoped to homepage + light→dark only, so it cannot loop.
- **Tailwind's stock `white` is now themed too.** `text-white` / `bg-white/N` / `border-white/N` are Tailwind built-ins, not part of the custom palette, so the earlier tokenisation missed all 138 of them — including the homepage `<h1>`, which rendered white-on-paper at **1.12:1**. Fixed by overriding `white` in all 22 `tailwind.config` blocks so the CDN itself emits a variable-backed rule; every opacity variant follows for free. **A `nav.css` pin does not work for stock colours** — the CDN injects its `<style>` after nav.css and emits a hard literal for built-ins, so at equal specificity the CDN wins. (Custom colours only *appear* to be pinned successfully because both sides resolve to the same variable.) Also swept 19 uses of the CSS keyword `color: white`, which the `#fff`-matching passes never saw.
- **Two mistakes worth recording.** (1) A literal script-closing tag written inside a JS *comment* truncated `index.html`'s module from 7,816 to 2,496 chars and orphaned the whole Nova engine — the HTML parser ends the script element on that byte sequence regardless of JS context. (2) The contrast auditor silently skips elements at `opacity: 0`, and `hero-enter` entrance animations never complete in the preview pane (rAF is frozen), so the broken `<h1>` was invisible to the audit. Audits of animated content must force `.hero-enter`/`reveal` elements visible first, or they under-report.
- **Sitewide light theme with a nav toggle.** The site now ships two themes. Dark is unchanged and remains the default; light is opt-in via a toggle in the shared nav (icon button in the desktop header, labelled row with a switch in the phone dropdown), persisted to `localStorage` under `sds-theme` and synced across open tabs. Built as a value-swap: `assets/nav.css` holds the full 51-colour Tailwind palette as RGB channel triplets (`--t-*`) plus light overrides under `:root[data-theme="light"]`, and all 22 inline `tailwind.config` blocks now consume them as `rgb(var(--t-NAME) / <alpha-value>)` — so 776 palette entries and every utility using them follow the theme with **no markup changes**. See DECISIONS for why this rather than `dark:` variants.
- **Pre-paint theme resolution.** An inline resolver is now the first script in all 34 in-system page heads, and `#dm-critical-dark-baseline` gained light-mode rules. Without this a light-mode visitor got a black flash on every navigation, because the no-white-flash baseline paints `#050505` from three places before content shows. The baseline deliberately keeps hex literals — it runs before `nav.css` exists.
- **~1,700 hardcoded colour literals converted to tokens**, in five property-aware passes, each with a reusable script under `scripts/`: 569 white/black veils → `--c-veil-rgb`/`--c-shadow-rgb` (channel variables, so each call site keeps its own alpha); 314 palette hexes in page `<style>` blocks; 99 `color:#fff` (with 10 correctly left alone on accent fills); 560 translucent palette colours in `rgba()` form; 61 in inline `style=` attributes; plus 114 logo declarations and 15 bespoke page panels. Four categories were deliberately **not** swept: the critical baseline, `pokecard-dropin/` (vendor), `multichannel-commerce-website-test-4.html` (already a light page, outside the system), and one-off design accents with no palette equivalent.
- **Light "blueprint" backgrounds.** `cosmic-web.css` (30 pages) reuses its existing hero photo through an `invert(1)` filter — a dark filament web inverted *is* a blueprint — with all overlay colours specified as pre-inversion complements, and per-page tint identity preserved across all 10 `cosmic-tint-*` variants. `fractal.js` (3 pages) listens for a new `sds-themechange` event and re-derives its palette: fog switches to the page ground and the configured neons are pushed toward ink via HSL, since a WebGL scene cannot read CSS variables.
- **Contrast: measured, not eyeballed.** Added `scripts/audit-contrast.js`, which composites translucent ancestor layers to resolve each element's real background and checks against the size/weight-appropriate WCAG AA threshold. Light text tokens are sized against the **darkest** light surface (`#e5e2db`), not the page ground — the first pass sized them against the ground and landed at 4.28:1 on card surfaces. Final: homepage 99 elements / 0 failures, casestudies 182 / 0, pricing page 1,246 / 0 (from 821 at the start of the pass). Dark mode re-verified at 0 failures with every probe byte-identical to its original value. Validator: 67-item baseline, unchanged.
- **Known-remaining (pre-existing, not introduced):** nebula-purple decorative icons on the homepage measure 2.91:1 against the 3:1 large-text bar, and the `#fractal-gui-toggle` dev control is deliberately near-invisible at 0.18 alpha. Both failed identically before this change.

## 2026-08-08

- **Portfolio: only Naples Estate Jewelry and SpotCalc are still live; the other four are offline.** Elite Yacht Detailing, JPSurette, Sean's Ads, and AuctionBuddha lost their sites, so all 16 outbound links across `casestudies.html`'s two duplicated grids became `Ask to View` -> `contact.html`, and their card status tags now read `status - offline` (previously `live`, `in progress`, and `live ecosystem`). The four detail pages followed: the three using `.portfolio-live-widget` got a new `--offline` modifier (href to `contact.html`, `visibility` icon instead of `open_in_new`, `Offline` eyebrow, `Request a Viewing` title, and the pulsing dot animation stopped - it read as a live heartbeat next to the word Offline), and `portfolio-seansads.html` needed a deeper rewrite because its hero eyebrow, a status chip, a section heading, and a whole closing section were built on the site being live. Verified: zero links to the four domains remain anywhere in the site, and the only external portfolio links left are Naples and SpotCalc.
- **Renamed NaplesEstateJewelry.co to .com** across `casestudies.html` (both grids), `portfolio-naplesestatejewelry.html` (title, OG, Twitter, h1, live link), and the `multichannel-commerce-website.html` "See the Live Store" button. Matched on a `` boundary so the substitution could not touch `portfolio-naplesestatejewelry.html` paths or re-apply to `.com`.
- **Caught a card-back regression before shipping.** The first pass used the label `Request Viewing`, which pushed AuctionBuddha's back face into a 24px scroll at 320px - violating the never-scroll rule. The button was not wrapping; the `.poke-links` **row** was, because two buttons no longer fit side by side. Measured the threshold (~105px of button width on a 237px card: `Request Viewing` 108px overflows, `Request Access` 102px barely fits, `Ask to View` 84px fits with margin) and shipped `Ask to View`. Re-verified all 12 backs at 320px, 880px, and 1280px with zero vertical and horizontal overflow. Recorded in DECISIONS, including the trap that the desktop grid is `display:none` below 880px and reports fake passes.

- **Retired the hosted app demos across the site; access is now by request.** Five of the six Surette Data Systems apps — Auction House & Consignment, SDMS, Benji Payroll, Third Street / Antique Mall, and MetalsPredictor — no longer have public instances, so every surface that advertised self-serve access was rewritten. Removed all outbound links to `auctionconsignmentapp.netlify.app`, `secondhanddealer.netlify.app`, `benjipayroll.netlify.app`, `thirdstreetauctions.com`, `chrisappmet.netlify.app`, and the shared `portal.darkmatterapps.com`. Each profile page now leads with a `Request Access` CTA to `contact.html` and a shared amber `.app-access-notice` panel, and the sections built around self-serve access ("Demo Login", "Try It Free", SDMS's "hosted SaaS demo" deployment copy, Benji's "Open the live demo" line) were rewritten to match. On `app-catalog.html` the stat pills changed from delivery type to availability — five read `By Request`, SpotCalc reads `PWA` — and the meta descriptions no longer promise "live-demo" apps. A stray `benjipayroll.netlify.app` button was also found and fixed on `app-pricing.html`, outside the app pages. The notice is defined **once** in `assets/surette-data-systems-app-profile.css` rather than in five inline `<style>` blocks, per the repo's shared-component decisions. Verified at 320px and 1280px across all five profiles: notice renders styled, zero dead external links, zero page overflow, notice contained inside its panel and below the CTA row. → `surette-data-systems-app-profile.css?v=20260808-access-notice` (6 pages). Validator: 67-item baseline, unchanged.
- **Renamed MetalsCalc to SpotCalc site-wide.** 44 brand strings updated across `app-catalog.html`, the app profile page, both hand-duplicated grids in `casestudies.html`, and `portfolio-metalscalc.html`; the app link now points to `https://app.spotcalc.com` and the marketing link to `https://spotcalc.com`; `scripts/capture-metalscalc.js` capture URLs updated too. SpotCalc is the only app still presented as live. **Page filenames and asset directories deliberately keep the `metalscalc` spelling** so no live URL moves and no screenshot path breaks — the brand strings and the paths differ in case, which is what let the rename be applied by case-sensitive substitution without touching a single path. The card palette class became `.tcg-card--app-spotcalc`, so `collectible-card.css` was cache-busted to `?v=20260808-spotcalc` on both gallery pages; without that, a returning visitor's cached CSS would have left the SpotCalc card unstyled. Re-verified the card-back never-scroll guarantee on all 18 backs (12 in `casestudies.html`, 6 in `app-catalog.html`) at a 320px viewport: zero vertical and zero horizontal overflow, and all six catalog cards keep their palettes. Also confirmed the long-standing `darkmatterwebsites.com` OG-URL task is stale — zero references remain anywhere in the site.

## 2026-08-01

- **Softened the shared mobile bottom-nav motion.** Replaced the original brisk 300ms transform / 220ms fade with separate directional curves: a 460ms ease-in/out plus 340ms fade while leaving, and a 520ms spring-like ease-out plus 380ms fade while returning. The scroll thresholds and accessibility behavior are unchanged. Mid-transition sampling confirmed continuous motion and opacity interpolation; verified at 390px with no overflow. All 34 nav.css consumers now use `v=20260801-scroll-hide3`; validator remains at the 67-item known baseline.
- **Added conventional directional hide/reveal behavior to the shared mobile bottom navigation.** The Home / Services / Portfolio / Contact bar now slides and fades away after 24px of downward scrolling and returns after 6px upward, with direction hysteresis to suppress jitter. It always returns at the top, resets at the 879/880px responsive switch, ignores mobile-browser height-only resize noise, closes the Services popout cleanly on scroll, remains keyboard-recoverable, and disables animation under `prefers-reduced-motion`. Implemented once in `standard-site-nav.js` and `nav.css`; cache-busted across 29 nav consumers and 34 stylesheet consumers. Verified on root and nested-service pages at 390px plus the breakpoint boundary with no overflow. JS syntax passes; validator remains at the 67-item known baseline.
- **Improved the spatial concept hero paragraph's readability over the perspective grid.** Replaced low-opacity floating copy with a full-white treatment on a deep navy translucent panel, plus a lime left rule, subtle blur, and lime-tinted offset shadow. The spatial scene remains intact while the AI Listing Assistant description now reads clearly. Verified at 320 and 636px with zero horizontal overflow.
- **Built an unlinked spatial-editorial concept for the Multichannel Commerce page.** Added `multichannel-commerce-website-test-4.html`, a full-content clone styled after the requested Treasure Island `test-4` reference with a cream canvas, cobalt spatial hero, perspective grids, coral/pink geometry, lime accents, oversized Fraunces typography, offset image cards, and a high-contrast AI workflow. The test retains the live marketplace/social destinations and explicitly shows the upload-images, speak-naturally, AI-prefilled-form, review, and publish flow. It is `noindex`, has no inbound site links, and does not alter the current sales page. Verified at 320, 390, and 1440px with zero horizontal document overflow and all local images loaded. Validator: 67 known items, the prior 66 plus this test page's expected missing Spanish mirror.
- **Fixed the multichannel customer-gallery card overflowing off narrow phone screens.** The first figure's `aspect-ratio:16/10` and 250px mobile minimum height forced a 400px card inside smaller containers. Phone CSS now constrains all gallery figures to the grid width and removes the first figure's conflicting aspect ratio while preserving the intended 250px height. Verified at 320, 366, 390, 560, 561, and 879px plus a visual check at the reported 366px case; all figures and captions remain contained with zero page overflow. Validator stays at the documented 66-item baseline.
- **Tightened the multichannel hero copy.** Removed the standalone “Every channel is live now” sentence while retaining the clearer per-channel `LIVE NOW` availability badges.
- **Linked the Apps hub to the Complete Multichannel Commerce page.** Added a featured commerce-platform banner to `apps.html` with the "List it once. Sell everywhere." promise, a concise description of the AI image-and-voice listing workflow, and a direct "Explore Multichannel Commerce" CTA. Click-tested the CTA to the correct page and verified desktop plus 390px layouts without horizontal overflow; validator remains at the documented 66-item baseline.
- **Made the AI Listing Assistant a central multichannel-commerce feature.** The page now explains that sellers upload product photos, speak naturally about the item, and receive a structured, prefilled listing form to review and approve. Added this experience to metadata and hero copy, rebuilt the workflow steps, added a highlighted full-width package feature, and added a detailed FAQ. Human review remains explicit before any channel publishes. Verified desktop and 390px layouts with no horizontal overflow or clipped workflow copy; validator remains at the documented 66-item baseline.
- **Marked the Complete Multichannel Commerce marketplace and social publishing workflow live.** Updated the sales page metadata, hero copy, eBay/Etsy/Instagram/Facebook availability cards, automatic-publishing workflow, feature list, social section, and FAQ so every surface consistently describes all named publishing channels as available now. Removed the prior `COMING NEXT`, rollout, and finalization language. Verified the live local preview on desktop and at 390px with no new browser errors or mobile overflow; validator remains at the documented 66-item baseline.
- **Corrected the Complete Multichannel Commerce page logo across the full responsive header.** The shared logo runtime injects one style block scoped to its first mount (`#sds-logo`), so the second mount (`#sds-logo-mobile`) on this new page had no flex layout: its canvas sat above the Surette wordmark and crowded the 64px phone header. Added the same forced-flex two-mount logo rules used by established pages and explicitly initialized both mounts. Visually and geometrically checked 320, 375, 414, 767, 768, 879, 880, 1024, 1280, 1440, and 1920px; the brand remains inline, the account/hamburger controls clear it, all desktop links and the Contact CTA fit, and the hamburger dropdown opens/closes correctly. No browser errors; only the site's existing Tailwind CDN warning. Validator remains at the documented 66-item baseline.
- **Added and refocused the Complete Multichannel Commerce Website sales page.** New standalone `multichannel-commerce-website.html` turns the Naples Estate Jewelry foundation into a sellable package for jewelry, art, antiques, vintage, collectibles, resale, consignment, and specialty retail. The revised promise is **"List it once. Sell everywhere."**: one product record supplies the branded store, eBay, Etsy, Instagram, and Facebook product and advertising posts without rebuilding the listing. Replaced the dated storefront hero with a fresh live-site capture (`home-live-20260801.png`) while preserving the current catalog/product/auction images, and added the matching 1200x630 `multichannel-commerce-og-v2.png`. The page is not wired into navigation yet by design. Validator: 66 issues—the previous 65-item baseline plus this English page's expected missing Spanish mirror.
- **Homepage section, card, and heading symmetry fixed across responsive widths.** The Services intro is now one centered block; its cards stay 2x2 from 880-1279px and switch to one equal-height 4-card row at 1280px+. Each service card is now the anchor itself, so its complete surface is clickable on desktop, tablet, and phone. The remaining compact and support-section headings are centered, and equal-height tracks normalize the Services, Agentic AI, Process, support-example, and testimonial groups. Audited 12 widths from 320-1920px: all card-edge hit tests passed, with no orphaned groups, unequal repeated-object heights, misaligned section headers, or horizontal overflow. Validator remains at the documented 65-item baseline.

## 2026-07-21

- **Choppy contact-page load fixed (reported via the pricing page's "Schedule a Free Consultation" CTA — plain `contact.html` link, so the fix is the destination's load itself).** Two main-thread/paint hogs were landing inside the reveal-animation window: **(1)** `assets/site-message-forms.js` statically imported the entire supabase-js v2 bundle from jsDelivr at module top — fetched+parsed+executed right at DOMContentLoaded, exactly while the 0.38s root fade and the blurred glass-card object-reveals run (contact's form panel is a `backdrop-filter: blur(20px)` card). Now lazy: `loadSupabase()` dynamic-imports on first submit (failed loads clear the cache so retries re-import), attachments/RPC take the client as a parameter, and a post-`load` `requestIdleCallback` warm-up pre-fetches it so the first submit stays fast — measured: supabase-js now fetches after the load event instead of at DCL. **(2)** `surette-logo.js`'s reveal now waits for `document.fonts.ready` (capped at 500ms; the 850ms absolute safety unchanged) — `display=swap` fonts were arriving mid-fade and re-laying-out the text during the animation. Cached fonts resolve instantly. Verified: contact/apps/app-checkout forms all bind on the new module version, zero console errors, pricing page reveals normally on the gated path. Validator: 65 baseline. → `site-message-forms.js?v=20260721-lazy-supabase` (3 pages), `surette-logo.js?v=20260721-fonts-gate` (28 pages).

- **Double-blink on page load fixed (apps + pricing pages; latent everywhere) — and the wordmark knock-loose feature removed entirely.** The blink mechanism: `nav.css` hides `html` from first paint and reveals un-stamped pages via a 0.2s CSS fallback (`html:not(.sds-logo-loaded)`), but the stamp came from `surette-logo.js` at the BOTTOM of the body — on heavy pages (the 2,500-line pricing page; apps with its module scripts) parsing passes 200ms, so content appeared via the fallback, vanished when the late stamp arrived (the `:not()` rule stopped matching, base `opacity: 0` returned), then faded back in via the JS reveal: visible → hidden → visible = the double blink, and why only heavy pages showed it. Fix: an inline head script now stamps `sds-logo-loaded` before first paint on **all 28 pages that load surette-logo.js** (one stamp each, verified) — the fallback structurally can't fire on them; JS is the only revealer (0.9s CSS + 850ms JS safeties unchanged). **New-page rule: any page adding surette-logo.js must include the head stamp.** Apps.html had a second race: the loader only hid content 50ms in (its slow path), racing the html fade — content could flash, hide behind the loader, reappear. Now a head CSS rule pre-hides everything except the loader from first paint until the loader marks `is-apps-ready`, with a 5s CSS-only `@keyframes` safety un-hide (loader's own JS cap is 4s). **Knock-loose removed at every width** (owner call — was mobile-only since earlier today): `#wm-canvas { pointer-events: none !important }` moved out of the mobile media block to base CSS, and the dead rect-corrected pointer/click/touch handler re-registrations were deleted from the inline script. The load intro (light-up → letters fall → settle) is frame-driven and untouched; `terms.html`'s wordmark keeps its interactions (page-scoped CSS, shared module untouched). Verified: stamp present in head before the baseline style and exactly once on all 28 pages; both reported pages reach opacity 1 / ready state with content visible and zero console errors; canvas takes no events at 414px and 1280px (hits fall through to the `h1`). Validator: 65 baseline.

- **Deploy cleanup: repo root trimmed for as-is Netlify deployment (~11MB of stale files deleted, 18MB node_modules untracked).** Deleted: one-off root scripts (`_enum_artifacts.py`, `_enum_out.txt`, `fix_titles2.py`, `update_titles.py`), the empty `netlify/` dir, and 33 orphaned assets confirmed unreferenced by a full basename scan across every served file type — 6 stale Elite captures (7.6MB; pages use screenshot-compare/home/services only), 5 stale Naples captures, all 6 doc-flagged `antique-mall-*.png` (pages use `thirdstreet-*.png`), 4 generic MetalsCalc `screenshot*.png` (pages use `metalscalc-*.webp`), 3 legacy `darkmatter-og-image*` files, `case-studies-building-blocks.webp`, and 5 dead code files (`care-plans.css`, `site-hero.js`, `surette-data-systems-home-widget.css`, `surette-data-systems-apps.js`, `apps-lang-nav.js`). **Kept despite being unreferenced (deliberate):** the "built/powered by dark matter" badge assets (brand-protected), the SDS brand webp set, Benji SVG source variants, wordmark snippets, `nova/` (engine reference copy), and `pokecard-dropin/app-template/` — which is NOT dead: it's the "Tech Business Site Template" live demo linked from casestudies' View Live button. **Repo hygiene:** `node_modules/` (18MB, 186 files, was tracked!) untracked + gitignored (kept on disk for the playwright tablet-audit; new root `package.json` makes it restorable via `npm install`); `.claude/settings.local.json` untracked + ignored. **The local git index was badly stale** — whole pages (portfolio, terms, privacy, accessibility, metalscalc/metalspredictor, website-design-hosting) and load-bearing assets (`standard-site-footer.js`, `collectible-card.css`, all current portfolio/app screenshots) existed on disk but were untracked; `git add -A` brought the index in line with the disk (98 adds / 225 deletes / 60 modifications staged). Validator: 65 baseline after cleanup — nothing referenced was removed.

- **Fixed the background canvas stuttering during mobile scroll on the apps page — and the same defect on process.html, account.html, and the wordmark canvases.** `fractal.js` (the Three.js attractor background shared by apps/process/account) called `renderer.setSize()` on every window resize; on phones, collapsing/expanding browser chrome fires height-only resizes mid-scroll, and each one reallocates the WebGL buffer — the visible stutter. Same mechanism the homepage Nova background had, so it got the same two-part fix: (1) `fractal.js`'s resize handler is now debounced 180ms and skips mobile (<880px) height-only changes (width/DPR changes still resize, folding in any accumulated height delta) — mirrors `shouldResizeNova()` in index.html; (2) `#canvas-container` + canvas promoted to a composited layer on all three pages (`translate3d`, `backface-visibility: hidden`, `contain: strict` — the `#nova-bg` treatment), so scroll never repaints the fixed background. **Same-class fix in `surette-physics-wordmark-canvas.js`** (apps.html + terms.html): its `_resize()` reallocated `canvas.width` on every resize event even when dimensions were unchanged — and a same-value `canvas.width` set still blanks the bitmap, a wordmark blink mid-scroll; now guarded to skip when width/height/DPR are all unchanged. Canvas audit: index.html Nova already fixed (pattern source), nav logo canvas has no resize listener, remaining `<canvas>` hits are vendor/reference files. Verified with instrumented buffer-write counters on real resize events: height-only resizes (812→700 on apps, 700→600 on process) produce **zero** buffer writes on every canvas; a width resize (375→414) resizes exactly once after the debounce to the correct new dimensions; account.html confirmed on the new script + CSS. Validator: 65 baseline. → `fractal.js?v=20260721-scroll-stutter` (3 pages), `surette-physics-wordmark-canvas.js?v=20260721-resize-guard` (2 pages).

- **Fixed the white hairline under the mobile header (and above the bottom tab bar) on portfolio.html — third confirmed instance of the injected-markup Tailwind failure.** The three fixed bars' hairline borders were Tailwind utilities on injected markup (`border-white/10` desktop, `border-starlight-white/10` mobile header + bottom bar) — classes in no page's static HTML, surviving only on the CDN's DOM observer. On portfolio.html the observer missed and the borders fell back to Tailwind's preflight default `#e5e7eb`: a solid light-gray 1px line across the header bottom and the bottom bar top (reading as a line "on the footer" since the bar sits over it). Fixed per the standing DECISIONS rule: borders pinned in plain CSS in `nav.css` next to the bar-tint rule (`rgba(255,255,255,0.1) !important` — same value the utilities intended; `starlight-white` is #FFFFFF). Utility classes left in the generator markup (harmless; the pin always wins). Verified by computed-style measurement on all 28 public pages at 375px — before: only portfolio.html failed; after: every page renders the identical hairline on all three bars. Validator: 65 baseline. → `nav.css?v=20260721-nav-border-pin` (33 pages).

- **Apps-page wordmark: knock-loose interaction removed on mobile — which also fixed the page being unscrollable at first.** Owner asked for the hover/knock-loose feature off on the phone. One rule in `apps.html`'s existing ≤879.98px block: `#wm-canvas { pointer-events: none !important; }`. Taking the canvas out of hit-testing kills every input path at once — per-frame hover-proximity drops (`_updateHover`), tap releases (`click`/`touchstart`), and the module's own `dblclick` release-all handler, which the page's corrected-handler block had never unbound (a double-tap would have scattered the whole wordmark). The load intro (light-up → letters fall left-to-right) is frame-loop-driven and unaffected; desktop (≥880) keeps full interaction. **The scroll fix came free:** the engine sets inline `touch-action: none` + `preventDefault()` on touches, and on mobile the canvas is extended 280px past the wordmark (bounce floor) — covering the top ~430px of the first viewport, so swipes starting there did nothing. With the canvas off hit-testing, touches fall through and scroll normally. Verified at 375px: 7 probe points down the first viewport all hit normal content with zero `touch-action` blockers in the ancestor chain; 964px of scrollable overflow scrolls (instant-behavior test). The loader's `body.is-apps-loading { overflow: hidden }` was reviewed and deliberately left alone — it only locks while the full-screen loader overlay is visible (≤~1.2s first visit, 4s hard cap, instant on repeat visits). Two pane facts confirmed while verifying: `requestAnimationFrame` never fires in this preview pane (measured directly — the wordmark cannot animate or screenshot here), and the site's `scroll-behavior: smooth` makes programmatic scroll tests read 0 in the pane (smooth scrolls animate on rAF) — **test scrolling with `behavior: 'instant'`**.

- **Mobile nav dropdown: removed the redundant plain "Contact" row; audited every menu for duplicate destinations.** The dropdown emitted two `contact.html` links back-to-back — a plain "Contact" row and the gradient "Contact Us" CTA. Removed the plain row from `standard-site-nav.js` (single-source: all 28 pages at once), kept the gradient CTA — it mirrors the desktop header's pattern (plain links + one gradient CTA), and the bottom tab bar's own Contact item keeps the active-page highlight for contact.html, which the removed row had carried in the dropdown. Audit of every injected menu surface for intra-menu duplicate hrefs (desktop nav, dropdown, bottom bar, mobile services popout, both footer variants), on a root page and a `services/` page (`../`-prefixed path): the dropdown pair was the only redundancy site-wide; all surfaces now zero-dupe. (Contact appearing once each in dropdown/bottom bar/footer is by design — separate surfaces.) Validator: 65 baseline. → `standard-site-nav.js?v=20260721-contact-dedupe` (28 pages).

## 2026-07-17

- **Tech Business Site Template card reformatted on mobile — including a real no-scroll violation nobody had caught.** Owner asked for the template hero widget to get the same treatment as the gallery cards. It isn't a `.tcg-card`, so none of the gallery declutter or the narrow-card container tier ever reached it — and measurement showed its back face **overflowing by 148px and scrolling at a 320px viewport**, violating the owner's no-scroll rule (the never-scroll sweeps had only ever covered `.tcg-card`s). New rules in `assets/portfolio-mobile-fixes.css` scoped `.md\:hidden .lab-poke-section`: stage line + "weakness · brings in too much business" tag hidden, View Live full-width, name 0.8rem, gallery-matching body/badge tightening, and the gallery backs' narrow-tier values applied to its back (which cleared the 148px overflow entirely). Viewport scoping is correct here — this card's width tracks the viewport (`min(360px, 86vw)`, no zigzag), so no container query is needed. **Both desktop copies keep the full design** (stage line, weakness joke, native type). Also fixed in passing: all three copies of the widget's back said `"one call â†’ full estimate"` — mojibake (double-encoded arrow) that had survived the 2026-07-15 sweep; now `&rarr;`. Verified at 320/375 by geometry (screenshots time out on this card — the documented pane limitation with the drop-in's animated hero): stage/tag hidden, art 64→123px (320) / 189px (375), button inside plate, nothing horizontally clipped, back 0/0 overflow both axes, desktop copies untouched and arrow renders, no console errors. Validator: 65 baseline. → `portfolio-mobile-fixes.css?v=20260717-template-mobile` (7 pages).

- **Back-face titles enlarged on every card, both galleries.** Owner request off a MetalsCalc screenshot. New base rule in `assets/collectible-card.css`: `.tcg-card .poke-back-inner h3` at **1.15rem** (up from the widget's 0.95rem — now matches the front name's native size), and the narrow-card container tier's h3 raised 0.78rem→**0.95rem**. The base rule also adds `overflow-wrap: anywhere`, which is load-bearing, not cosmetic: the back h3 markup carries no `<wbr>` (unlike the front `.poke-name`), and a domain is one unbreakable token — at the larger size "NaplesEstateJewelry.co" / "EliteYachtDetailing.com" would overflow the `overflow: auto` back panel **horizontally**, i.e. a scrollbar, which backs must never show. With the guard they wrap to two lines at 320px (".CO" drops to line 2, same break the front makes) and sit on one line at 375px+. Verified both galleries at 320/375/500/1280: vertical AND horizontal overflow 0 on all backs (horizontal now checked explicitly — the never-scroll rule had only been verified vertically), desktop backs fine at 1.15rem, app-catalog's long app names wrap on their natural spaces. Validator: 65 baseline. → `collectible-card.css?v=20260717-back-h3-up`.

- **Mobile card fronts decluttered — stage line and status tag removed, buttons full-width, art gets the space.** Owner walked the gallery card-by-card in mobile view and called the chrome out ("keep only what's needed and completely design each card at this size"). Measured at 375px: the screenshot — the hero — was getting 122–139px of a 409px card while flavour chrome ate the rest; the stage line ("STAGE · X / NO. 00N") duplicates both the zigzag's number chip and the badges; the foot's status tag wrapped to 2 lines on jewelry/auction and **3 lines** on photo ("STATUS · IN PROGRESS"), and "live" is implied by the Live button anyway. Fix in `assets/portfolio-mobile-fixes.css`, `.md\:hidden`-scoped: `display: none` on `.poke-stage-line` + `.poke-foot .poke-tag`, and the two foot buttons become flex:1 full-width tap targets (116px each at 375px, up from ~60–90px). **Hidden via CSS, not deleted from markup, deliberately** — the phone and desktop grids are hand-duplicated identical copies ("edit BOTH or they drift" rule); forking the mobile copy's structure would break every future both-grids edit. Desktop keeps both elements (no number chip there; foot has room). Art grew to 138–154px at 375px.
- **Front-face rules added to the shared narrow-card container tier** (`@container tcg-card (max-width: 336px)` in `assets/collectible-card.css`, previously back-only): body 0.68rem→0.58rem/lh 1.35, tighter body padding/margin, tighter badge row, stage line 0.5rem (matters on app-catalog, which keeps its stage line). Reason: at the zigzag's ~237px cards (320px viewport) the front's fixed-size sections pinned the flex-fill art at its 64px floor; the tier hands back what it can — art now 64–106px at 320px depending on each card's content length (jewelry/yacht/auction stay near floor because 2-line domains and 4-line copy are genuinely that long; nothing clips), and app-catalog's 320px art improved to 93–131px. Verified per card at 320/375 (screenshots + geometry): no button overflow, no clipped labels, all 12 backs still 0px overflow both galleries, desktop grid untouched (stage/tag visible, base type, 340px cards outside the tier). Validator: 65 baseline. → `portfolio-mobile-fixes.css?v=20260717-front-rebuild` (7 pages), `collectible-card.css?v=20260717-front-tier` (2 pages).

- **Back-face paragraph text bumped up, two passes — the first pass was too timid because the wrong metric was trusted.** Owner asked to increase back-of-card text size. First pass raised `p` 0.54rem→0.6rem in the `@container tcg-card (max-width: 336px)` tier and verified `scrollHeight === clientHeight`, same check used throughout the "never scroll" work — but the owner sent a screenshot showing real empty space below the badges on Naples and said the change was barely visible. **The overflow check only proves "doesn't scroll," not "fills the space":** `.poke-links` carries `margin-top: auto` (pokecard.css), which stretches the flex column to fill `clientHeight` regardless of how little text is in it, so `scrollHeight` reads equal to `clientHeight` even with 60px of visible empty room — it can never expose slack, only overflow. Correct metric is `clientHeight − (sum of children's own heights + gaps + padding)`. Measuring that per card at a 320px viewport found Naples had 45px of true slack, AuctionBuddha only 20px — confirming AuctionBuddha, not the card in the screenshot, is the real ceiling. Second pass pushed `p` to 0.68rem (line-height 1.18) and `h3` to 0.78rem, paid for by tightening gap (0.32rem→0.14rem), padding (0.5rem 0.55rem→0.35rem 0.4rem), badges (0.46rem→0.4rem), flip-hint (0.46rem→0.4rem), and button text (0.58rem→0.54rem) — leaving AuctionBuddha ~12px of true slack rather than 0, since a razor-thin margin is exactly how the zigzag's back-scroll regression happened earlier this session. Re-verified 0px overflow (both galleries, full viewport sweep) plus the true-slack numbers on all 6 casestudies cards. → `assets/collectible-card.css?v=20260717-back-text-up2`.

- **Phone gallery cards now shrink and stagger in a numbered zigzag, with a container-query fix for the back-scroll regression it caused.** Owner asked for the `casestudies.html` mobile grid (`.md:hidden` only — desktop and `app-catalog.html` untouched) to shrink each card slightly and stagger it against a number: odd cards (1/3/5) shrink and push right with the number chip on the left, even cards (2/4/6) push left with the chip on the right. Implemented by wrapping each of the 6 cards in a new `.tcg-mobile-row`/`.tcg-mobile-num` pair (`assets/portfolio-mobile-fixes.css`): the row is a flex container that reserves `2.7rem` for the chip via `width: calc(100% - 2.7rem)` on the card, with `justify-content: flex-end`/`flex-start` alternating per row for the stagger; the base `max-width: 340px` is left alone so the shrink only actually engages below a ~423px viewport — wider phones keep the historic full-width card. The chip itself picks up the card's own `--tcg-face` theme colour (the row wrapper carries the same `.tcg-card--<theme>` class).
- **That shrink broke the previous day's "cards never scroll" guarantee** ([[card back faces must never scroll]] entry, 2026-07-16) — caught by re-running the same `scrollHeight === clientHeight` check this session, not assumed fine because the visual change looked front-face-only. Root cause: front and back are two faces of the same fixed-aspect-ratio 3D object, so shrinking the card's width for the chip also shrinks its height, and the back-face copy had been tuned to a **zero-slack** budget the day before — any reduction reopened the scrollbar, badly (up to 155px overflow at a 320px viewport, worse than before the original fix). A `max-width: 300px` cap tried initially also left a smaller but *permanent* ~9px overflow at every wider mobile width, because it undercut the 340px the back-copy budget assumed even where there was no chip-driven shrink to justify it — removed once identified.
- **Fix: retargeted the back-face tightening in `assets/collectible-card.css` from a viewport media query to a container query** (`@container tcg-card (max-width: 336px)`, with `container-type: inline-size` added to `.poke-scene.tcg-card`). The old `<375px` viewport rule was the wrong signal once card width stopped tracking viewport width 1:1; a container query keys off the card's actual rendered width regardless of what shrank it. Widened the tightened values themselves to cover the zigzag's narrowest case (236.8px at a 320px viewport) — first as two tiers, which left a gap (milder tier insufficient just above the aggressive tier's cutoff, up to 105px overflow), then collapsed to one tier sized for the tightest case, since more width only ever adds slack once font-size/line-height are fixed. `app-catalog.html` shares the same CSS but not the zigzag, so it was re-swept to confirm no regression there.
- Verified via live DOM measurement (`scrollHeight` vs `clientHeight`) across both galleries at the full mobile range — 320, 327, 330, 336, 375, 400, 419, 420, 460, 767 viewport widths, spanning every container-query boundary — all six casestudies cards and all six app-catalog cards at **0px overflow everywhere**; flip interaction re-tested (wrapper markup doesn't intercept the `.poke-card` click target); screenshots confirm the stagger reads correctly (odd cards right/chip-left, even cards left/chip-right) at 375px and 320px. Validator: 65 baseline, unchanged. → `assets/portfolio-mobile-fixes.css?v=20260716-mobile-stagger` (7 pages sharing the file), `assets/collectible-card.css?v=20260716-container-fit3` (2 pages).
- **Card title + front-face badges shrunk, mobile gallery only.** Owner feedback: the badge pills ("WEB DESIGN", "E-COMMERCE"...) were "larger than they need to be given the text size within them" — their padding/border had been sized for the base 0.58rem type, not the tighter mobile card, so tightened padding and border alongside the font shrink (`.poke-badge`: 0.58rem/2px 7px/1.5px border → 0.46rem/1px 5px/1px border) rather than just scaling the font down; title (`.poke-name`) 0.9rem → 0.7rem. Same `.md\:hidden` scoping as the pill work (desktop and `app-catalog.html` untouched), and the same specificity trick the width override needed (`.md\:hidden .tcg-card .poke-name` — 3 classes — to out-rank collectible-card.css's `.tcg-card .poke-name`, 2 classes, which loads after this file). **Deliberately scoped to front-face badges only** (`.poke-badges .poke-badge`, not `.back-badges .poke-badge`): back badges already get their size from the back-fit container query's zero-slack scroll-prevention budget, and changing that needs the same full-range re-verification as any back-copy edit — out of scope for a front-face sizing request. Verified: 4 of 6 titles now fit one line even at 320px (was multi-line for most before), no overlap with the "Flip" cue, back faces confirmed still 0px overflow (untouched), flip interaction re-tested. → `assets/portfolio-mobile-fixes.css?v=20260717-title-badges`.
- **Mobile-only estimate pill resized to 70% (owner request, two passes).** First pass shrank the `.tcg-stat-pill` to 50% on the same `.md:hidden` casestudies grid the zigzag touches — legible-enough for a "there's a tag here" read but too small to actually read the number, per owner feedback, so raised to 70%. Same rule (`.md\:hidden .tcg-stat-pill` in `assets/portfolio-mobile-fixes.css`, scoped off desktop and off `app-catalog.html` which doesn't load the file): height 22px → 15.4px, font-size 0.62rem → 0.434rem, padding/border/letter-spacing scaled the same factor, pull-up margin recalculated (not scaled) to hold the pill's 8px floating gap above the art's corner at the new height. Dimensions only, never `transform: scale()` — a transform would promote the pill to its own compositing layer on mobile, the exact mechanism behind the flip-bleed bug the in-flow rule exists to prevent. Verified all 6 pills fit inside their art bounds at a 320px viewport (worst case) after each pass. → `assets/portfolio-mobile-fixes.css?v=20260717-pill-70`.

## 2026-07-16

- **Card back faces must never scroll — copy condensed to fit, everywhere.** The AuctionBuddha back showed a scrollbar (`.poke-back-inner` is `overflow: auto` in the drop-in); measurement widened the problem usefully: at standard card widths only AB overflowed (8px), but at the narrowest supported viewport (320px → 280px card, where the fixed 2.5/3.5 ratio costs the plate ~60px) **all six casestudies backs overflowed** (AB by 147px). Two-layer fix: condensed back copy (AB cut hardest, twice — its desc and estimate lines are now tighter; Naples/JPS/SeansAds/MetalsCalc each trimmed ~a line; Elite untouched) and a `<375px` media block in `assets/collectible-card.css` that slightly tightens back-face type/gaps (p 0.72→0.66rem, gap 0.7→0.55rem, smaller badges) so narrow phones don't force gutting the copy at all widths. **Deliberately did NOT set `overflow: hidden`:** that would change the failure mode from a visible scrollbar to a silently clipped Case Study button — the guarantee is that copy FITS, and the CSS comment + this rule require re-verifying every back at a 320px viewport (`scrollHeight === clientHeight`) after any back-copy edit. Verified: all 12 casestudies backs (both grids share strings) and all 6 app-catalog backs at 320, 375 (normal type, 335px card), and 1440 — zero overflow everywhere, desktop type untouched (11.52px), fronts unaffected. → `v=20260716-back-fit` (2 pages). Validator: 65 baseline.

- **Naples estimate raised: ~95–130 → ~120–160 hours (owner call, above the rate-derived figure; implies ~$15K–$20K at $125/hr, still inside the Online Store package anchor's "larger stores $7,500–$15,000+ before custom features").** Updated in step: both gallery pills + both card backs in `casestudies.html`, the detail-page headline, and — for internal coherence — the storefront line item (60–120 → 70–125 hrs, so the items still bracket the headline: sum 106–161 around 120–160). Docs synced; Naples is now the one estimate not strictly derived from the old dollar range, noted as such in CURRENT_STATUS/HANDOFF.

- **Portfolio pricing labels converted from dollars to build-time hours, with a published shop rate.** Owner decisions: single published rate of **$125/hr** (added to `website_pricing_plan.txt` as §37 so spec and site can't drift — the $95–$150/hr range stays for proposal-scoped hourly work), full sweep, detail pages hours-led but keeping their package dollar anchors. Hours were derived from the existing package-based dollar ranges at $125/hr so hours × rate reconciles with the pricing page: Naples ~95–130, Elite ~70–80, JPSurette ~35–50, SeansAds ~60–80, AuctionBuddha ~280–440+, MetalsCalc ~15–20 hrs. Changed: 12 gallery pills ("EST 95-130 HRS" — ASCII hyphen on purpose, VCR-font en-dash glyph coverage is untrusted; body-font surfaces keep the en-dash), 12 card backs ("~95–130 hours at our $125/hr shop rate"), a rate line above each gallery grid (both layout copies), all six `#build-pricing` sections (eyebrow → "Estimated build time", headline → hours, line items → hours, intro cites the rate), and `portfolio.html`'s folder chip ("Est. 15–440 hrs"). Every disclosure now also carries **"hours × rate is not a quote"** — packages remain fixed-price per the spec, and displaying a rate next to hours invites exactly that multiplication. Dollar strings surviving on the six detail pages were verified to be exactly the intended package anchors and nothing else.
- Verified: all 12 pills fit their art on both grids at 375 and 1440 (widest "EST 280-440 HRS" = 120px in ~281px art), plates fit, rate lines visible in both layouts, AuctionBuddha's longest nowrap headline ("~280–440+ hours") fits at 375px, all six detail pages assert hours + rate + no-quote guard + one dollar anchor, no overflow. Validator: 65 baseline. Docs re-derived (DECISIONS amended, CURRENT_STATUS/HANDOFF/TASKS synced, including fixing HANDOFF's now-useless "grep $35K" guidance).

- **Portfolio hub's two category cards rebuilt as "collection folders."** Owner asked for the trading-card treatment "but not an exact copy, and no flip." The metaphor does the differentiating: the galleries are card sets, so the hub cards are the **folders that hold them** — a file-folder tab on top (`SET 01 · SOFTWARE` / `SET 02 · WEBSITES`), the same visual language as the cards (paper plate `#f3f1ea`, 2px ink `#14101f` lines, 150° hue-gradient frame, hard offset shadow `10px 14px 0`, VCR/mono type) but landscape, with a **fanned stack** of two hue-tinted cards peeking out behind the screenshot to say "cards inside" (fans wider on hover). Hues quote each gallery's palette: teal from the apps set, gold from the websites set. Chips carry the set facts (`6-card set`, `Live demos` / `Est. $2K–$55K`); CTA is a poke-btn-style "Open the set". **No flip, no tilt, no JS** — plain `<a>` links, pure CSS, all hover motion inside a `prefers-reduced-motion: no-preference` query. `vcr-osd-mono` is @font-faced directly from `pokecard-dropin/pokecard/fonts/` (3.5KB) rather than loading the whole widget CSS. `position:absolute` on the peeks is fine here — no 3D/flip context on this page; the in-flow rule is about the gallery cards' faces. Old `.portfolio-hub-card/__media/__body/-chip/-arrow` CSS deleted. Flex-column card → equal frame heights across the row (342px both, CTA bottoms aligned; they differed 342/314 before the fix).
- **Deleted `casestudies.html`'s dead referrer back-link script.** Found while building the apps.html reveal; investigated before touching it, and "retarget vs delete" resolved decisively to delete — it was dead **three ways over**: it tested for `services/website-design.html` (retired into `website-design-hosting.html` in the 2026-07-15 merge), its `[data-website-design-back]` element **existed nowhere in the markup** (the earlier `grep -c` hit of 1 was the script's own `querySelectorAll` line, not an element), and the surviving pricing page **doesn't link to casestudies at all**, so there is no journey a back link could serve. Retargeting would only have made dead code point somewhere new; rebuilding the feature would have been unrequested scope. Script removed; `apps.html`'s comment updated so it no longer cites the deleted precedent. Verified after: casestudies renders 6 cards + 6 pills, flip still wired, logo init (which sat next to the removed block) still runs, footer present, no console errors, no dangling references anywhere. Validator: 65 baseline. `apps.html` is now the site's only referrer-reveal.
- Verification note: a probe reported `html` computed `opacity: 0` on casestudies after the deletion, which would have meant an invisible page — a **control test on untouched `index.html` showed the identical reading**, proving it a preview-pane artifact (same family as its dead screenshots and throttled timers), not a regression. The inline `opacity: 1` from `surette-logo.js` is present and both pages genuinely paint. Don't trust this pane's computed `opacity` on `html`.
- **"Back to Portfolio" link on `apps.html`, shown only when the reader came from the Portfolio hub.** Conditional rather than permanent on purpose: `apps.html` is primarily a *Services* destination (its nav is `data-active="services"`) and is linked from the Services dropdown, `index.html` (×2), the footer, and `app-catalog.html`'s own back link — a persistent "Back to Portfolio" would send most visitors somewhere they'd never been. Referrer-based rather than the `?from=` param `app-pricing.html` uses: both pages are same-origin, where the default referrer policy sends the full URL, so it's reliable without hanging a query string on a canonical marketing URL. Markup/placement/styling mirror `app-catalog.html`'s "Back to Apps" (`hidden` div before `<main>`, revealed by removing the class). Verified by driving real same-origin navigations: from `portfolio.html` the link appears top-left (x=64/y=112 desktop, x=20/y=128 phone), clears the fixed nav (101px desktop / 64px mobile header) and sits above the hero; from `index.html` and on a direct visit it correctly stays hidden; no overflow, script parses clean, validator 65 baseline.
- Two verification traps worth recording: `location.href='apps.html'` (no cache-buster) served a **stale cached copy** and reported the new element missing — always cache-bust the destination when testing a referrer flow; and a probe regex testing `document.referrer` **whole-URL** failed against `portfolio.html?v=5` while the page script (correctly testing `.pathname`) matched — mirror the page's own test, not an approximation of it.
- **Apps folder card no longer implies it opens straight into the gallery.** `apps.html` is the software hub (wordmark landing); the six-card gallery is `app-catalog.html`, one step inside — but the card said "Open the set" with a "6-card set" chip, promising the gallery directly. Copy now reflects the real path (owner chose honest copy over retargeting the link): desc ends "Opens the software hub first — the six-card app gallery sits one step inside", chips are `Software hub / Gallery inside / Live demos`, CTA is **"Open the hub"**. The Websites folder keeps "Open the set" — `casestudies.html` genuinely is the gallery. Verified at 414/960/1440: new copy renders, plates fit, frames still equal (364px both at 1440 — the longer desc added a line to each via the flex stretch), no overflow. Validator: 65 baseline.
- **Folder cards: fixed the tablet/mobile portrait-sliver screenshots.** At ~880–1200px the two folders still shared a row with two-column plates, so the long text column dragged the row tall and the art — which filled its column — stretched into a vertical sliver (329×~600 at 960px). Two changes in `portfolio.html`: the art is now a **capped square stack** (`aspect-ratio: 1/1`, `max-height: 20rem`, centred) so no text column can ever stretch it — the cap deliberately breaks the ratio toward landscape, never portrait; and a new **880–1199.98px band shows one full-width folder per row** with the horizontal plate intact (1200 is a component reflow point like client-portal's 860 — the site's 880 line is untouched). Swept 1440/1250 (2-up, square 252/211px art), 1024/960 (1-up, 354×320 / 329×320), 860 (stacked, 741×320 letterbox via the cap), 414 (stacked 16/10, 310×194): art is landscape-or-square at every width, plates fit, no overflow. Validator: 65 baseline.
- **Fixed the hub's stale stats row: "5 Apps" → 6** (MetalsPredictor had been added without the count). Verified: both folders render at 1440 (2-up, equal heights, plates fit) and 414 (stacked, 16/10 art, no overflow), VCR font loads, tabs seat on frames (±3px), images load, links correct, old classes absent from the DOM, no console errors. Validator: 65 baseline. (Preview pane stopped returning screenshots entirely this session — verification is computed-style/geometry.)

- **The stat pill now rides the card through the flip — the original design, done by making the pill IN-FLOW.** Owner rejected the settle-then-fade behaviour below ("stay on the card the whole time"), and the whole device saga pointed at the actual rule: everything **in-flow** on the face (title, badges, buttons, art) behaved perfectly across all three device tests; the pill was the only element that was `position:absolute` + `z-index` (later + `will-change`), i.e. the only one inviting layer promotion, and the only one that bled/flickered. So the pill is now the same kind of object as a `.poke-badge`: static, un-promoted, rasterised into the face itself — it rotates with the card, is culled with the face at 90°, and needs **no state juggling, no timing gates** (the entire `data-flip` opacity mechanism is deleted). Placement without `absolute`: the pill sits in a zero-height flex row (`.tcg-stat-row`) after the art, pulled up over the image by negative margin (`-30px`, right inset `calc(0.7rem + 8px)`) — contributes nothing to layout (all plates re-verified fitting), lands at the same 8px/8px art-corner insets as before. Markup restructured on all 18 cards (12 casestudies, 6 app-catalog).
- **One real bug caught by hit-testing during this change:** the drop-in makes `.poke-art` `position: relative` (it anchors the template card's `.art-ph` placeholder), and a positioned box paints above ALL in-flow content in its stacking context — the image was drawing **over** the in-flow pill, tree order notwithstanding. Fixed with `.tcg-card .poke-art { position: static }` (our cards have no placeholder; the hero template card keeps its `relative`). All 18 pills then verified painting above their images via `elementFromPoint` probes — rows out of viewport reach probed by temporarily relocating the card (internal paint order travels with it; this pane's scrolling was uncooperative — note `app-catalog` scrolls on **body** (`overflow: hidden auto`), not `documentElement`, so drive `document.body.scrollTop` there). Desktop flip re-verified: pill gone with the face when flipped, back at rest.
- **The rule for face overlays, final form (supersedes the state-hide guidance):** keep them in-flow. No `position:absolute`/`fixed`, no `z-index`, no `transform`, no `will-change`, no opacity transitions — each invites the mobile compositor to promote the overlay out of the face's flattening, which is the root of both the mirrored bleed-through and the mid-flip flicker. If it must overlap something, do it with layout (zero-height row + negative margin) and tree-order painting — and check nothing positioned sits earlier in the same stacking context. → `v=20260716-pill-inflow` (2 pages). Validator: 65 baseline. **Confirmed on the owner's device: the pill stays on the card through the flip — no bleed, no flicker.** This closes the card-overlay saga; the in-flow rule above is the durable takeaway.

- **Pill flicker, final round: the pill now waits out the whole flip, on a permanent layer.** The fade-from-70ms fix below reduced the device flicker from several to one flick — which narrowed the cause: every artifact happened while the pill was visible *during rotation* on the compositing layer that device promotes it to (the same promotion behind the original bleed). Two-part fix in `assets/collectible-card.css`: reappearance delay raised past the end of the flip (`transition: opacity 0.18s ease-out 0.52s` vs the 0.5s flip — the settled card is the only state the device provably renders the pill correctly in, so the price lands just after the card does), and `will-change: opacity` pins the pill to a **persistent** compositing layer — browsers promote for the duration of an opacity transition and demote when it ends, and that demotion repaint is the other classic single-flick source; a permanent layer has no promote/demote boundary to glitch on. Safe against the original bleed by construction (opacity is zero for the whole flipped state; a promoted-but-invisible layer paints nothing). **The 0.52s delay must stay ≥ the flip duration in `pokecard.css`.** Verified by sampling: zero through the entire 500ms flip (first movement 568ms), monotonic rise to full by ~740ms, instant hide on flip, both galleries. → `v=20260716-pill-settle`. Validator: 65 baseline. **Confirmed on the owner's device**: no flicker, no bleed, and the post-settle fade-in reads as quick rather than laggy — the trade-off held.

- **Pill flicker on flip-back — my reappearance gate was derived from bad arithmetic.** Confirmed fixed on device, but the pill popped in oddly on the return. The flip is 0.5s of `cubic-bezier(0.22, 1, 0.36, 1)`, a hard ease-out that is **96.7% complete at the halfway point in time**, so the card crosses 90° (front becomes viewer-facing) at **~65ms**, not the ~250ms I had assumed — my code comment claimed "~12° left at 0.26s" with the geometry backwards; it is ~6° left. The 260ms gate therefore left the pill missing for a **194ms dead window** after the face had already arrived, then snapped it on in 10ms against a still-settling card. Gate is now `transition: opacity 0.16s ease-out 0.07s` — it starts fading ~5ms after the 90° crossing, so the pill simply arrives with the face. The crossing point is computed in the comment; **changing the flip duration or easing in `pokecard.css` invalidates it**.
- **Dropped `translateZ(0)` + `backface-visibility` from the pill — they were inert and actively harmful.** Once the state hide exists the pill is only ever opaque while the front already faces the viewer, so backface culling can never fire; meanwhile `translateZ(0)` *forces* the exact layer promotion that caused the original bleed, and parks a culling boundary right beside the new fade — a plausible flicker source in its own right. Removing them leaves the state rule as the single, sufficient mechanism (verified: `transform: none`, `backface-visibility: visible`, no bleed). Bleed safety is unchanged by construction: `opacity: 0` applies instantly on flip and holds until 70ms into the flip-back, which is past the crossing — there is no instant where the pill is non-zero while its backface faces the viewer.
- Verified by sampling the real opacity curve through a flip-back (`0ms:0 → 76ms:0 → 113ms:0 → 153ms:0.43 → 231ms:0.88 → 270ms:1`), asserted **monotonic** — a flicker would show a rise-then-fall. Instant hide on flip, zero for the whole flipped state, zero at the start of flip-back, full afterwards; passes on the phone grid, desktop grid, and app-catalog. `assets/collectible-card.css` → `v=20260716-pill-fade-in`. Validator: 65 baseline. (Sampled values read ~40ms late — this preview pane throttles timers hard, same family as its frozen rAF.)

- **Pill bleed-through, round two: the backface-visibility fix did NOT hold on real devices — replaced with a state-driven hide.** The property-only fix below failed in device testing, and the reason is structural: `backface-visibility` on a transform-less descendant of a *flattened* face is evaluated against an identity transform when the mobile compositor promotes it, so it is simply ignored. (The widget's `.poke-glare` survives because it is a **direct child of the preserve-3d card with its own translateZ** — a first-class 3D participant; the pill is buried three levels deep in a flattened subtree.) The guarantee is now **state, not compositing**: `pokecard.js` maintains `data-flip` on the scene, and `.poke-scene[data-flip="1"] .tcg-stat-pill { opacity: 0; transition: none }` hides the pill the instant a card flips — no GPU quirk can override it. Reappearance is gated by `transition: opacity 0.01s linear 0.26s` on the base rule, parking the pill until the flip-back has swung the front past 90° (~12° left at 0.26s of the 0.5s ease-out) so it can't transiently bleed on the return either. **The gate is opacity, not visibility, deliberately** — a first attempt used a delayed visibility transition and Chrome treats a pending visibility show-transition as visible immediately, so the delay never held; an interpolable property reliably keeps its start value through its delay. `translateZ(0)` + `backface-visibility: hidden` stay as a second layer (they make the pill a true 3D participant like the glare, hiding it at exactly 90° mid-animation on compositors that honour it). Overlays inside a face need BOTH treatments. `assets/collectible-card.css` → `v=20260716-pill-flip-hide2` (both galleries).
- Verification note that cost two false test runs: the pane had silently reset to desktop width, so the timing assertions ran against the **hidden phone copy** of the card — and in a `display:none` subtree transitions don't run at all (properties jump instantly) and Chromium reports `transform: none`, which mimicked both "the delay doesn't work" and "translateZ didn't apply". Re-run against the *rendered* card: full pass on the desktop grid at 1533px, the phone grid at 414px, and app-catalog — hide is instant, the gate holds at 0ms/150ms after unflip, pill returns by 450ms, `translateZ` computes as a real matrix, resting state restores. When asserting on this page, always select the card copy with a non-zero rect first.

- **Fixed the estimate pill bleeding through the back of a flipped card, mirrored — on real phones/tablets only.** Reported from a device; desktop never showed it. Cause: `.poke-card` is `transform-style: preserve-3d` and `.poke-face` is `transform-style: flat`, so on desktop the pill is flattened into the face's own rasterisation and hides with it. Mobile GPUs promote an absolutely-positioned, z-indexed element to its own compositing layer — which escapes that flattening, joins the card's 3D context, and (at the default `backface-visibility: visible`) paints its backface through the flipped card. Audited every descendant of the front face: `.tcg-stat-pill` was the **only** one that promotes, which is exactly why only the price label bled and nothing else did. Fixed with `backface-visibility: hidden` (+ `-webkit-`) on the pill in `assets/collectible-card.css` → `v=20260716-pill-backface` (2 pages). **This is not a dev-server artifact — it would have shipped**; the server sends identical CSS, only the browser differs.
- The widget had already solved this for itself and it doubles as the proof: `.poke-glare` is the same shape of element (absolute, z-indexed, in the 3D context) and carries an explicit `backface-visibility: hidden` with the comment "only reveal the glare on the side currently facing the viewer". The glare does **not** bleed through on the reporter's device, which is direct evidence that the property is honoured there — so the pill, now matching it, should behave the same. **Any future overlay added inside a card face needs this property**; noted in the file's comment.
- Verified: all 12 pills report `backface-visibility: hidden`, pill text and rendering intact on the front (6/6 phone pills at 86px), flip still toggles `data-flip`/`inert` correctly, no overflow, no console errors, validator unchanged at 65. Caveat: the bug itself cannot be reproduced on this machine (desktop Chrome flattens correctly) and the preview pane stops screenshotting once the forced-3D simulation runs, so the desktop repro was confirmed by computed style rather than a picture — **needs a re-test on a real device**.

- **Wide footer's legal block is now three short lines instead of two long ones.** "Southwest Florida" used to ride the copyright line behind a "·", making that line 282px — by far the widest thing in the column. On its own line at the bottom, the block reads `© 2026 Surette Data Systems` / `Call or text (239) 404-8505` / `Southwest Florida`, and the column is only as wide as the phone line: **282px → 162px, 43% narrower**, which also hands ~120px back to the brand column. The separator between the copyright and the location is gone with it. Wide footer only — the phone footer's legal is a single centred paragraph in a different format and carries no phone number, so it was left alone. `standard-site-footer.js` → `v=20260716-footer-3line` (27 pages).
- **Footer legal/meta text is phrase-atomic again; the phone stopped breaking.** The shared footer's legal block was two plain strings, so it broke at any space — "Call or text (239) 404-8505" was splitting the number as "404-" / "8505". **Not a narrow-viewport problem: it happened at 1440px**, because `.dm-footer__inner`'s `space-between` row let the 9-link column eat the width and shrank the legal column to **159px**, against the 282px its two lines need. Two fixes: the legal is built from atomic phrases (`phrases()` helper in `standard-site-footer.js`, `white-space: nowrap` per phrase in `nav.css`) so a tight column wraps *between* phrases and never inside one; and `.dm-footer__legal` gets `flex-shrink: 0` at ≥1024px so the link list — which already wraps — gives up the width instead. The phone is a real `tel:+12394048505` link again. Both were lost in the 2026-07-16 footer consolidation: the old hand-authored footers had exactly this treatment (`.footer-meta` + a tel link), and the shared replacement didn't carry it over — the now-orphaned `.footer-meta` rules are still sitting in `nav.css` protecting a footer that no longer exists.
- **Fixed the brand tagline the same way — a wrap I caused.** Giving the legal column its 282px took the brand column from 248px to 220px, pushing the tagline from 2 lines to 3 and stranding "FLORIDA" from "SOUTHWEST". It was still a plain string, so it got the same `phrases()` treatment: still 3 lines at 1440px, but now breaking as "SOFTWARE · WEBSITES" / "BUSINESS TECH" / "SOUTHWEST FLORIDA". The phone footer's centred legal line got it too — centred text is where a stray word is most obvious ("Business" / "Tech").
- Swept 320 / 414 / 900 / 1024 / 1440 / 1920: **no phrase splits anywhere in either footer variant at any width** (21 phrases asserted per pass), the phone is always one line, the legal is 2 clean lines from 1024 up, nothing overflows, and the phone footer clears the bottom tab bar by 48px. Deliberately did **not** add a phone number to the phone footer — it mirrors the homepage's, which carries none, and that would be a content change nobody asked for. `standard-site-footer.js` + `nav.css` → `v=20260716-footer-meta2` (33 pages). Validator: same 65-issue baseline.

- **Darkened the three fixed nav bars — they had become unreadable over the new cards.** The desktop header, phone header, and phone bottom tab bar were `bg-surface/10`, i.e. a **10%** dark tint over a blur. That was fine while everything scrolling under them was dark; both galleries now pass bright pastel cards and near-white screenshots underneath, and the bars washed out. Measured, not eyeballed: the tab-bar labels scored **1.17:1** against a light card and the active cyan item **1.09:1** — invisible, exactly as reported. Now `rgba(8, 10, 14, 0.9)` in `nav.css`, which brings the same labels to ~4.25:1 and the active item to ~11:1 while keeping the blur and a little colour bleed. **Going this dark is free:** the tint sits within a few points of the page background, so over normal dark pages every alpha from 0.1 to 1.0 measures an identical 4.68:1 — the change only bites where light content is behind, and the homepage is pixel-unchanged (confirmed). Included the desktop header deliberately: it is `fixed` too, so the same cards scroll under it and it had the same defect.
- **Moved the bar tint off Tailwind and into `nav.css`, per DECISIONS.** `bg-surface/10` lived in injected nav markup, and **no page contains that class in its static HTML** — it was surviving purely on the CDN's DOM observer, the same bet that previously rendered a footer's legal line white and left `portfolio.html`'s hamburger invisible. The tint is now plain CSS keyed to `.dm-standard-desktop-nav` / `.dm-unified-mobile-header` / `.dm-standard-bottom-nav`, and the utility was removed from the generator so there is one source of truth. The bottom bar had no hook class at all, so it gained `dm-standard-bottom-nav` — same reasoning that produced `dm-standard-mobile-shell`. `nav.css` → `v=20260716-bar-tint` (33 pages), `standard-site-nav.js` → same (28 pages).
- Known limitation, left alone: the inactive tab labels are `rgba(196,199,199,0.6)`, so they composite *onto* the bar — as the bar darkens the label darkens with it, capping it at **4.69:1 even on a pure black bar**. That is a pre-existing design choice (dim inactive items), not something the tint can fix; raising the label's own opacity is the only route to comfortable AA there. Flagged in TASKS rather than changed unasked.

- **The Apps gallery is collectible cards too, in its own palette — and the card became a shared component.** `app-catalog.html`'s 6 app tiles are now themed cards matching the Websites treatment. Because a second page needed it, the card CSS moved out of `casestudies.html`'s inline `<style>` into **`assets/collectible-card.css`** rather than being pasted a second time — this repo's recurring failure is exactly that kind of copy (the nav once had ~16). Prefix renamed **`cs-` → `tcg-`** (trading-card gallery): "cs" reads as *case study*, which would be wrong on the Apps page. `.cs-price-pill` → `.tcg-stat-pill` for the same reason — it holds a build estimate on Websites but a delivery type (LIVE DEMO / PWA / WEB APP) on Apps. Structure unchanged; casestudies re-verified on the shared sheet (12 cards, 6 gradients, 6 pill hues).
- **Apps palette is deliberately separate:** coral / lime / green / teal / periwinkle / magenta (`.tcg-card--app-*`), sitting in the hue gaps the Websites set leaves (~20/75/120/172/248/315 vs ~45/190/215/280/350), with every pair inside the Apps set ≥45° apart. Faces must stay light — the plate is paper and the ink near-black.
- **app-catalog needed no duplication:** unlike casestudies it is a single responsive layout, so one grid serves every width — `grid-cols-1 md:grid-cols-2 xl:grid-cols-3` (1-up on phone for the same 2.5/3.5-ratio reason). Removed its tile CSS including the six `nth-child` per-tile background colours, now that each card carries its own hue; what survives is only what the "Let's build yours today" open slot needs. Its cards carry **one** foot button (Open Profile) rather than the Websites cards' two, because an app has one destination — the live demos are linked from the profile pages, and inventing demo URLs for the four that don't document one would have been worse than the asymmetry.
- **Docs said five apps; there are six.** `metalspredictor-gold-silver-analyzer.html` (MetalsPredictor — Gold & Silver Trend Analyzer) had been added without CURRENT_STATUS/HANDOFF being updated. Both corrected, and it is card No. 006 (magenta).
- Verified at 1440px and 414px: 6 cards, 6 distinct face hues, all gradients applied, all art images loaded, names clear the flip cue, pills seated in the art, every plate fits, back faces `inert` (proving `pokecard.js` wired them), art spread 13px, grid resolves to 1 column on phone with every card inside the viewport, no overflow, open slot 340×476. Validator: same 65-issue baseline, and every new profile href resolves.

- **The phone gallery is collectible cards too — the desktop/phone split is closed.** `casestudies.html`'s `md:hidden` grid dropped its seven `.portfolio-tile`s for the same six themed cards plus the open slot. **One-up, not two-up, and that was measured rather than guessed:** the card is a fixed 2.5/3.5 ratio, so a 2-up phone column (162px at 375px) yields a 226px card, while the anatomy needs ~312px even with the art squeezed to its 64px floor — it could not have fitted. One-up gives a 335×469 card, which also happens to match the widget's own native phone sizing (`min(360px, 86vw)`) and needed no new CSS: the existing `.poke-scene.cs-card` rule (`width:100%; max-width:340px`) already resolves correctly. No JS either — `pokecard.js` picked up all 15 `.poke-scene`s on the page, and tap-to-flip is its documented no-hover behaviour, so phones get flip without tilt for free. Back-face hints say "Tap to flip back" and aria-labels say "Tap" rather than "Click".
- **Removed the tile CSS the conversion orphaned.** The phone grid was the last consumer of `.portfolio-tile__media/__img/__badge/__price` and of the whole phone tile-shrink block (badge/price/media sizing, a forced `0.75rem` body and `9px` labels) — all now zero-usage, verified against both markup and computed styles. What stays is only what the "Your Project Here" open slot still needs: the surface, the hover, `__cta`, and `--open`. Side benefit: with the forced shrink gone the open slot renders at its authored 13px/11px instead of being overridden down to 12px/9px, which suits a full-width card. **Deliberately not chased into `assets/portfolio-mobile-fixes.css` or `app-catalog.html`** — that page keeps its own copy of the tile CSS and still renders real tiles.
- Trade-off recorded: the six cards are now hand-duplicated across the two layouts (~330 lines). This page has always kept two parallel layouts — its hero, CTA, footer and template card are all already duplicated — and static HTML was kept over JS-rendering them because the card copy is the page's actual SEO content. They will drift if only one copy is edited; flagged in TASKS and in an HTML comment above the phone grid.
- Verified at 375px and 414px: 6 phone cards + 6 desktop cards, 6 distinct themes, every plate fits, titles single-line and clear of the flip cue, pills seated in the art, both buttons inside every foot, back faces `inert` at rest, tap flips and swaps `inert` while inner links still navigate, both open slots render dashed at 335×469, no horizontal overflow, no console errors. Validator: same 65-issue baseline. (Note: with 15 `.poke-scene`s the preview pane's screenshots get flaky — at 375px it returned a DPR-2 crop that *looked* like a blown-up layout; geometry proved the layout correct — `scrollWidth` 375, card at x=20–355 — and 414px rendered clean.)

- **The table scroll hint now repeats instead of firing once** (`assets/table-scroll-hint.js` → `v=20260716-table-hint-repeat` on its 3 pages). A single nudge on scroll-into-view is easy to miss if the reader happens to be looking elsewhere on the page, so it now nudges every ~4s of stillness (~5s cycle including the ~1.05s animation) for as long as the table is in view and the reader hasn't engaged. Turning a one-shot into a loop moved all the weight onto the stop conditions, so those got the attention: engagement listeners are now armed for the whole cycle rather than only during an animation (previously interaction during the quiet gap wouldn't have registered); leaving the viewport **pauses and rewinds** rather than nagging an offscreen table — the rewind matters because a nudge aborted mid-travel would otherwise leave `scrollLeft > 0` and be misread on return as the reader having scrolled it; and because a loop can't ignore what a one-shot could, it now also listens for `prefers-reduced-motion` switching on mid-session (stops for good) and for crossing the 1279.98px desktop line (parks, and un-parks on the way back — without this the cycle would die at desktop and never resume). Unchanged: user-driven events only (`pointerdown`/`touchstart`/`wheel`/`keydown`) since our own animation writes `scrollLeft` and would race a `scroll` listener; a table the reader already scrolled is still never touched; still generic (walks up from every `<table>`, no opt-in); still silent with no real overflow.
- **Capped at `MAX_NUDGES = 6`** (~30s of intermittent motion), which also settles the WCAG 2.2.2 (Pause/Stop/Hide) exposure that unbounded motion beside readable text would have carried: six is well past "they missed the first one", and someone who has ignored that many has seen it and chosen not to scroll. Counted per page load, not per time in view. The cap is checked at the top of `run()` rather than only where the next nudge is queued — a table that goes offscreen mid-cycle re-enters through `evaluate()`, which would otherwise walk straight past a spent budget (verified: burn 3 → hide → show → still stops at exactly 6). On reaching it the entry retires through the same `stop()` path as engagement, releasing listeners and unobserving rather than idling observed-but-silent. `engaged` renamed to `done` since retiring now means "reader took over **or** we've said our piece".
- Verified by re-running the shipped file in a sandbox with a virtual clock and stubbed IntersectionObserver/ResizeObserver/matchMedia (the preview pane freezes rAF and IO, so this behaviour can't be watched — same approach the original was validated with). Traced: silent while offscreen; first nudge peaks at 52px and rests back at 0; repeats while ignored; **stops at exactly 6 and is still 6 two minutes later, with observers and listeners released**; the cap survives a hide/show mid-cycle; engagement before the cap still wins; interaction stops it permanently; offscreen pauses and rewinds to 0, returning resumes; a table pre-scrolled to 120px is never touched (one distinct scroll position across 20s); reduced-motion stops it mid-session and never runs when set at load; desktop parks it and tablet un-parks it; no overflow stays silent. (One initial red was a bug in the harness, not the file — the nudge counter read a pre-scrolled 120px start as a nudge.)

- **Gallery cards: live link on the front, plus a flip affordance.** Each desktop card's foot now carries two buttons — the live site immediately left of Case Study (`Live` on five; **`Preview` on JPSurette**, since its link is an in-progress Netlify preview, not a live site, matching its "in progress" status and the detail page's wording). The foot has ~286px, so the second button squeezes the status tag onto two lines on three cards, but the foot height is button-driven so the art windows didn't move.
- **Swapped the estimate and the flip cue.** The estimate left the widget's HP slot in the top bar and became `.cs-price-pill` — a pill on the art's bottom-right corner (the placement the old `.portfolio-tile__price` used), in the card's own hue, `pointer-events: none` so it can't swallow the flip click, and deliberately **not** `aria-hidden` since it is real information. The top-right slot now holds `.cs-flip-top`: the word "Flip" plus a small `autorenew` arrow, `aria-hidden` because the card's aria-label already says it flips. Worth knowing for future edits: **this incidentally fixed the two-line titles.** "Flip ⟳" is far narrower than `EST $12.5K`, so the name now has ~238px instead of ~197px and every domain fits on one line — Naples and Elite had been wrapping at their `<wbr>`. The 0.9rem name size and the `<wbr>`s are kept as guards rather than reverted, since they cost nothing and a longer domain would need them again. Verified: flip cue and icon render inside the plate on all six, no name/cue overlap, pills inside the art and click-through, all plates fit, art spread 16px, no overflow.

- **AuctionBuddha repriced $15K–$25K+ → $35,000–$55,000+ and rewritten to describe what it actually is.** Owner context: it is effectively a **localized eBay for a single vendor mall** — a complete drop-in commerce ecosystem, not a marketplace website. Any vendor in the antique-vendor-mall model opens and runs their own mini storefront inside the main site; each can fire an immediate timed auction on their own lots *or* schedule those lots into the store's end-of-month house auction (two workflows, one engine); the admin center is a full back office with messaging carried through the storefronts; and the platform does label printing and integrates with and controls in-store operations, so the online catalog and the shop floor run off the same records. The public-facing side materially undersold all of this. Updated in five places: the desktop card's EST stat and copy (attack line now "Localized eBay", flavour "status · live ecosystem"), its back face, the phone tile's pill and description, and `portfolio-auctionbuddha.html`'s hero subtitle, intro, and `#build-pricing` (headline figure, a rewritten intro, and five line items — multi-vendor storefronts / dual-path auction engine / admin ecosystem + messaging / in-store ops + label printing / public marketplace — with the closest-package card restated). Also added an **"Under the hood"** section to the detail page: six cards covering the ecosystem, placed between the public screenshots and the price, since the captures can only ever show the public tip. Package mapping is unchanged (Custom Software & AI Systems, from $10,000, milestone-based) — this is a multiple of that entry point, not a different tier.
- Trimmed the AuctionBuddha card's front badges back to three after measuring: a fourth ("In-Store Ops") pushed the badge list to a second row and cost the art window 40px (151px vs 181–193px on the sibling cards), which would have made that card's screenshot read visibly smaller in the row. The in-store point is carried by the body copy and the back face instead; art spread across all six is now 17px. (Measurement note: an early reading flagged a name/stat overlap on JPSurette that turned out to be a false positive — it ran before the bundled VCR/IBM Plex webfonts swapped in, so the text was transiently wider. Gating the check on `document.fonts.ready` is what made it reliable; the card actually has 91.9px of clearance.)

- **Desktop Websites gallery is now six themed collectible cards.** Each entry in `casestudies.html`'s `.hidden md:block` grid is a copy of the hero's Tech Business Site Template card (`pokecard-dropin`), one hue per project: Naples gold `#ffd76a`, Elite aqua `#7de8ff`, JPSurette rose `#ff9aa8`, SeansAds blue `#9dbcff`, AuctionBuddha violet `#cba6ff`, MetalsCalc steel `#c3cede` (steel and the deliberately-skipped green keep every card distinct from the green template card above them). **Themed from the page, not by editing the drop-in:** `.poke-face` already exposes `--card-face`, but the widget also hardcodes green in three spots the vars don't cover (the 150deg face gradient's light/deep stops, and the `.poke-top` / `.poke-foot` tint washes), so `casestudies.html` re-points those via `--cs-face/--cs-light/--cs-deep/--cs-tint` on `.poke-scene.cs-card`. The drop-in stays reusable and the hero card stays green (asserted in-browser). No new JS — `pokecard.js` already wires every `.poke-scene` it finds, so the six new cards got tilt, glare, flip, and `inert` face-swapping for free. Card anatomy carries the old tile's information rather than dropping it: status → the flavour line, build estimate → the HP stat, category chips → type badges, tile number → `No. 00X`, and both faces carry the case-study link (the back adds the live site). The "Your Project Here" slot keeps its dashed treatment but is sized to the card silhouette (340×476) so it reads as an empty sleeve. **Desktop only, as requested — the `md:hidden` phone grid still renders the seven `.portfolio-tile` cards untouched**, so the two layouts intentionally diverge for now.
- **Fixed the card title clipping its own stat.** The widget's title wraps on spaces ("Tech Business Site Template"); a domain is one unbreakable token, so at the inherited 1.15rem `NaplesEstateJewelry.co` widened the top row and shoved the EST stat clean out of the `overflow:hidden` plate — measured 348px of content into 286px of space, clipping the stat by 62.5px (Elite by 64.4px). Three parts, because the first two weren't enough: `flex-shrink: 0` on `.poke-hp` so the stat is never squeezed; `<wbr/>` before each TLD so a long domain breaks at the dot instead of mid-word; and `font-size: 0.9rem` on `.cs-card .poke-name`, because even after wrapping, the longest remaining segment (`NaplesEstateJewelry`, 19 chars) still overflowed its flex box and ran *underneath* the stat — 19 chars needs ≤15.6px to clear it inside the 340px card. `overflow-wrap: anywhere` is a last-resort guard that never fires with the `<wbr>` present. Verified all six: stat inside the plate, no name/stat overlap, no text overflow.
- Verified in-browser at 1440×900 and 1120×820: six distinct theme colours, both faces + glare on every card, back face `inert` at rest (so keyboard users never tab into hidden links), flip toggles `data-flip`/`aria-pressed` and swaps `inert` correctly, clicking an inner link navigates instead of flipping, hero template card still green, phone grid still 7 `.portfolio-tile`s, no horizontal overflow, no console errors. Validator: same 65-issue baseline. Note: the preview pane stops returning screenshots once all nine `.poke-scene`s are live (3D transforms + `mix-blend-mode: screen` glare); the visual pass was done before that threshold and the rest confirmed by measurement.

- **Redesigned the Websites gallery cards (`casestudies.html`) so they read as objects.** The page felt messy because the cards had no real presence: `background: rgba(255,255,255,0.03)` + `border: 1px solid rgba(255,255,255,0.1)` and **no resting box-shadow at all**, so on the grid/nebula background they were translucent panes rather than contained cards, and each screenshot's bottom gradient melted straight into the page with no line between picture and text. Fixed at the surface level: near-opaque gradient background (`rgba(23,26,35,0.93)` → `rgba(12,14,19,0.96)`), border to `rgba(255,255,255,0.16)`, a resting shadow (`0 16px 38px rgba(0,0,0,0.5)`) plus a 1px inset top highlight, radius 1rem → 1.1rem, and a `border-bottom` divider on `.portfolio-tile__media` so the image is visibly contained. With the border now drawing the boundary, the media gradient no longer had to do structural work and was lightened (0.78/0.85 → 0.55/0.7 opacity), which makes the screenshots read brighter and crisper. The `--open` "Your Project Here" placeholder was deliberately flattened (no resting shadow, 2.5% background) so it recedes as an empty slot instead of competing as a seventh project.
- **Fixed illegible badge/price chips on the gallery cards (caught by measuring, not by eye).** The chips sit directly on the screenshots and were accent-tinted (`rgba(0,240,255,0.15)` / `rgba(255,212,0,0.12)`), so they were leaning on the media gradient for contrast — and lightening that gradient (above) made a pre-existing weakness materially worse. Sampled the actual image pixels behind each chip via canvas (emulating `object-fit: cover` + `object-position: top center`) and composited the gradient + chip tint to get true ratios: **four of six price pills failed WCAG AA** — Naples **1.8:1** (gold on a 253,251,246 near-white storefront), Elite 2.08, AuctionBuddha 2.35, MetalsCalc 3.6 — and the cyan badge had the same latent bug (cyan-on-white at Naples' top-left). Both chips now use an opaque dark base (`rgba(6,8,11,0.82)`) with the accent kept as text + border, so legibility no longer depends on the image behind them. Re-measured: worst price pill **11.19:1**, worst badge **8.9:1**, all pass AA. Side benefit — the two chips now share one base and differ only in accent colour, which reads as deliberate rather than as two floating tints.
- Verified in-browser at 1280px and 375px (screenshots + computed styles): cards read as distinct objects, image/text divider present, no chip collisions in the 2-up phone grid, no horizontal overflow, no console errors. Validator: same 65-issue baseline. Scoped to `casestudies.html` only — `app-catalog.html` carries its own copy of the `.portfolio-tile` CSS and still has the old treatment, so the two galleries now diverge until it gets the same pass.

- **Build-cost estimates on the Websites gallery and all six portfolio detail pages.** Each case study is now priced against the one-time build scale in `website_pricing_plan.txt` (the pricing source of truth). Gallery tiles (`casestudies.html`, both desktop and mobile grids) carry a small gold `.portfolio-tile__price` pill at the bottom-right of the media area ("EST. $12.5K+" etc. — bottom-right deliberately, so it never collides with the existing top-left number badge on narrow 2-up phone tiles). Each detail page gained a "What would a build like this cost?" section (`#build-pricing`) before the final CTA: headline estimate range, plain-language mapping to the closest published package, itemized scope drivers with approximate figures, links to `services/website-design-hosting.html#one-time-builds` and `contact.html`, and the spec-required disclosure ("estimate only… every project is scoped and quoted individually"). The estimates: Naples Estate Jewelry $12K–$16K (custom e-commerce, larger-store tier + live spot-pricing/scrap-value engine + Spanish entry), Elite Yacht Detailing $8.5K–$10K (Premium Brand Website), JPSurette $4.5K–$6.5K (Local Business + premium editorial design; Fight Night Archive flagged as a separately-quoted future phase), SeansAds $7.5K–$10K (Growth Website + full Spanish mirrors), AuctionBuddha $15K–$25K+ (custom marketplace platform under Custom Software, explicitly framed as "not a template website"), MetalsCalc $2K–$2.5K (Starter + live spot-ticker integration; the PWA app itself noted as a separate custom-software product). SeansAds' section uses that page's own idiom (`font-['JetBrains_Mono']`, standard spacing utilities) because it takes its Tailwind config from `assets/tailwind-nav-config.js`, not the inline config the other detail pages carry. Verified in-browser: 12/12 pills render with correct computed styles, no badge/pill collisions at 375px, `#build-pricing` present with correct range on all six pages, no horizontal overflow, no console errors (screenshots unavailable — the preview pane freezes rAF, per the known caveat). Validator: same 65-issue pre-existing baseline, nothing new.

- **The whole site now has ONE mobile/desktop line at 880px.** Moving only the header to 880 (below) left the page itself still flipping at Tailwind's stock `md:` 768px, so 768–880 showed the mobile bottom bar over a *desktop* layout — the homepage's four-column process grid squeezed into unreadable one-word-per-line strips. Fixed at the source rather than patched per component: `"screens": {"md": "880px"}` added to every page's inline `tailwind.config` (21 pages) and to `assets/tailwind-nav-config.js`, which retargets *every* `md:` utility site-wide in one place. The hand-written media queries that encode the same split were swept to match (`max-width: 767/767.98px` → `879.98px`, `min-width: 768px` → `880px`) across `nav.css`, `cosmic-web.css`, `portfolio-mobile-fixes.css`, `surette-data-systems-apps.css`, `surette-data-systems-home-widget.css`, `surette-physics-wordmark.css`, and the inline styles on 15 pages — both halves have to agree or the page tears in the middle. Deliberately left alone: `account-admin/-settings/-users` and `app-checkout` (no `md:` utilities, no nav loader, so nothing to keep in sync), `client-portal.css` (its 700/860px breakpoints are component reflow points, not the site split), and `care-plans.css` (loaded by no page). Deleted a dead `.sds-nav-contact-stack` tight-band rule that the sweep would otherwise have turned into an impossible `(min-width: 880px) and (max-width: 879.98px)` range — the element it targeted was removed when the apps pages moved to the shared nav. Audited all 28 pages at 390/812/879/880/1280 for coherence: the bottom bar never appears over a desktop layout, bar and hamburger always agree, no mobile bar with the wide footer or vice versa, no horizontal overflow. Assets cache-busted to `v=20260716-md880`.
- **The header now switches to the hamburger at 880px instead of half-collapsing.** Between 768px and 880px the wide header had no room for the Contact CTA beside the full link row, so `nav.css` hid the button — leaving links on the left, a dead gutter on the right, and no way to reach Contact from the header at all. That band now gets the hamburger, so the header is either whole or a menu and never partway. The injected nav's classes are `hidden md:flex` / `md:hidden`, baked to Tailwind's 768px, so two rules in `nav.css` move the boundary for the nav *only*: the shell gained a `dm-standard-mobile-shell` class (`standard-site-nav.js`) precisely so page layouts, which use the same `md:hidden` for their own phone wrapper, are not dragged along and keep switching at 768px. The mobile-chrome media block (dropdown, popout, header, bottom bar, hamburger colours, footer bar-clearance) widened from `max-width: 767px` to `879.98px` to follow the nav; `body.has-dm-standard-nav .page` / `.app-footer` were pulled out into their own 767px block, since those are page layout and would otherwise have forced 20px phone gutters onto the app pages at tablet width. Removed the now-dead tight-band rule. Audited all 28 pages at 768/834/879/880: never both navs, never neither, no partial collapse, hamburger present, no footer links behind the bar, no horizontal overflow. (Two false alarms during that audit were audit bugs, not site bugs — `getComputedStyle` reports a child's own `display` inside a `display:none` parent, and `offsetParent` is null for `position:fixed`; a zero bounding box is the test that is right for both.) `nav.css` and `standard-site-nav.js` → `v=20260716-nav-880`.
- **Homepage wide hero capped like the mobile one.** `#hero` is `min-h-screen`, so the same flexible stage that strands the mobile CTA stranded the desktop one: fine at a laptop's 800–980px tall (CTA ~82% down, ~180px gap — the tuned look), but a 1278px-tall window stretched it into a ~550px void and dropped the button to 87%, with the Services section pushed off the fold. Now `min-height: min(100vh, 1000px)`, which leaves every common desktop height (800, 900, 982) byte-identical and only reins in taller windows. At 1014×1278: hero 1278→1000, CTA 1117→839, gap 550→272, Services 1300→1022. Headline untouched.
- **Homepage hero no longer strands its CTA on tall viewports.** `#hero-mobile` was `min-height: 74vh` with a `flex: 1 1 auto` middle stage that absorbs all leftover space, so the Contact Us button always rode ~72% down the viewport. That is well tuned on a phone (~160px of sky above the button at 390×844) but 74vh is a ratio, not a distance: at 614×1278 — a tablet-sized window still under the 768px breakpoint, so still the mobile layout — it became ~450px of empty starfield, pushing the button and every section beneath it off-screen. Changed to `min-height: min(74vh, 700px)`. Chose an absolute cap over a width breakpoint deliberately: a `@media (min-width: 600px)` variant worked but invented an arbitrary threshold and jumped the hero 179px the moment you crossed it, whereas the cap engages smoothly only once 74vh exceeds 700px (viewports taller than ~945px) and leaves every real phone on its original ratio. Headline spacing is untouched — the space comes out of the middle stage, below the copy, which is what was asked. Measured across 390×844, 412×915, 430×932 (all unchanged), 599/614×1278, 700×1100, 767×1024 (hero 946→700, CTA 922→676, following section 1030→784) and 768×1024 (desktop hero untouched); confirmed with screenshots at 614×1278 and 390×844.
- **PWA manifest description** aligned with the site's current positioning ("Custom agentic AI-powered business software, web apps, websites, hosting, branding, and on-site tech for Southwest Florida"), matching the homepage hero copy; it still described the pre-SDS "managed websites, hosting, support, local IT setup" scope.
- **PWA manifest rebranded to Surette Data Systems.** `site.webmanifest` still declared `name: "Dark Matter Surette Systems Portal"` / `short_name: "Dark Matter"` — the label an installed app shows on a home screen. Now `name: "Surette Data Systems"`, `short_name: "Surette"`. The short name is intentionally not the full brand: it is what sits under the icon and gets truncated past ~12 characters, so "Surette Data Systems" (20) would have been clipped. Also versioned the manifest's two icon `src`s to `?v=20260716-blur-on`, matching the HTML links, so an already-installed app picks up the new artwork. Verified the manifest still parses, serves as `application/manifest+json`, and both icons return 200. Left the "powered by / built by Dark Matter" badge assets alone — those are a deliberate sub-brand attribution, per DECISIONS.
- **Favicon is now the footer's `blur_on` mark.** Replaced the old black-hole artwork (still literally `aria-label="Dark Matter"`, so also off-brand) with the Material Symbols Outlined `blur_on` glyph in electric cyan on the void-black rounded square — the same icon the shared footer shows beside the wordmark. Used the official 960-grid outline from Google's icon endpoint (Apache 2.0, same family the site already loads) rather than approximating the path by hand. The transform is measured off the glyph's *ink*, not its box: the dots only span 100..860 on the 960 grid, so fitting the box would have left the mark at 57% of the tile and mushy — `translate(23.2, 488.8) scale(0.485)` puts the 760-unit ink at 72% of the tile, verified centred to ±0.1px. Regenerated `favicon-32/192/512.png` and `apple-touch-icon.png` at their existing dimensions by rasterising the SVG through Playwright (the repo already ships it; the Browser pane's screenshot was wedged). Browser/tab icons keep the SVG's transparent rounded corners; **apple-touch-icon is deliberately an opaque square** (`rx=0`, `#050505` fill) because iOS applies its own mask and fills transparency, which would otherwise double-round the icon. Added `?v=20260716-blur-on` to all favicon/apple-touch links across 33 pages, since browsers cache favicons hard and the site's `?v=` convention did not previously extend to them. Verified each URL serves 200 with the right content-type, and reviewed a rendered proof sheet at 16/24/32/180/512 on light and dark chrome. Note: at 16px the dots merge into a cyan speckle — inherent to `blur_on`'s fine grid, legible from ~24px up.
- **One shared footer site-wide (new `assets/standard-site-footer.js`).** The site had drifted to ~10 different footers across 27 pages — `.app-footer`, `.service-mobile-footer`, `.portfolio-hub-footer`, compact/centered one-offs — each with its own link set, so a footer change only ever reached the page it was made on. Same fix as the nav: every page now injects the footer from one file via `<script src="assets/standard-site-footer.js?v=20260716-shared-footer">`, and **no page carries inline footer markup**. Reproduces the homepage exactly, which means two variants rather than one responsive layout (the homepage's wide and phone footers differ in content, not just arrangement): `.dm-footer` from 768px up (tablet included) with the full 9-link list + tagline, `.dm-footer-mobile` below it, centered with 7 short labels and the brand mark. Pages built as two parallel layouts (index, contact, casestudies, accessibility, process, privacy, terms, and the two service pages) take `data-variant="desktop"` / `"mobile"` inside their respective wrappers, since a footer injected into the hidden wrapper would be hidden with it; every other page takes one tag and lets CSS choose. Paths get a `../` under `services/`, same as the nav. **Styled with plain CSS in `nav.css` (`.dm-footer*`) rather than the homepage's Tailwind utilities**: the CDN only builds utilities it finds in a page's *static* HTML, and once the footers are injected those classes exist nowhere else — probed on the pricing page, `text-on-primary-container/60` silently rendered white. Verified by injecting the component next to the homepage's real footers and diffing every computed property at both breakpoints: **zero style diffs**, identical link text and legal line (one real bug caught this way — the brand icon's line-height was 20px vs the homepage's 28px). Then audited all 27 pages at 390px and 834px: correct variant, exact link set, correct `../` prefixes, one visible footer each, and bar clearance intact.
- **Fixed an invisible hamburger button on `portfolio.html`** (pre-existing, found during the footer audit). Its three lines colour themselves with `bg-electric-cyan` / `bg-starlight-white/55` / `bg-starlight-white/35` on injected nav markup, and that page never used those classes in its own static HTML, so the CDN never generated them and all three resolved to `rgba(0,0,0,0)` — the menu button was present and clickable but completely invisible. Pinned the three colours in `nav.css` alongside the injected nav's other fallbacks; identical values, so the 27 pages where the CDN does generate them are unaffected. Verified all 28 pages now render three visible lines.
- **Fixed the mobile bottom tab bar covering page footers.** The bar is `position: fixed`, so it reserves no space and simply sits on top of the last 5rem of every page. An audit of all 28 public pages at 390px found **7 with real content underneath it**: the six portfolio detail pages (Privacy, Terms, Accessibility, phone and email all unreachable) and `services/website-design-hosting.html` (Accessibility). The portfolio ones were a regression from the 2026-07-15 nav migration — deleting `assets/unified-mobile-menu.js` also removed its `body.has-dm-unified-mobile-shell .min-h-screen { padding-bottom: 5rem }` rule, which was the only thing holding those footers clear; the pricing page had been broken independently, since that rule never applied to it. Fixed with one rule in `nav.css`: `body.has-dm-standard-nav footer { margin-bottom: calc(5rem + env(safe-area-inset-bottom, 0px)) }` under 768px. This adopts the idiom `index.html` already used (a `mb-20` on its footer — 5rem, exactly the bar height), so pages already doing it resolve to the identical value and are untouched. Deliberately a **margin on the footer, not padding on the body**: the first attempt used body padding and passed every measurement, but a screenshot showed it lifting the footer off the bar and exposing a strip of the fixed starfield beneath it — the footer must stay flush so the bar's blur sits over the footer's own background. Also removed the now-redundant `pb-24` from the two service pages' mobile wrappers, which would otherwise have double-counted (their footers end 16px tighter than before, no content affected). Verified across all 28 pages via an iframe harness at 390px: zero hidden links and every footer flush against the bar (±1px), plus screenshots of the pricing page, a portfolio detail page, and the homepage. `nav.css` cache-busted to `v=20260716-bottom-bar-clearance` on all 33 referencing pages.
- **Scroll hint for horizontally-scrollable tables (new `assets/table-scroll-hint.js`).** Wide tables scroll sideways on phones/tablets but nothing said so, so readers never found the columns past the right edge. The first time such a table scrolls into view it now eases ~52px to the right, holds, and eases back (~1s total). Written generically on purpose — it walks up from every `<table>` to whatever ancestor actually has `overflow-x`, so any table on any page is covered without opting in and a new one can't be forgotten; it resolves `.wp-scroll` on the pricing page and `.client-admin-table-wrap` in the portal without naming either. Guards: only when the container *really* overflows (which is why desktop stays still), only at ≤1279.98px, never when already scrolled, cancelled by any pointer/touch/wheel/key interaction mid-flight, and fully skipped under `prefers-reduced-motion`. Watches both an IntersectionObserver *and* a ResizeObserver because the two preconditions (in view, wide enough) can arrive in either order — the portal's tables ship with an empty `<tbody>` and only overflow once rows load, confirmed in-browser. Loaded on `services/website-design-hosting.html`, `account-admin.html`, `account-users.html` (`v=20260716-table-scroll-hint`). Verified by re-running the shipped file in-browser under a stubbed clock/observer harness (the preview pane freezes rAF, so a visual check was not possible): traced 0 → 52px peak → hold → 0 with inline styles cleaned up, plus each guard proven independently (desktop gate blocks it, already-scrolled left at 120px untouched, interaction cancels mid-flight, reduced-motion registers zero observers). No console errors.
- **Fixed in-page anchor landing on the pricing page.** Clicking the purple jump pills (Managed Plans, One-Time Builds, Hosting & Care Plans, …) landed with the section's teal `.wp-eyebrow` crammed against the fixed nav. Cause: `section[id] { scroll-margin-top: 110px }` vs. a nav that measures **101px** from 768px up — 15px of clearance, which read as the heading tucked under the nav's blurred edge. One offset can't serve both navs (the mobile header is 64px, where 110px was already fine), so the value is now `145px` with a `110px` override under 768px, giving ~50px of breathing room at every width. Verified across all eight pill targets at 834px and 1280px (15px → 50px) with mobile unchanged.

## 2026-07-15

- **Site-wide mojibake sweep and repair.** Scanned every `.html/.css/.js/.md/.txt/.json/.toml/.svg` file (excluding `node_modules`) for cp1252-misdecoded UTF-8 artifacts, BOM-bytes-as-text, and U+FFFD replacement characters. Fixed: `account-created.html` had a double-encoded BOM (six junk bytes before `<!DOCTYPE>`, forcing quirks mode — same defect `portfolio-seansads.html` had); the three Benji SVG screenshots (`assets/apps/benji/benji-approval-queue.svg`, `benji-dashboard.svg`, `benji-pay-stub.svg`) had 14 U+FFFD replacement characters where their `·` field separators should be; `DEVELOPER-NOTES.md` and `project-docs/` (`ARCHITECTURE.md`, `CURRENT_STATUS.md`, `HANDOFF.md`, `TASKS.md`) had ~70 em-dash artifacts restored to real em dashes. `casestudies.html` and `app-checkout.html` were fixed in a parallel session. Validator mojibake findings for site content are now zero (one remaining hit is a `node_modules` vendor file; `_enum_out.txt` is stale generated output with unrecoverable replacement chars, flagged in TASKS for regeneration/deletion). Total validator issues dropped 81 → 65; the remainder is the documented missing-`es/`-mirror and node_modules noise.
- **Nav migration: one source of truth site-wide.** Root cause of the recurring "nav drift" was that only 11 pages used the `assets/standard-site-nav.js` generator while ~16 pages carried hand-copied inline nav markup in 4 different vintages, papered over at runtime by `assets/unified-mobile-menu.js` — so every nav fix landed in the generator but never in the copies. Migrated all remaining public pages to the loader and deleted the shim. Details: (1) fixed the loader to prefix all emitted links with `../` on `services/*` pages (mirroring `mobile-services-nav.js`) — before this, every injected nav link on `services/website-design-hosting.html` 404'd (`services/index.html`, `services/services/...`); (2) replaced inline navs with the loader tag on `index.html` (`data-active="home"`), `apps.html` (`services`), `app-catalog.html` (`apps`), `contact.html` (`contact`), `casestudies.html` (`portfolio`), `built-by.html`/`accessibility.html`/`process.html`/`terms.html`/`privacy.html` (`none`), all five `portfolio-*` detail pages (`portfolio`), and `services/in-home-services.html`/`services/office-network-setup.html` (`services`) — removing their duplicated desktop navs, mobile headers, hand-rolled hamburger dropdowns + inline menu JS (index/terms/privacy), bottom tab bars, and `apps.html`/`app-catalog.html`'s `.sds-nav-contact-stack` floating CTA (redundant with the generator's built-in Contact button); (3) deleted `assets/unified-mobile-menu.js` and its script tag on all 28 referencing pages plus its `.dm-unified-mobile-shell`/`has-dm-unified-mobile-shell` rules in `nav.css`; (4) cache-busted every `standard-site-nav.js` reference to `v=20260715-nav-unify`. Bugs this kills: apps/app-catalog/casestudies/built-by/accessibility/process highlighted **Portfolio** as the current page in the mobile hamburger (shim's active-detection fallthrough) and `apps.html`'s bottom tab bar had the Portfolio tab's active classes baked in; homepage mobile dropdown had a divergent structure (separate accordions vs the canonical single "All Services"); `index`/`contact`/portfolio-detail desktop "Client Login" was still hardcoded cyan (pre-`login-highlight` vintage); portfolio detail pages had **no mobile nav in markup at all** (synthetic runtime shell); `terms.html` never loaded `surette-logo.js` so its nav logo was empty (added, matching `privacy.html`). Also removed `app-pricing.html`'s `data-lang-alt="es/app-pricing.html"` (no `es/` pages exist — the nav's ES toggle 404'd), versioned `portfolio-seansads.html`'s bare `tailwind-nav-config.js` reference, and repaired a double-encoded BOM at the top of `portfolio-seansads.html` (its BOM bytes had been re-encoded into six literal junk characters before the doctype, forcing quirks mode). Verified in-browser at 375px and desktop across index, apps, app-catalog, contact, terms, privacy, casestudies, built-by, process, both `services/*` pages, and portfolio detail pages: exactly one desktop nav/mobile header/bottom bar each, correct active highlighting everywhere, login link grey while signed out, hamburger + accordion + services popout working, all `services/*` nav links resolving with `../`, no console errors. Validator: no new issues vs baseline (pre-existing noise documented; mojibake in `casestudies.html`/`app-checkout.html` content is pre-existing uncommitted work, flagged separately).
- Fixed "Client Login" always rendering highlighted/cyan regardless of actual sign-in state (site-wide, both desktop nav and the mobile hamburger menu row). This was a separate, older bug than the `dm-mob-acct` icon-only header button, which already correctly went grey/cyan based on session state via `assets/account-nav.js`; the full-row/full-link versions were hardcoded cyan in the generator scripts and never consulted the session at all. Changed `desktopClass("account")` and `mobileMenuLinkClass("account")` (`assets/standard-site-nav.js`) and `itemClass('account')` (`assets/unified-mobile-menu.js`) to emit the same neutral/grey styling as any other non-active nav link by default, tagged with new marker classes `dm-account-link-desktop`/`dm-account-link-mobile`. Extended `account-nav.js`'s existing `apply()` (which already re-labels "Client Login" ↔ "Account" on session change) to also toggle an `.is-signed-in` class on these marker classes; new CSS in `assets/nav.css` makes `.is-signed-in` cyan (plus a subtle background tint on the mobile row, matching how "current page" highlighting already looks elsewhere in that menu). `!important` used defensively, matching this file's established pattern for overriding Tailwind CDN-JIT class-generation quirks on dynamically injected nav markup. Cache-busted all 4 files (`v=20260715-login-highlight`) across every referencing page. Verified on both the `standard-site-nav.js` path and the legacy `unified-mobile-menu.js` path by toggling the `dm_logged_in` localStorage flag: signed-out shows neutral gray + "Client Login" on both desktop and mobile, signed-in shows cyan (+ background tint on mobile) + "Account" on both, and clearing the flag correctly reverts. No console errors, same 84 pre-existing validator issues as baseline.
- Restructured the mobile hamburger dropdown's services section (site-wide — both nav-generator scripts). Previously "All Services" was a plain link to `apps.html` sitting above two *always-visible* accordion toggles ("Online Services" and "In-Home & Office Services"), so a first-time visitor saw all three service-related rows cluttering the main menu list before touching anything. Consolidated into the pattern the bottom-nav's Services popout (`mobile-services-nav.js`) already used correctly: "All Services" is now the single accordion toggle, and "Online Services"/"In-Home & Office Services" became small non-interactive category tags (new `.mobile-menu-group-label` — same visual language as `mobile-services-popout__heading`) sitting directly above their links inside the one panel, instead of being their own nested accordions. One click on "All Services" now reveals everything at once. Preserved the cyan "current page" highlight on the toggle when `active === "services"`. Fixed in both `assets/standard-site-nav.js` (current pages) and `assets/unified-mobile-menu.js` (legacy-template compatibility layer — same bug, independently duplicated), added the shared `.mobile-menu-group-label` CSS to `assets/nav.css`, and cache-busted all three (`v=20260715-services-accordion`) across every referencing page (33 for nav.css, 11 for standard-site-nav.js, 28 for unified-mobile-menu.js). Verified both code paths in-browser (`services/website-design-hosting.html` for the standard path, `services/in-home-services.html` for the legacy path): collapsed state shows only "All Services," expanding it reveals both category labels with correct relative-path links, no console errors, no horizontal overflow. Confirmed tablet width (768px+) is unaffected since this site's tablet band already uses the desktop hover-nav, not the mobile hamburger. Reran the validator: same 84 pre-existing baseline issues, nothing new.
- Applied the footer wrap fix site-wide. Moved `.footer-meta`/`.footer-meta-sep` into shared `assets/nav.css` (cache-busted to `v=20260715-footer-wrap-fix` on all 33 referencing pages) and audited every page for the "Call or text (239) 404-8505" copyright-line pattern. Found the site actually has ~5 different footer template variants, not one uniform copy-paste:
  - **Already safe, left untouched**: `index.html`, `accessibility.html`, `privacy.html`, `terms.html` (already split into two separate block-level spans) and 7 app-profile pages — `antique-mall-*`, `app-pricing.html`, `auction-house-*`, `benji-payroll-*`, `metalspredictor-*`, `metalscalc-buying-calculator.html`, `secondhand-dealer-*` (already use `<br/>` to force the phone onto its own line), plus `app-catalog.html`/`apps.html` (same `<br/>` pattern, no email/location in that line). None of these could wrap mid-phrase to begin with.
  - **Had the bug, fixed (15 instances / 11 files)**: `casestudies.html` (×2), `contact.html` (×2), `process.html` (×2), `services/in-home-services.html`, `services/office-network-setup.html`, `portfolio-auctionbuddha.html`, `portfolio-eliteyachtdetailing.html`, `portfolio-jpsurette.html`, `portfolio-naplesestatejewelry.html`, `portfolio-metalscalc.html`, `portfolio.html`. Same flex-wrap-phrase-spans treatment as `website-design-hosting.html`, adapted per file: preserved each page's own wrapper tag/class and added `justify-center`/`md:justify-start` matching its existing text-alignment intent, added a `tel:+12394048505` link where the phone was previously plain unlinked text, and normalized two pre-existing inconsistencies found along the way — 4 portfolio pages were missing the separator dot before the email (double-space instead of `&middot;`), and `portfolio-metalscalc.html` used `&nbsp;` separators instead of `&middot;` like everywhere else.
  - Verified: `casestudies.html`'s two instances both correctly inherit the shared nav.css rule with working tel/mailto links; `portfolio-jpsurette.html` and `portfolio.html` (no-email variant) confirmed visually and via computed style at 375px — phrases wrap as whole units, phone number never splits, no horizontal overflow. Reran the validator: same 84 pre-existing issues as baseline, nothing new.
- Fixed the site footer's copyright/contact line wrapping chaotically on mobile ("Call or text" splitting from the phone number mid-phrase). It was one plain text string with `&middot;` separators, so the browser could break anywhere a space allowed. Restructured into flex-wrap spans — each phrase ("© 2026 Surette Data Systems", "Southwest Florida", "Call or text (239) 404-8505", the email) now wraps as a whole unit, never mid-phrase. Also gave the phone number a proper `tel:+12394048505` link with `white-space:nowrap` (it was plain unlinked text before, inconsistent with the email being a working `mailto:` link right next to it) and matched its cyan link color. Verified: 4 clean stacked lines on mobile with the phone number never breaking internally, single unbroken line restored at desktop width, no overflow. Scoped to this page only — the identical hand-authored footer string exists on all 26 other pages and would need the same fix there if wanted.
- Widened the One-Time Website Builds table too, same fix as Managed Plans below: its 8 columns squeezed into 620px (~78px each) was even tighter than Managed Plans had been, causing 2-3 line wraps on most rows. Bumped to 900px min-width with a 138px sticky Package column and a 108px floor on the other 7 columns. Table height dropped 676px → 550px despite being wider; most rows are now single-line. Checked the FAQ and Add-ons sections too — Add-ons summaries are all single-line already (no fix needed), and FAQ is already at 100% of its available mobile width (single-column, no unused space to reclaim like the tables had), so its occasional 2-line question wrap is normal accordion behavior, not the same bug — left as-is.
- Widened the Compare Managed Plans table specifically (mobile only) after the shrink above wrapped too aggressively — 3-line labels and 2-line prices were making the table *taller*, working against the "less scrolling" goal. Bumped `#managed-plans .wp-table` from 640px to 800px min-width, sticky label column from 90px to 122px, and added a 128px min-width floor on the data columns. Net result: most rows are now 1-2 lines instead of 2-3 (e.g. "First-year total (monthly)" and plan-name headers now fit on far fewer lines), trading a bit more horizontal scroll for meaningfully less vertical scroll. Confirmed the other two tables (one-time-builds, scheduling) and tablet/desktop are unaffected.
- Shrank the 3 comparison tables on mobile only (`services/website-design-hosting.html`, max-width:767.98px): 12px body / 12.5px header font (from 14.5px/15px), tighter 8px/10px cell padding (from 13px/16px), and — the change that actually mattered — let header/label text wrap instead of staying `nowrap`, since the min-width reduction alone did nothing while nowrap text kept forcing columns wide (tables rendered at 1006px/863px/440px, wider than the 640/720/440 floors, until wrap was allowed). After wrapping, tables measure ~640/634/380px, roughly 30% narrower, with more columns visible per screen and less vertical row height. Had to force `white-space:nowrap` back onto the "Most Popular" badge specifically, since it briefly wrapped mid-word ("MOS"/"POP") once its parent header cell allowed wrapping. Verified tablet/desktop are untouched (media query gated at 767.98px).
- Made the first column sticky (`position:sticky; left:0`) on all 3 horizontally-scrolling comparison tables (`services/website-design-hosting.html`) — the row-label/plan-name column now stays pinned in view while the rest of the columns scroll underneath it on mobile/tablet, with an opaque background (sticky cells are otherwise transparent) and a drop shadow signaling more content to the right. Verified at 375px and 834px on all three tables via `scrollLeft`.
- Recolored the `.wp-h4` mini-header tier (29 instances — card sub-headers, FAQ group headers, process step titles) from near-white to soft cyan (`#8fe0ee`), since at that size it sits directly next to similarly-sized body text and needed hue, not just size, to read as a header.
- UX iteration pass on the pricing page (`services/website-design-hosting.html`) after the initial build: (1) split the flat type scale into a real hierarchy — new `.wp-h4` (muted mini-headers) and `.wp-label` (mono uppercase scaffolding labels) tiers, brighter `.wp-lead`, dimmer `.wp-body`; (2) added scroll-reveal to all 70 cards/tables/FAQ columns with per-grid `nth-of-type` stagger delays; (3) contrast pass — pills, ghost buttons, chips, and accordions all got stronger tinted backgrounds/borders, pills now purple-tinted (nebula accent) with an `arrow_forward` icon + hover lift/glow so they read as buttons, not info tags; (4) simplified the hero to just H1 + lead + 3 CTAs and moved the jump-pill nav below the "Two Ways to Build" section where its labels make sense to a first-time reader; (5) novice-flow pass — plain-English glossary accordion (hosting/domain/SSL/form+lead/backups/SEO) and a "not sure which path?" guidance line in the Two Ways section, orienting sentence at the top of One-Time Builds, hero "Compare Website Options" retargeted to `#paths`, and `.wp-price-note` bumped 12.5→13.5px for readability.
- Sitewide: widened the shared scroll-reveal trigger in `surette-logo.js` from `rootMargin '0px 0px -60px'` (which fired *after* elements were already 60px inside the viewport — visible blank flash) to `'0px 0px 320px'` with threshold 0, so elements begin animating while still below the fold; cache-busted `surette-logo.js` on all 27 referencing pages (`v=20260715-reveal-margin`).
- Built the complete public pricing page on `services/website-design-hosting.html`, replacing the placeholder. Content implements `website_pricing_plan.txt` (project root — the owner's source-of-truth pricing/policy spec) in its prescribed 17-section order: hero, two purchasing paths, featured "$0 Upfront Starter Website" (with "Website Build Included" badge and explicit not-free disclosure), 5-plan managed comparison table with pricing/third-party-cost disclosures, per-plan detail cards, after-12-months ownership/renewal/cancellation, 8-package one-time build comparison + details with payment structure and bug-fix definition, 4 hosting/care plans, update-allowance/support/revision policies, lead-capture and data handling, scheduling-by-plan, 8-step process with timelines/delay policy and plain-language fine-print accordions (copywriting, images, page definition, SEO definitions, accessibility, legal pages, upgrades/downgrades), ownership/domain/handoff, add-on price list, 36-question FAQ, and final CTA. Uses native `<details>` accordions, 3 horizontally-scrolling comparison tables, and page-scoped `wp-*` component styles; "Most Popular" badge on Lead Capture. Verified in-browser: 17 sections/94 accordions render, all in-page anchors resolve, no duplicate IDs, no mojibake, zero horizontal overflow at 375px, and all plan figures/policy strings present. Validator shows only documented pre-existing noise plus the expected missing Spanish mirror.
- Fixed stale mobile Services popout links: `assets/mobile-services-nav.js` still pointed at the three retired pages (`website-design.html`, `managed-hosting.html`, `website-care-plans.html`) in both English and Spanish copy — dead links on every page using the popout since the merge. Replaced with a single "Website Design / Hosting" / "Diseño Web / Hosting" entry matching `standard-site-nav.js` and `unified-mobile-menu.js`, and cache-busted the script reference on all 28 referencing pages to `v=20260715-services-merge`.

- Fixed `portfolio.html`/`account.html` still showing the Contact Us button and the Account nav link inconsistently after the earlier font-utility fallback fix: the same Tailwind-CDN-JIT-doesn't-reliably-scan-dynamically-injected-markup problem also affected custom-theme *color* utilities (`text-electric-cyan`, `text-on-surface-variant`, `text-outline`, `text-void-black`, `border-electric-cyan`, `bg-electric-cyan/5`), not just fonts — so the Account link was rendering in the default text color instead of highlighted cyan, and Home/Services were inheriting the wrong gray. Added the same kind of explicit CSS fallback for these color utilities to shared `assets/nav.css`. Verified via `getComputedStyle` sweeps: zero color mismatches across every `.text-electric-cyan`/`.text-on-surface-variant`/`.text-starlight-white`/`.text-outline` element on both pages, not just the nav.
- Found one more instance of the same bug: the Contact Us button's gradient background (`bg-gradient-to-r from-electric-cyan to-nebula-purple`) was also failing to render on `portfolio.html`/`account.html` — the JIT scanner generated a bare `.bg-gradient-to-r` rule (since that class also appears elsewhere on the page) but never generated `.from-electric-cyan`/`.to-nebula-purple`, which set the `--tw-gradient-stops` custom property the base rule depends on. Without those, `background-image` silently resolved to `none` and the button rendered with no visible background at all. Added an explicit `.bg-gradient-to-r.from-electric-cyan.to-nebula-purple { background-image: linear-gradient(90deg, #00F0FF, #7000FF); }` fallback to `assets/nav.css`. Verified `getComputedStyle(...).backgroundImage` renders the gradient correctly on both pages now.

- Consolidated `services/website-design.html`, `services/managed-hosting.html`, and `services/website-care-plans.html` into one new page, `services/website-design-hosting.html` ("Website Design & Hosting"), left as a placeholder pending content. Updated the single merged nav entry across every touchpoint: `assets/standard-site-nav.js` and `assets/unified-mobile-menu.js`'s Online Services list (English + Spanish copy), the services dropdown on all 19 hand-authored pages (including the single-line minified markup on `apps.html`/`app-catalog.html`), `assets/rail.js`'s process-rail step slugs, `scripts/tablet-audit.js`'s page list, and index.html's homepage services-section cards, footer links, and "How It Works" process step links (desktop + mobile variants of each). Removed a dead "Back to Website Design" breadcrumb in `casestudies.html` that no JS ever un-hid. Left `terms.html`'s legal "Services Covered" prose untouched — only its nav/footer links changed. Added 301 redirects in `netlify.toml` from all three old URLs (English + Spanish) to the new page for any existing bookmarks/backlinks.
- Fixed the Services nav-trigger cursor: it opens a hover dropdown with no `href`, so browsers were showing the default text-select cursor instead of a pointer, which read as broken/unclickable. Added `cursor: pointer` to `.services-nav-group > a:first-child` in shared `assets/nav.css`.

- Removed the internal Sean's Google Ads portal feature entirely: deleted `seans-google-ads-dashboard.html`, `account-ads-status.html`, and `assets/seans-ads-dashboard.js`. Removed the `sean_ads_admin` role and its helpers (`isSeanAdsAdminUser`, `canOpenSeanAdsPortal`, `getSeanDashboardAccess`) from `assets/portal-auth.js`, the role/email allowlist from `assets/supabase-config.js`, and all UI wiring (banner, admin-panel link, Admin Center "Sean Ads" tab) from `account.html`, `account-settings.html`, `account-admin.html`, `assets/client-portal.js`, and `assets/account-settings.js`. Updated `supabase/portal-role-setup.sql` to drop `is_portal_sean_ads_admin()` and removed the role assignment from `scripts/set-portal-roles.ps1`. The public SeansAds.com portfolio case study (`portfolio-seansads.html`, its `casestudies.html` tile) is unrelated and was intentionally left untouched.
- Fixed top-nav consistency bugs found while auditing header/menu appearance across every page (desktop, tablet, mobile): `assets/standard-site-nav.js` and `assets/unified-mobile-menu.js`'s Client Login link/mobile-dropdown Account item now always render in the cyan "always-on" style regardless of which page is active (previously only styled that way on `account.html` itself). Normalized the Contact Us button classes (font, color, hover/glow) across `casestudies.html`, all six portfolio detail pages, `built-by.html`, the five `services/*.html` pages, `process.html`, `privacy.html`, `accessibility.html`, and `contact.html`, which had each drifted to different one-off styles (mismatched font, wrong text color, missing hover/glow). Centralized the `.glow-cyan` CTA glow utility into shared `assets/nav.css` (it was previously duplicated inline on only 3 pages, so the Contact Us button silently lost its glow anywhere else that referenced the class). Fixed `portfolio-metalscalc.html`'s `data-active="websites"` (not a recognized nav key, so its Portfolio tab never highlighted) to `data-active="portfolio"`. Verified in-browser at desktop and 375px mobile (hamburger menu, dropdown, and bottom nav all match) and reran `scripts/validate-site.ps1`; no new issues, only documented pre-existing noise.
- Found and fixed a deeper nav-text bug the class-string audit above missed on `portfolio.html` and `account.html` (both use the `assets/standard-site-nav.js` generator): computed-style inspection in the browser showed Home/Services/Portfolio/Client Login rendering in the page's default sans-serif font instead of JetBrains Mono, even though the HTML classes (`font-label-mono`, `text-label-mono`) were correct. Root cause was two-fold — `account.html`'s own `tailwind.config` was missing the `label-mono`/`label-caps` theme keys entirely (fixed to match the rest of the site), and Tailwind's CDN JIT scanner unreliably generates custom-theme utility classes for markup injected after page load via `insertAdjacentHTML` (affecting `portfolio.html` even though its config was correct). Added explicit `.font-label-mono`/`.text-label-mono`/`.font-label-caps`/`.text-label-caps` rules directly to shared `assets/nav.css` so these frequently-used utilities render correctly everywhere regardless of a page's own Tailwind config or JIT timing — verified via `getComputedStyle` sweeps (zero mismatches) across `account.html`, `portfolio.html`, and every other nav-generator page, both desktop and the mobile dropdown/bottom nav.

- Unified public header/navigation behavior across standard and legacy page templates. Added `assets/unified-mobile-menu.js` for legacy mobile markup, updated `assets/standard-site-nav.js` hooks, and tightened shared `assets/nav.css` viewport anchoring, side gutters, and phone-width overflow containment. Browser verification passed 30 public routes at five widths (1440px, 834px, 768px, 767px, and 390px), including Portfolio and Client Login.
- Added `portfolio.html` as a new Portfolio hub with two category paths: Surette Data Systems apps/software and Websites/case studies. Both cards use existing site imagery and link to the existing `apps.html` and `casestudies.html` pages.
- Replaced separate Apps and Websites items in hand-authored desktop headers and mobile nav rows with one Portfolio item pointing to `portfolio.html`; existing Apps/Websites pages and detail pages remain available downstream.
- Converted `portfolio.html` from a one-off hard-coded header/mobile nav to the shared `assets/standard-site-nav.js` generator with `data-active="portfolio"`, matching the rest of the site navigation path.
- Verified `portfolio.html` at desktop and 390px mobile preview: no horizontal overflow, mobile gutters fixed, page reveal reaches opacity 1, and the only browser warning is the existing Tailwind CDN warning. Reran `scripts/validate-site.ps1`; remaining failures are documented pre-existing noise plus the expected missing Spanish mirror for `portfolio.html`.

## 2026-07-04

- Restored Netlify Forms capture for customer message forms while keeping Supabase/Admin Center saves. Contact, Apps consultation, App Checkout, and account-dashboard requests now save to Supabase first and then post to Netlify Forms so Netlify can send the owner notification email.
- Removed the Resend/Netlify Function notification path and cache-busted `assets/site-message-forms.js` / `assets/client-portal.js` references on the affected pages. Verified JS syntax and reran the static validator; validator still fails only on documented pre-existing noise.

## 2026-07-03

- Fixed service-page header/menu alignment by anchoring fixed top headers to the viewport in shared `assets/nav.css`, then cache-busted `nav.css` references. Verified all service pages no longer show the mobile header shifted right.
- Removed profile-level business/company and website URL fields from account settings and Admin Center tables. Supabase setup/schema files now omit those columns and drop old `company_name`/`company`/`website`/`site_url` profile/subscriber columns if present.
- Separated Admin Center Subscribers from Account holders. Subscriber tables now read only `homepage_email_signups`, account holders stay in the account-holder tab, new account signups still mirror into subscriber rows, and rerunning `portal-role-setup.sql` no longer backfills all auth users into Subscribers after an owner deletion.
- Widened Admin Center Subscribers and Account holders table name/email columns with explicit column groups, and narrowed the Subscribers source column so "Account signup" can wrap compactly.

- Made the mobile header account icon color state consistent across pages: `assets/account-nav.js` and portal nav updates now render the icon grey while signed out and cyan/blue while signed in, with cache-busted script references.
- Added the standard mobile header/menu chrome to the service-page mobile markup path: fixed Surette logo header, account icon injection target, promoted top nav row, active Services tab, and working shared services popout.
- Added compact mobile footers to the service-page mobile markup path for Website Design, Managed Hosting, Website Care Plans, In-Home Tech Services, and Office Network Setup. The existing desktop service footer remains unchanged and hidden correctly on mobile.
- Added a no-white-flash dark loading baseline across the static site: critical inline head CSS on HTML pages, stronger shared dark `html`/`body`/app-root defaults in `assets/nav.css`, `theme-color` normalized to `#050505`, and desktop/mobile navigation/reload verification across the main public routes. Kept visual page shells transparent after verification so the account page's animated portal background remains visible.
- Added the shared desktop site footer to the remaining service-page outliers, `services/in-home-services.html` and `services/office-network-setup.html`.
- Removed stale standalone demo pages that were not linked from the live site: `surette-logo-demo.html`, `blackhole-icon.html`, and `singularity.html`.
- Added a sitewide object-level reveal for repeated cards, panels, pricing tiles, glass blocks, and portfolio tiles so whole objects fade in together after page-ready instead of text appearing before the background/border treatment.
- Added a same-site outbound navigation loader: link clicks now show the small black spinner overlay briefly before navigation so page changes feel intentional instead of frozen.
- Stabilized the page reveal after the white-screen regression: restored the `html`-based fade path, kept the root/background black, added a CSS safety fallback for logo pages if JS reveal is interrupted, and cache-busted `surette-logo.js` across pages.
- Updated public-facing contact email references from legacy/old addresses to `info@SuretteSystems.com` across site pages while leaving sample input placeholders and Supabase/admin setup emails unchanged.
- Ran a focused header/transition audit across representative desktop and mobile pages. Confirmed the standard reveal settles cleanly, validator noise remains pre-existing, and service-detail mobile nav is the main remaining consolidation opportunity.
- Normalized the account page desktop header to match the main site nav: "Client Login" label, account-circle icon, matching font classes, and a cache-busted `client-portal.js` reference so the runtime signed-out state no longer rewrites it to "Login".
- Matched the homepage newsletter email field more closely to the account-page inputs with a transparent wrapper, thin cyan outline, darker subtle interior, clearer sample-email placeholder, and a brighter focused state.
- Refreshed project memory/docs to current state: English-only working copy, Supabase-backed public forms, unified Admin Center Message Center, optional message attachments, and current Supabase SQL setup requirements.
- Added `drop function if exists public.list_portal_messages();` before recreating the RPC in `supabase/portal-role-setup.sql` so Supabase accepts the expanded message-center return columns for source/page/attachment metadata.

## 2026-07-02

- Removed remaining Netlify Forms wiring from Contact, Apps consultation, and App Checkout. Added `assets/site-message-forms.js`, Supabase `submit_site_message()`, private message-attachment storage, and Admin Center attachment-link rendering so public form submissions land in the Message Center.
- Rewired account-dashboard support/change requests away from Netlify Forms. `account.html` now calls `submit_portal_message()` into Supabase `client_messages`, and `account-admin.html` now includes a Message Center tab backed by `list_portal_messages()` / `delete_portal_message()`.
- Added a Copy emails tool to the Admin Center Subscribers table. It opens a modal with Line list, Comma list, and CSV format options, shows the formatted emails in a large scrollable preview, and includes a Quick copy action with a selected-text fallback when the browser blocks clipboard writes.
- Fixed the embedded Admin Center Subscribers tab so it uses the same subscriber source as the standalone subscribers page. Later updates separated Subscribers from Account holders completely, so account-holder fallback rows no longer appear in Subscribers.
- Added Delete actions to Admin Center Subscribers and Account holders tables. Each action opens a confirmation modal before calling owner-only Supabase RPCs (`delete_newsletter_subscriber()` / `delete_portal_account_holder()`); the account delete flow blocks deleting the signed-in owner account and cleans related portal rows where those tables exist. Updated `homepage_email_signups` to be the connected subscribers source and added an account-email backfill in `portal-role-setup.sql`.
- Expanded Admin Center for ultra-wide desktop use and changed side tabs to open their working tables directly. The Subscribers tab now renders the newsletter subscriber table in-place, and the Account holders tab auto-loads its table without a secondary load button.
- Added homepage desktop/mobile newsletter signup fields and `assets/newsletter-signup.js`, wired to insert public submissions into Supabase `homepage_email_signups`. Updated RLS setup permits public inserts while keeping subscriber reads owner-only.
- Changed the Admin Center Users area to Subscribers. `account-users.html` now presents a newsletter subscriber table only, de-dupes by email, treats portal account signups as newsletter subscribers, and `supabase/portal-role-setup.sql` now creates `homepage_email_signups` plus mirrors new Auth signups into it.
- Reorganized `account-admin.html` into a left-anchored tab workspace. Overview, Subscribers, Account holders, and Sean Ads now live in separate tab panels with ARIA state, hash-aware navigation, and a mobile scrollable tab strip.
- Added an owner-only `account-users.html` subscriber table page linked from Admin Center. It lists account signups as subscribers through the existing Supabase account-holder source and reserves a `homepage_email_signups` source for the upcoming homepage email subscription capture flow.
- Split owner-only portal tools into a separate `account-admin.html` page. `account.html` now stays visually aligned with regular client dashboards except for an owner-only Admin Center link.
- Moved owner-only portal tools behind an "Admin Center" card on `account.html`; super-admin accounts now see a regular client dashboard until opening Admin Center, where account-holder review and owner shortcuts live.
- Switched canonical production references from `darkmatterwebdev.com` to `surettesystems.com` across site metadata, Supabase auth redirect config, snippets, and project memory. Verified `account.html` preview advertises `https://surettesystems.com/account.html`.
- Added a canonical `siteUrl` to `assets/supabase-config.js` and changed portal auth email redirects to use it, preventing password reset emails requested from local preview from linking back to localhost / `127.0.0.1`.
- Moved the `account.html` forgot-password button handling into `assets/client-portal.js`, reusing the configured Supabase client and shared status UI instead of an inline handler.
- Confirmed local preview at `http://127.0.0.1:4173/`, checked `account.html` for console errors, and ran `node --check` on the edited JS files. `scripts/validate-site.ps1` still fails only on documented pre-existing validator noise.
- Fixed the mobile header account control so it remains icon-only in signed-in state. `assets/account-nav.js` and the portal nav updater now keep `.dm-mob-acct` visually as the icon while preserving accessible `aria-label`/`title` text.
- Cache-busted `account-nav.js` references to `v=20260702-mobile-account-icon` across HTML pages. Verified `apps.html` mobile preview loads the updated script and renders the header account control at icon-only width with no visible "Account" text.
- Smoothed mobile homepage scrolling for the Nova particle background by compositing the fixed `#nova-bg`/canvas layer, using stable viewport sizing, and ignoring height-only mobile toolbar resize events so WebGL does not reallocate mid-scroll.
- Verified the homepage at a 390x844 mobile viewport: `#nova-bg` and its canvas remained 390x844 before and after scroll, with no browser console errors.
- Ran `scripts/validate-site.ps1`; it still reports pre-existing issues including missing `es/` mirrors, node_modules HTML scans, stale Sean Ads Spanish links, and mojibake warnings.

## 2026-06-27 (mobile Services tab black-screen fix)

- Fixed the mobile bottom-nav **Services** tab blanking the entire page to black on tap. Root cause: the tab's `click` bubbled to document-level handlers (the hamburger-menu close handler in `index.html`, plus the Nova Three.js/OrbitControls pointer listeners) in the *same* event that opened the `.mobile-services-popout`; that combination corrupted the Nova WebGL compositing layer and blanked the page (persisting until reload). The popout CSS itself (incl. `backdrop-filter`) renders fine — proven by isolating a non-bubbling click, which worked. Fix: `assets/mobile-services-nav.js` now calls `event.stopPropagation()` in the Services-tab click handler so the popout-toggle click never reaches those global handlers.
- Also fixed a latent 404 in the same popout: "Custom Business Web Apps" / "Aplicaciones Web Empresariales" linked to `services/custom-development.html` (does not exist) — now points to `apps.html`, matching the desktop nav.
- Cache-bust: bumped `mobile-services-nav.js?v=20260609-services-trim` → `?v=20260627-blackfix` across all 26 referencing HTML pages so users get the fixed script.
- `index.html` — changed the mobile bottom-nav Services tab `href` from `apps.html` (a dead duplicate of the Apps tab) to `index.html#services`, giving it a sensible no-JS fallback that scrolls to the homepage Services section. JS still intercepts it (detection matches on the "services" substring) and opens the popout; the href only matters if JS fails. Other pages' bottom Services tab still falls back to `apps.html`.
- Verified in mobile preview: real tap opens the popout with the Nova hero fully intact (no blackout), toggle-close and outside-click-close both work, popout links resolve. Validator shows only the known pre-existing noise (missing `es/` mirrors, node_modules, Sean's Ads `es/` links).

## 2026-06-22 (nav flash + page fade-in fix)

- Fixed the nav jump and flash on all pages. Root causes: `#sds-logo` / `#sds-logo-mobile` are empty placeholder divs until `surette-logo.js` inserts the 60×60 canvas at DOMContentLoaded, causing the nav to reflow; Tailwind CDN processes injected nav HTML (on `standard-site-nav.js` pages) asynchronously via MutationObserver, briefly showing unstyled classes.
- `assets/nav.css` — added `html { opacity: 0; transition: opacity 0.38s ease }` immediately after `@import`, so the page is hidden before the browser's first paint (render-blocking CSS guarantees this). Added a CSS animation fallback (`dm-page-show`, 200ms delay) scoped to `html:not(.sds-logo-loaded)` so portal/utility pages that don't load `surette-logo.js` still reveal within 200ms. Reduced-motion override included.
- `surette-logo.js` — (1) stamps `sds-logo-loaded` on `<html>` synchronously at IIFE parse time, suppressing the CSS animation for all pages that load the logo script; (2) after the DOMContentLoaded logo init, schedules the reveal via `setTimeout(0)` + 2×`requestAnimationFrame`, ensuring the canvas is painted before opacity is set to 1; (3) 850ms safety timeout ensures the page is never permanently invisible if something stalls.
- `assets/client-portal.js` — changed 4 serial `maybeQuery` awaits to `Promise.all` for parallel Supabase dashboard reads.
- `netlify.toml` — rewrote with security headers (`X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`), `Link` preconnect hints for Tailwind/fonts/jsdelivr CDN, `Cache-Control: no-store` for HTML, long-lived immutable cache for `/assets/*`, all original redirects preserved.

## 2026-06-22 (doc audit)

- Full project-docs audit against actual disk state. Updated `CURRENT_STATUS.md`, `TASKS.md`, `HANDOFF.md`, `ARCHITECTURE.md` to reflect: MetalsCalc app + portfolio entry, three new portal pages (`account-created.html`, `account-settings.html`, `account-ads-status.html`), removal of black-hole hero video references from ARCHITECTURE, correct Surette brand asset filenames, and stale antique-mall screenshot note. Updated `MEMORY.md` project memory accordingly.

## 2026-06-22

- Redesigned the `app-catalog.html` application gallery cards to match the Websites/Case Studies (`casestudies.html`) product-listing tiles. Removed the old "DepthFold" 3D `.ux-card` theme system (gradient/glass/orbit-circle CSS and markup) and replaced it with `.portfolio-tile` cards: a 16:10 screenshot media area with a numbered "0X · LIVE DEMO" badge and gradient scrim, cyan/purple tag chips, an `font-headline-md` title, a description, and a hover-reveal "Open profile →" CTA. Grid changed to `grid grid-cols-2 xl:grid-cols-3 gap-3 md:gap-gutter` (2-up mobile, 3-up desktop). The "Let's build yours today" card is the dashed `.portfolio-tile--open` tile. Card screenshots: `assets/apps/auction/auction-dashboard.webp`, `assets/apps/sdms/sdms-dashboard.webp`, `assets/apps/benji/benji-dashboard.webp`, `assets/apps/antique-mall/thirdstreet-home.png`. Added a mobile-only `.portfolio-card-title` size override (the Case Studies title-fit JS/container-query CSS was intentionally not pulled in, since its global `0.72rem` base depends on that script). Verified desktop + mobile in preview: 5 tiles, all images load, no console errors, no horizontal overflow.
- Removed the "fly" canvas hero background from `app-catalog.html`. The `assets/fly/` folder was deleted, so unwired it: removed the `.fly-background` div, the `assets/fly/fly.js` module script, the fly-specific inline CSS, and the now-unused `es-module-shims` + three.js/lil-gui `importmap` (only fly used them). Removed the `body.sds-apps-page` transparent / `::before { display:none }` overrides so the standard cosmic-web background returns, and tightened `main.catalog-main` top padding from `pt-[46vh]` (room for the old canvas hero) to `pt-6 md:pt-10` so the gallery sits just below "Back to Apps". Verified in preview: no fly/three/importmap requests, cosmic-web `body::before` active, 5 gallery tiles intact, no console errors. The animated SuretteLogo nav badge canvas was intentionally kept (user confirmed no other canvas objects should be removed).

## 2026-06-21

- Made the Websites page "Back to Website Design" link referrer-aware: it is hidden on direct visits and only appears when the visitor came from `services/website-design.html`.
- Recaptured the NaplesEstateJewelry.co homepage screenshot after the live homepage update and refreshed the portfolio tile asset.
- Refreshed the NaplesEstateJewelry.co Websites listing and detail page from the live site, replacing stale WebP screenshot references with current PNG captures for the homepage tile, shop catalog, product value panel, estate evaluation page, and auction guidance page.
- Matched the Websites page desktop nav active state to the homepage: the `Websites` link is now cyan, underlined, and marked with `aria-current="page"` on `casestudies.html`.
- Changed the AuctionBuddha closer-look screenshot cards to show full contained screenshots with smaller text below, instead of cropped fixed-height previews with oversized lower text panels.
- Removed the AuctionBuddha detail page's full-page mini previews block after one preview rendered poorly, leaving the closer-look screenshots and feature cards.
- Added AuctionBuddha.com to the Websites gallery with desktop/mobile tiles, created `portfolio-auctionbuddha.html`, and captured public screenshots into `assets/portfolio/auction-buddha/` for the homepage, store, auctions, vendor/seller access, login, and mobile home.
- Renamed the case studies page hero and metadata to "Websites We've Built" to match the new `Websites` navigation label.
- Changed the `Websites` header nav item back to the standard plain nav-link treatment so it matches the rest of the header menu on all pages.
- Replaced the top-level `Process` header/bottom-nav item with a green outlined `Websites` link to `casestudies.html` across shared and hand-authored navigation, while adding `View Our Process` CTAs to Website Design, Managed Hosting, Website Care Plans, Custom Business Web Apps, In-Home Tech Services, and Office Network Setup.
- Removed the lower "Demo Access" and "Get Started" CTA blocks from the Antique Mall / Third Street profile page so the profile ends after the custom-version content and shared footer.
- Refreshed `antique-mall-vendor-management-system-and-auction-platform.html` around the live ThirdStreetAuctions.com app: updated CTAs to the production URL, replaced screenshot cards with seven new public captures (home, shop, shops, auctions, listing detail, auction detail, login), and updated captions/features for vendor shops, filters, carts, accounts, and auction lots.
- Fixed the Antique Mall profile marketplace-flow visual so its Vendors, Listings, Browse, Auctions, and Accounts cards use a responsive CSS grid instead of overlapping absolute positioning.
- Updated the Antique Mall card in `app-catalog.html` to present the current Third Street Auction Marketplace positioning.

## 2026-06-18

- Removed the homepage "Do Not Press" widget and its linked page. Deleted the desktop and mobile `.sds-home-widget--right` buttons from `index.html` (they linked to `face-interactive.html`), removed the now-unused `.sds-home-widget--right` CSS variant from `assets/surette-data-systems-home-widget.css`, and deleted the root `face-interactive.html` page. The `assets/face/` source was already removed. Verified no remaining references repo-wide; the bottom-left "View our business software" widget is unaffected.
- Anchored the homepage hero darkening tint. Because the Nova background is `position:fixed` but each hero's gradient overlay was `position:absolute` (inside the hero section), the tint scrolled away with the hero and left the rest of the page over full-brightness particles. Removed both per-hero `absolute` overlays and added one fixed, viewport-anchored scrim (`#hero-tint`) as a sibling of `#nova-bg` at `z-index:2` — above the Nova background (`z-1`) and below all page content (`main` is `z-10`). The tint now stays put across the whole page while text scrolls freely over it, keeping content readable over the particle field. Verified on desktop and mobile.
- Replaced the homepage (`index.html`) black-hole Three.js hero background with the self-contained Nova particle-galaxy widget from `nova/nova-widget.html`. Dropped in the widget's `<script type="module">` (CONFIG + engine) verbatim and mounted it via a new `#nova-bg` div in the same fixed full-viewport hero slot (`z-index:1`) the black hole used. Removed the old `#singularity-hero-bg` div and its driving module script. The widget loads Three.js 0.136 from `esm.sh`, so it does not use the page importmap. Verified in preview on desktop and mobile: Nova canvas mounts and renders, no console errors, content/nav layer cleanly on top.

## 2026-06-14

- Documented the portal Supabase separation from Naples Estate Jewelry across project memory, Supabase config comments, and setup SQL; removed the stale `jewelrySiteUrl` config entry.
- Added a super-admin account-holder viewer to the client dashboard: `account.html` now has a "View all site account holders" button, `assets/client-portal.js` loads and renders account rows, `assets/client-portal.css` styles the responsive admin table, and `supabase/portal-role-setup.sql` defines the super-admin-only `list_portal_account_holders()` RPC plus an Auth signup trigger that seeds `client_profiles`.
- Added Antique Mall Vendor Management System and Auction Platform to `app-catalog.html` with a matching 3D catalog card and new standalone profile page.
- Captured six screenshots from `https://antique-shop-naples.netlify.app/` into `assets/apps/antique-mall/` covering homepage, vendors, auctions, listing detail, auction detail, and gated seller login.
- Verified the new catalog/profile pages and screenshot assets return `200` locally; the screenshot modal opens and loads images. Ran `scripts/validate-site.ps1`; it still fails on broader repo issues plus the expected missing Spanish mirror for the new English-only profile.
- Removed the defunct English/Spanish navigation toggle from the individual app detail pages: Auction House & Consignment Store Software, Secondhand Dealer Management System, and Benji Payroll Management System. The shared standard nav now renders language UI only when a page provides a real alternate URL.
- Started a local static preview at `http://127.0.0.1:4173/` and verified the Auction app detail page has no `.lang-switch` elements or stale `es/` links in the live nav.
- Ran `scripts/validate-site.ps1`; it still reports broader pre-existing issues in this working copy, including missing `es/` mirrors, old Sean Ads Spanish links, `node_modules` HTML scans, and mojibake warnings.

## 2026-06-08

- Stabilized the Surette Data Systems physics wordmark hover-reattach: the shake handoff (`handoffCharToGsapShakeAnchor`) now clears only filter/opacity/color leftovers and sets the shake transform + transform-origin deterministically, instead of clearing the transform in the same `gsap.set` and rebuilding it via a re-measure/compensate. This removes a one-frame jump/origin-snap that could surface when a main letter was activated again after a full fall + rematerialize cycle. Bumped the `apps.html` / `es/apps.html` wordmark script cache key to `20260608-reattach-stable`.
- Increased the mobile Surette hero block-letter spacing slightly.
- Added subtle spacing between the Surette hero block-letter groups.
- Fixed mobile apps hero overlap between the "Workflow software..." subtext and Software Catalog.
- Changed the apps hero lead copy to white and removed the `DATA` / `SYSTEMS` vertical offsets so the subtitle aligns cleanly.
- Tuned the mobile apps hero Surette block field: more vertical room, smaller mark, and a gentler right-side bias.
- Pushed the desktop apps hero Surette block drift much farther right without horizontal overflow.
- Extended the desktop apps hero Surette block drift farther right and slightly reduced its lower travel.
- Added a right-biased mobile drift path for the apps hero Surette block and slightly limited the desktop lower travel range.
- Adjusted the desktop apps hero Surette block drift field slightly downward after review.
- Lifted the desktop apps hero Surette block mark drift field higher without adding rightward anchoring.
- Changed the apps hero `DATA SYSTEMS` subtitle/rules to white on desktop and tightened the gap before Software Catalog on desktop/mobile.
- Changed the mobile apps hero `DATA SYSTEMS` subtitle and side rules to white.
- Expanded the apps hero Surette block mark drift on desktop and mobile, layering text above the drifting visual and clipping mobile overflow to prevent horizontal scroll.
- Changed the apps hero orange/purple Surette block mark to a slow zero-gravity drift animation with desktop/mobile containment checks.
- Added `assets/surette-data-systems-floating-orange-blocks.webp` and placed it as a large floating visual beside the Surette Data Systems apps hero wordmark.
- Added `assets/case-studies-building-blocks.webp` and placed it as a large floating hero visual on English/Spanish case studies pages with updated building-blocks positioning copy.
- Added compact typography and mobile hero callout overrides for Surette Data Systems app profile pages so hero and section headings take less vertical space without changing fonts or colorways.
- Added mobile-only "more app details below" cues under app profile hero buttons so phone visitors can tell more content follows.
- Changed mobile app profile hero CTAs to a compact two-column button grid and verified no clipping at 390px or 360px widths.
- Added `assets/surette-data-systems-geometric-icon.webp` and replaced white-square Surette icon usage in home widgets, app credits, app pricing footer, and app profile footers.
- Adjusted the apps-page Surette `DATA SYSTEMS` subtitle so `DATA`, `SYSTEMS`, and the side rules align more deliberately.
- Replaced the previous floating Surette icon with `assets/surette-data-systems-geometric-icon.webp` site-wide and added the small mark above the apps-page company kicker.
- Updated the homepage Surette Data Systems widget copy to "View our business software."
- Changed Auction and SDMS app detail CTAs from "Request Live Login" to subscription portal links.
- Removed the small pulsing purple dot from the mobile Contact header in English and Spanish.
- Removed the top-left tile from the Surette hero `S` so the mosaic letter reads less like a `5`.
- Kept the Surette Data Systems hero intro glow on during the full letter-by-letter fall sequence, turning it off only after the first run completes.
- Added a Surette Data Systems hero intro sequence: full-word glow, then left-to-right letter fall/materialize before hover activation.
- Updated Surette Data Systems Apps hero and meta copy with business-owner search terms including CRM, ERP, inventory management, workflow automation, dashboards, reporting, and operations software.
- Added a Surette Data Systems app-page animation lock so each started hero wordmark hover animation finishes even if the user moves the pointer away.
- Rebalanced the Surette Data Systems hero hover timing so the rumble continues into a slower, more gradual off-page fall.
- Slowed the Surette Data Systems hero hover fall and added independent shake, bounce, and tumble stages before the blocks leave the page.
- Changed the Surette Data Systems hero materialize hover effect to run once per hover instead of looping continuously.
- Extended the Surette Data Systems hero wordmark hover so blocks fall off-page, vanish briefly, and materialize back into each hovered letter.
- Changed the Surette Data Systems hero wordmark hover from a jumble loop to a staggered block-fall interaction per letter.
- Smoothed the Surette Data Systems hero wordmark hover so block letters start slowly and build into faster jumbled motion.
- Removed the faint pseudo-text layer behind the Surette Data Systems hero mosaic wordmark so only the primary block letters render.
- Replaced the app-library raster hero wordmark with a responsive inline SVG/CSS Surette Data Systems widget whose icon and mosaic block letters animate independently on hover.
- Rebranded the app/software line to Surette Data Systems across English/Spanish app pages, app pricing, home widgets, app profile credits, and app footers.
- Added lossless WebP brand assets: `assets/surette-data-systems-logo.webp` and `assets/surette-data-systems-icon.webp`; removed the stale former-brand SVG.
- Renamed app-brand CSS assets to `surette-data-systems-*` filenames.

## 2026-06-05

- Wired portal privileged access to Supabase `app_metadata.role` via `assets/portal-auth.js`, with email allowlist fallback and shared Sean dashboard gating in `assets/seans-ads-dashboard.js`.
- Added `supabase/portal-role-setup.sql` for future RLS role helpers.
- Compacted project markdown memory for faster AI handoff and added an explicit startup trigger for `analyze this project's files`.
- Trimmed old feature docs into short pointers and removed stale markdown-only leftovers, including the imported Elite Yacht `DESIGN.md` and the old meeting note.
- Removed unreferenced root screenshot leftovers: `sean-check-final.png`, `sean-check-matte_loose.png`, `sean-check-matte_soft.png`, and `sean-check-plain.png`.
- Ran a phone-width mobile layout sweep, fixed app pricing question-button containment, kept pricing tiers two-wide on mobile, and patched targeted overflow on Built By, Preference Builder, SeansAds portfolio, and SDMS profile pages.
- Made app pricing tier cards clickable through the client portal to the selected app checkout context.
- Shortened Apps hero copy and added soft translucent color backing to app gallery tiles.
- Added app-aware pricing return links and pricing CTAs from Auction and SDMS app profiles.
- Slimmed the app pricing page to a compact banner plus immediate tiers and removed public benchmark links.
- Updated Auction and SDMS app profile pages with WebP screenshots, modal preview tiles, pricing/custom/demo CTA rows, language callouts, powered-by banner, and footer.
- Added/updated portal-only checkout pages and safe `next` redirects for app checkout.
- Added browser-side owner/Sean allowlists for portal UI access; removed the temporary `admin` / `admin` bypass.

## 2026-06-04

- Renamed the app library from Downloads to Apps, including `apps.html`, Spanish mirror, nav labels, and redirects from old Downloads URLs.
- Reworked Apps and Portfolio pages into gallery-first layouts with standalone detail pages.
- Added SDMS as a second Dark Matter app listing and full app profile.
- Added Auction House & Consignment Store Software profile pages and live demo/request CTA flow.
- Added powered-by/footer shells to app pages.
- Restored legitimate English/Spanish question marks and fixed malformed Spanish `textarea` tags.
- Documented that Sean's Google Ads source moved out of this repo and is managed separately.

## Earlier Milestones

- Built the static bilingual Dark Matter Surette Systems Portal site.
- Added Services, Process, Contact, Built By, Portfolio, Apps, Website Preference Builder, and Client Portal surfaces.
- Added Supabase starter config/schema for the portal.
- Added homepage hero media and shared cosmic-web visual system.
