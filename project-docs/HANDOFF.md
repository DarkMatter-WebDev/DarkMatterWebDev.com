# Handoff

Last updated: 2026-07-21

## Startup Prompt

If the user says `analyze this project's files`, start by reading:

1. `AGENTS.md`
2. `AI_START_HERE.md`
3. `project-docs/PROJECT_OVERVIEW.md`
4. `project-docs/CURRENT_STATUS.md`
5. `project-docs/TASKS.md`
6. `project-docs/DECISIONS.md`
7. `project-docs/HANDOFF.md`

Then summarize the project in a few bullets and continue with the latest request.

## Immediate Context

- Static Dark Matter / Surette Data Systems marketing + client portal site.
- English root pages only; Spanish `es/` mirrors are absent from this working copy.
- Top-level public navigation now uses one Portfolio item (`portfolio.html`) instead of separate Apps and Websites items. `portfolio.html` links to the existing Apps (`apps.html`) and Websites (`casestudies.html`) pages.
- Public navigation has ONE source of truth (2026-07-15): every public page renders its full nav (desktop header, mobile header + hamburger dropdown, mobile bottom tab bar) from `assets/standard-site-nav.js` via a single `<script ... data-active="...">` tag near the top of `<body>`. `assets/unified-mobile-menu.js` was deleted; no page carries inline nav markup. New pages must add the loader tag (with `../assets/...` under `services/` — the loader auto-prefixes its links there), never a hand-copied nav. Valid `data-active`: `home`, `services`, `portfolio`, `apps` (→ portfolio highlight), `account`, `contact`, `none`.
- No-white-flash baseline is sitewide: HTML pages include `#dm-critical-dark-baseline` in `<head>`, `assets/nav.css` anchors dark document/app-root backgrounds, visual shells stay transparent for animated/cosmic backgrounds, and public pages use `theme-color="#050505"`.
- Homepage Nova WebGL background is scroll-smoothed for mobile in `index.html`: the fixed `#nova-bg`/canvas layer uses stable viewport sizing and resize logic ignores height-only mobile toolbar changes.
- Mobile header account control is intentionally icon-only. `assets/account-nav.js` keeps `.dm-mob-acct` visually as the icon while updating `aria-label`/`title` for signed-in state. The mobile icon renders grey when signed out and cyan/blue when signed in.
- Service pages have separate desktop and mobile markup paths; mobile now includes the fixed Surette logo/header, account icon injection target, promoted top nav row with working Services popout, and compact `.service-mobile-footer` inside each page's `md:hidden` wrapper.
- Service-page fixed top headers are anchored through shared `assets/nav.css` so the padded mobile service wrapper cannot shift the header/menu row to the right.
- App/software brand is **Surette Data Systems**. **Six** app profiles: Auction House, SDMS, Benji Payroll, Antique Mall (Third Street Auctions), MetalsCalc, MetalsPredictor. (The docs said five until 2026-07-16 — MetalsPredictor had been added without them being updated.)
- Websites/portfolio gallery has six entries: Naples Estate Jewelry, JP Surette, Elite Yacht Detailing, Sean's Ads, AuctionBuddha.com, MetalsCalc.com.
- Portal uses a dedicated Dark Matter / Surette Data Systems Supabase project (not Naples Estate Jewelry).
- Portal auth email redirects use `assets/supabase-config.js` `siteUrl` (`https://surettesystems.com`) as the canonical base, so local reset requests do not create localhost links.
- Homepage newsletter signup forms are wired to Supabase `homepage_email_signups`; live use requires running the updated `supabase/portal-role-setup.sql` in the hosted project.
- Public website forms and account-dashboard support/change requests are dual-routed. Public forms call Supabase `submit_site_message()` through `assets/site-message-forms.js`; account requests call `submit_portal_message()`. After the Supabase save, the same forms post to Netlify Forms so Netlify can send owner notification emails. Admin Center has a Messages tab that lists/deletes them via `list_portal_messages()` / `delete_portal_message()` and can render signed links for optional photo attachments. Live use requires running the updated `supabase/portal-role-setup.sql`; the current SQL includes `drop function if exists public.list_portal_messages();` because the return shape changed to include source/page/attachment fields.
- Admin Center table deletes are wired in `account-admin.html` / `assets/account-admin.js` with a confirmation modal. Live delete actions require `delete_portal_message()`, `delete_newsletter_subscriber()`, and `delete_portal_account_holder()` from the updated `supabase/portal-role-setup.sql`.
- Admin Center Subscribers and Account holders tables use explicit column groups; name/email columns are wider, and the Subscribers source pill can wrap in a narrow column.
- Admin Center Subscribers and Account holders are independent lists. New portal account signups mirror into `homepage_email_signups`, but Subscribers reads only subscriber rows; deleting a subscriber row does not delete or re-show the account holder.
- Client profiles are intentionally minimal. Account settings and Admin Center no longer collect or display business/company name or website URL profile fields; Supabase setup drops those old columns if present.
- Local preview: `http://127.0.0.1:4173/` (start with `npx http-server` or `start-preview.bat`).
- Production: `https://surettesystems.com/`.
- Sean's Ads source is intentionally not here — do not recreate it.

## Page Inventory (root)

| Page | Purpose |
|---|---|
| `index.html` | Homepage — Nova WebGL hero, Surette Data Systems widget |
| `portfolio.html` | Portfolio hub — two "collection folder" cards linking to Apps and Websites |
| `apps.html` | Surette Data Systems software hub (interactive wordmark hero); conditional "Back to Portfolio" link |
| `app-catalog.html` | App gallery — 6 collectible cards + open slot |
| `app-pricing.html` | 4 pricing tiers routing through client portal to checkout |
| `app-checkout.html` | Portal checkout context |
| `casestudies.html` | Websites We've Built gallery — 6 collectible cards + open slot, each with a build estimate |
| `contact.html` | Contact page; public form submits to Supabase Message Center and supports optional photos/screenshots |
| `process.html` | Process page (not in main nav; reached via service-page CTAs) |
| `built-by.html` | Built By page |
| `account.html` | Client portal dashboard (Supabase auth; owner sees Admin Center link only) |
| `account-admin.html` | Owner-only ultra-wide left-tab Admin Center with Messages, Subscribers, and Account holders tabs |
| `account-users.html` | Owner-only newsletter subscriber table for account signups and homepage email sign-ups |
| `account-settings.html` | Profile/password management (Supabase gated) |
| `account-created.html` | Email confirmation landing after Supabase signup |
| `auction-house-consignment-store-software.html` | Auction app profile |
| `secondhand-dealer-management-system.html` | SDMS app profile |
| `benji-payroll-management-system.html` | Benji Payroll app profile |
| `antique-mall-vendor-management-system-and-auction-platform.html` | Antique Mall / Third Street app profile |
| `metalscalc-buying-calculator.html` | MetalsCalc app profile (PWA, precious-metal buying calc) |
| `portfolio-naplesestatejewelry.html` | Naples Estate Jewelry detail |
| `portfolio-jpsurette.html` | JP Surette detail |
| `portfolio-eliteyachtdetailing.html` | Elite Yacht detail |
| `portfolio-seansads.html` | Sean's Ads detail |
| `portfolio-auctionbuddha.html` | AuctionBuddha.com detail |
| `portfolio-metalscalc.html` | MetalsCalc.com detail |

Services pages in `services/`: `website-design-hosting.html` (merged Website Design + Managed Hosting + Website Care Plans; now the complete public pricing page — content spec lives in `website_pricing_plan.txt` at the project root and is the source of truth for all plan names, prices, and policies), `in-home-services.html`, `office-network-setup.html`.

## Most Recent Work

- **Choppy contact-page load fixed (2026-07-21, latest).** Owner reported the pricing page's "Schedule a Free Consultation" CTA landing choppy on contact.html (the CTA is a plain link — the problem was the destination's load). Root causes, both landing inside the reveal-animation window: **(1)** `assets/site-message-forms.js` statically imported all of supabase-js v2 from jsDelivr at module top-level — the fetch+parse+execute hit at DOMContentLoaded, concurrent with the 0.38s root fade and the object-reveal of contact's `backdrop-filter: blur(20px)` glass cards (the whole form panel is one). Refactored to `loadSupabase()`: dynamic import on first submit, failed imports clear the cached promise so retries work, `uploadAttachments`/RPC take the client as a parameter, and a post-`load` `requestIdleCallback` (setTimeout fallback) warm-up keeps first submits fast. Measured after: supabase-js fetch starts after the load event instead of at DCL. **Do not reintroduce a top-level supabase import in this file** (noted in CURRENT_STATUS too). **(2)** `surette-logo.js`'s scheduled reveal now waits on `document.fonts.ready` raced against a 500ms cap before its two rAFs — `display=swap` fonts were re-laying-out text mid-fade; cached fonts resolve instantly; the 850ms hard safety is untouched. Verified: contact (desktop+mobile), apps consultation, and app-checkout forms all bind on `v=20260721-lazy-supabase`; zero console errors; pricing page reveals fully on the gated path. Validator: 65 baseline. → `site-message-forms.js?v=20260721-lazy-supabase` (3 pages), `surette-logo.js?v=20260721-fonts-gate` (28 pages). Note: a real form submission wasn't test-fired (would post to Supabase + Netlify) — worth one production submit test after deploy.

- **Double-blink page load fixed + wordmark knock-loose removed everywhere (2026-07-21).** Owner reported apps + the pricing page "blink twice before loading." Mechanism: nav.css hides `html` from first paint and reveals un-stamped pages via a 0.2s CSS fallback (`html:not(.sds-logo-loaded) { animation: dm-page-show 0s 0.2s forwards }` — meant for portal pages that never load surette-logo.js); the stamp lived in `surette-logo.js` at the bottom of the body, so on pages that take >200ms to parse (pricing = 2,500 lines; apps = module scripts), content flashed in at 0.2s, vanished when the late stamp made the `:not()` rule stop matching, then faded in via the JS reveal. **Fix: inline head script stamps the class before first paint on all 28 surette-logo.js pages** (sed-inserted just above `#dm-critical-dark-baseline`; verified exactly one per page). **New-page rule recorded in TASKS.** Apps.html second race: the loader's 50ms slow path vs the html fade — fixed with a head CSS pre-hide (`body:not(.is-apps-ready) > :not(#sds-apps-loader) { visibility: hidden }` + `@keyframes dm-apps-unhide` 5s CSS-only safety; loader's own JS cap is 4s). **Knock-loose now removed at every width** (was mobile-only from earlier the same day): `#wm-canvas { pointer-events: none !important }` in base CSS; the rect-corrected pointer/click/touch re-registrations in apps.html's inline script deleted as dead code. Load intro unaffected (frame-driven); terms.html's wordmark interactions untouched (page-scoped kill). Verified: stamp order + count on all 28 pages, both pages reach full reveal with zero console errors, canvas hit-tests fall through to the `h1` at 414px and 1280px. Validator: 65 baseline. Note: end-state verification only in the pane — the offscreen-iframe timeline probe was useless (hidden iframes get timers throttled to nothing on top of the pane's frozen rAF); the fix is correct-by-construction (both hide rules exist in `<head>` before any content can paint).

- **Background-canvas scroll stutter fixed site-wide (2026-07-21).** Owner reported the apps page's background canvas stuttering during mobile scroll. Root cause: `fractal.js` — the Three.js attractor background shared by `apps.html`, `process.html`, and `account.html` — called `renderer.setSize()` on every window resize, and mobile browser chrome fires height-only resizes mid-scroll (toolbar collapse), so the WebGL buffer reallocated while scrolling. Identical mechanism to the homepage Nova stutter, so it got the identical two-part fix: **(1)** `fractal.js`'s handler is debounced 180ms and skips height-only changes on mobile viewports (<880px); width/DPR changes still resize and pick up any accumulated height delta — mirrors `shouldResizeNova()` in `index.html`; **(2)** `#canvas-container` and its canvas on all three pages now carry the `#nova-bg` compositing treatment: `transform: translate3d(...)`, `backface-visibility: hidden`, `contain: strict` (the centering transforms became `translate3d(-50%,-50%,0)`, including the mobile `!important` copies; account.html also has a 1279px override — updated). **Same-class fix in `assets/surette-physics-wordmark-canvas.js`** (loaded by `apps.html` and `terms.html`): `_resize()` reallocated `canvas.width` on every resize event; a same-value set still blanks the bitmap (wordmark blink mid-scroll); now early-returns when width/height/DPR are all unchanged — behavior-preserving, initial call always proceeds (`_W` starts 0). **Canvas audit:** index Nova already fixed (pattern source); nav logo canvas (`surette-logo.js`) has no resize listener; `nova/nova-widget.html`, `pokecard-dropin/app-template/*`, and the wordmark snippet files are vendor/reference, not linked pages. Verified with instrumented `canvas.width` setter counters on real pane resizes: height-only (812→700 apps, 700→600 process) = **0 buffer writes** on every canvas; width (375→414) = one debounced resize to exact new dims (fractal shows 2 writes — setPixelRatio + setSize, one event); account.html confirmed on new script + CSS. New decision recorded ("Fixed Background Canvases..."). → `fractal.js?v=20260721-scroll-stutter` (3 pages), `surette-physics-wordmark-canvas.js?v=20260721-resize-guard` (2 pages). Validator: 65 baseline.

- **Nav-bar hairline borders pinned in plain CSS (2026-07-21).** The three fixed bars' 1px hairlines were Tailwind utilities on injected markup (`border-white/10` desktop nav, `border-starlight-white/10` mobile header + bottom tab bar) riding the CDN's DOM observer — which missed on portfolio.html, leaving Tailwind's preflight fallback `#e5e7eb`: a solid light-gray line under the mobile header and above the bottom tab bar (read as "on the footer"). Now pinned in `nav.css` directly below the bar-tint rule: `border-bottom` on `.dm-standard-desktop-nav`/`.dm-unified-mobile-header`, `border-top` on `.dm-standard-bottom-nav`, all `rgba(255,255,255,0.1) !important` (the utilities' intended value; `starlight-white` = #FFFFFF). Utility classes left in the generator; the pin wins everywhere. Verified by computed style on all 28 public pages at 375px — only portfolio.html had been failing; all now identical. Third confirmed instance of the injected-markup Tailwind failure (DECISIONS amended). → `nav.css?v=20260721-nav-border-pin` (all 33 referencing pages).

- **Apps wordmark: no knock-loose interaction on mobile; hero scroll dead-zone fixed (2026-07-21).** Owner request. One rule in `apps.html`'s existing `≤879.98px` block: `#wm-canvas { pointer-events: none !important; }`. Removes every input path at once — per-frame hover drops, tap releases, and the engine's own `dblclick` release-all (never unbound by the page's corrected-handler block; a double-tap would scatter the whole wordmark). Load intro is frame-driven and unaffected; ≥880px keeps full interaction. **Side effect that mattered:** the engine sets inline `touch-action: none` + `preventDefault()` on the canvas, which extends 280px past the wordmark on mobile (bounce floor) — covering the top ~430px of the first viewport, so swipes starting there scrolled nothing ("page unscrollable at first"). With the canvas out of hit-testing, touches fall through and scroll. Verified: 7 probe points down the first 375px viewport all reach normal content, zero `touch-action` blockers in any ancestor chain. The apps loader's `body.is-apps-loading { overflow: hidden }` is separate and intentionally kept — it locks only while the full-screen loader overlay is visible (≤~1.2s first visit while fonts gate, 4s hard cap, instant reveal + no lock on repeat visits).

- **Mobile dropdown contact dedupe + site-wide menu audit (2026-07-21).** The hamburger dropdown emitted two `contact.html` links stacked — plain "Contact" row + gradient "Contact Us" CTA. Plain row removed from `standard-site-nav.js` (one edit, all 28 pages); gradient CTA kept because it mirrors the desktop header's pattern and the bottom tab bar's Contact item retains the active-page highlight for contact.html (the dropdown shows no highlighted item there now, by design). Audited every injected menu surface for duplicate destinations within the same menu — desktop nav (9 links), dropdown (8), bottom bar (4), mobile services popout (4), wide footer (10), phone footer (7) — on a root page and a `services/` page (`../` prefix path): zero duplicates anywhere; the dropdown pair was the only one. → `standard-site-nav.js?v=20260721-contact-dedupe` (28 pages).

- **Preview-pane verification notes confirmed this session (2026-07-21):** `requestAnimationFrame` never fires in the pane (measured: 0 callbacks in 3s) — the wordmark/fractal canvases stay blank and screenshots time out even on the homepage; verify by computed style, geometry, and hit-testing instead. And because the site sets `scroll-behavior: smooth`, programmatic scroll tests (`scrollTop = x`, `window.scrollTo(0, x)`) animate on rAF and read 0 in the pane — **use `scrollTo({top: x, behavior: 'instant'})` when testing scroll**. Browser-tab HTML caching also bites: after editing a page or a versioned asset, force `window.location.reload()` in the tab before re-measuring (two stale-read incidents this session).

- **Mobile gallery card polish session (2026-07-17, after the zigzag below).** Owner iterated on the phone cards over several rounds; all CSS, no markup forks. (1) Stat pill resized to 70% (50% first — too small to read); dimensions only, never `transform: scale()`, which would re-promote the pill to a compositing layer (the flip-bleed mechanism). (2) Card titles 0.9→0.7rem and front badges 0.58→0.46rem with tightened padding/border, mobile grid only. (3) Back-face body text enlarged in two passes to `p` 0.68rem — first pass was too timid because **`scrollHeight === clientHeight` cannot detect empty space** (`.poke-links`' `margin-top: auto` stretches the column; measure true slack = clientHeight − sum of children+gaps+padding instead; AuctionBuddha is the real ceiling, ~12px slack at 320px). (4) Back titles up to 1.15rem base / 0.95rem narrow tier with `overflow-wrap: anywhere` — mandatory, domains are unbreakable tokens and would otherwise overflow the back panel HORIZONTALLY; **check `scrollWidth` too when verifying backs**. (5) Fronts decluttered: stage line + foot status tag `display: none` on the mobile grid (chip already numbers the card; status wrapped to 3 lines), buttons full-width flex:1, freed height flows to the art (138–154px at 375px); front rules added to the shared `@container` narrow tier so 320px cards come off the 64px art floor. (6) **Template hero widget reformatted to match — and its back was scrolling (148px over at 320px)**: it's not a `.tcg-card`, so no gallery protection ever covered it; mirrored rules under `.md\:hidden .lab-poke-section` in `portfolio-mobile-fixes.css`. Any future non-tcg `.poke-scene` needs its own no-scroll audit. Also fixed `â†’` mojibake in all three template-widget copies. Desktop grid + app-catalog untouched throughout (verified each round). Current versions: `portfolio-mobile-fixes.css?v=20260717-template-mobile`, `collectible-card.css?v=20260717-back-h3-up`. Validator stayed at the 65 baseline all session.

- **Phone gallery zigzag + a container-query fix for the never-scroll rule it broke (2026-07-17).** `casestudies.html`'s `.md:hidden` grid only (desktop and `app-catalog.html` untouched): each of the 6 cards now sits in a `.tcg-mobile-row`/`.tcg-mobile-num` wrapper (`assets/portfolio-mobile-fixes.css`) that shrinks the card (`width: calc(100% - 2.7rem)`, base `max-width: 340px` left alone so it only bites below ~423px viewport) and staggers it — odd cards (1/3/5) right with the number chip left, even cards (2/4/6) left with the chip right — the chip picking up the card's own `--tcg-face` colour. **Caught before shipping**: shrinking the card shrinks its back face too (fixed aspect ratio, one 3D object), which reopened the zero-slack "never scroll" budget from the day before — up to 155px of overflow at a 320px viewport. Fixed properly, not patched: the back-face tightening in `assets/collectible-card.css` moved from a `<375px` **viewport** media query to a `@container tcg-card (max-width: 336px)` **container** query (`container-type: inline-size` added to `.poke-scene.tcg-card`), because card width no longer tracks viewport width 1:1 once the zigzag can shrink it independently — a viewport query would tighten the wrong cards. One tier, not two (a two-tier attempt left a real gap right above the aggressive tier's cutoff — see the comment above the rule in `assets/collectible-card.css`). Re-verified `scrollHeight === clientHeight` on all 12 casestudies backs and all 6 app-catalog backs across the full mobile width range, not just 320/375/1440 — this class of bug specifically hides at the tier boundaries. **If you touch either the zigzag's reserved chip space or any back copy again, re-run that full sweep, not just the two endpoints.** → `portfolio-mobile-fixes.css?v=20260716-mobile-stagger` (7 pages), `collectible-card.css?v=20260716-container-fit3` (2 pages).

- **Portfolio hub category cards = "collection folders" (2026-07-16).** `portfolio.html`'s two cards share the galleries' trading-card language (paper/ink/hue frame/hard shadow/VCR mono) but are a distinct object: folder tab, landscape, fanned card-stack behind the screenshot, **no flip/tilt/JS** — plain links with page-scoped `.folio-set*` CSS. Teal folder = apps set, gold folder = websites set. `vcr-osd-mono` @font-faced straight from `pokecard-dropin/pokecard/fonts/` (don't load the widget CSS for this). Absolute positioning is safe on this page (no 3D context — the in-flow rule is for the gallery card faces). Hub stats fixed to 6 apps.

- **Both galleries are collectible cards; the card is now a shared component (2026-07-16).** `app-catalog.html` joined `casestudies.html`: its 6 app tiles are now themed cards. Because two pages needed it, the card CSS moved out of `casestudies.html`'s inline `<style>` into **`assets/collectible-card.css`** (prefix renamed `cs-` → **`tcg-`**, since "cs" reads as case-study on the Apps page; `.cs-price-pill` → `.tcg-stat-pill`, as it holds an estimate on Websites but a delivery type on Apps). A page using it must also load `pokecard-dropin/pokecard/pokecard.css` + `.../pokecard.js`. **Apps have their own palette** (`.tcg-card--app-*`) kept distinct from the Websites hues. app-catalog is a single responsive layout (no desktop/phone duplication) — 1-up phone / 2-up md / 3-up xl. Its app cards have one foot button (Open Profile); demos are linked from the profile pages.

- **AuctionBuddha repriced to $35,000–$55,000+ and re-described (2026-07-16).** It had been sold short as an "auction marketplace website" at $15K–$25K. It is really a **localized eBay for one vendor mall**: every vendor opens their own mini storefront inside the mall, can run an immediate timed auction on their lots or schedule them into the store's end-of-month house auction, all on a full admin center with messaging, label printing, and integration/control of in-store operations — the online catalog and the shop floor run off the same records. The public site shows the tip of it, which is exactly why it was underpriced. Five surfaces updated (desktop card stat + front/back copy, phone tile pill + copy, and the detail page's hero, intro, and `#build-pricing`), plus a new "Under the hood" section on `portfolio-auctionbuddha.html`. Still maps to Custom Software & AI Systems (from $10,000) — a multiple of that entry point, not a new tier. (Since converted to hours: **~280–440+ hrs**; if this estimate changes again, grep `280` / `440` across `casestudies.html`, `portfolio-auctionbuddha.html`, and the docs.)

- **Websites gallery = themed collectible cards, desktop AND phone (2026-07-16).** `casestudies.html` renders six copies of the hero's Tech Business Site Template card (`pokecard-dropin`), one colour each (gold/aqua/rose/blue/violet/steel), in both layouts. **The phone grid is 1-up and must stay that way** — the card's fixed 2.5/3.5 ratio makes a 2-up phone column (226px tall) too short for the anatomy (~312px minimum). **The two grids are hand-duplicated: edit both or they drift** (static HTML on purpose — the card copy is the page's SEO content). `.portfolio-tile` now only serves the open slots (on both gallery pages); its media/img/badge/price rules were deleted from both. `assets/portfolio-mobile-fixes.css` still carries tile-era rules and may be partly dead — it is shared with the portfolio detail pages, so audit before deleting (in TASKS). Key constraints for anyone touching it: theme via the `--tcg-*` vars in **`assets/collectible-card.css`** (shared with the Apps gallery), **never edit `pokecard-dropin/`** (the hero card must stay green and the widget stays reusable — the drop-in hardcodes green in the face gradient and the top/foot tints, which is why the component overrides those three rules); `pokecard.js` auto-wires every `.poke-scene`, so extra cards need no JS; `.tcg-card .poke-name` is deliberately 0.9rem with a `<wbr/>` before each TLD, because a domain is one unbreakable token and at the widget's native 1.15rem it shoves the top-bar cue out of the `overflow:hidden` plate (Naples clipped by 62.5px) or runs underneath it; and **any overlay on a face must be IN-FLOW** (per `.tcg-stat-pill`: static, no z-index/transform/will-change/transitions, placed over the art via the zero-height `.tcg-stat-row` + negative margin) — positioned/promoted overlays painted mirrored through flipped cards and flickered mid-rotation on real devices across three round-trips, while in-flow face content (the badges) never glitched. Related: `.tcg-card .poke-art` is forced `position: static`, because the drop-in's `relative` made the image paint above all in-flow content and hid the pill. Heads-up: the preview pane stops returning screenshots with all nine `.poke-scene`s live (3D transforms + `mix-blend-mode` glare) — verify by computed-style/geometry measurement instead.

- **Websites gallery cards redesigned (2026-07-16).** `casestudies.html`'s `.portfolio-tile` cards had a 3% white background, a 10% border, and no resting shadow, so they read as translucent panes over the cosmic background rather than cards — the "messy/confusing" complaint. They are now a near-opaque gradient surface with a `rgba(255,255,255,0.16)` border, a resting shadow + inset top highlight, and a divider line under the media so the screenshot is visibly contained; the media gradient was lightened since the border now draws the boundary. **The badge/price chips are deliberately an opaque dark base with the accent only in text/border** — they sit on the screenshots, and measuring the real pixels behind them showed accent-tinted chips hitting 1.8:1 on the near-white Naples image (4 of 6 price pills failed AA). Worst case is now 11.19:1. Don't revert them to a translucent accent fill, and if the media gradient is ever darkened/lightened again, re-check the chips. `app-catalog.html` keeps its own copy of the `.portfolio-tile` CSS and was **not** updated, so the two galleries currently diverge.

- **Build-time estimates on the portfolio, in hours (2026-07-16; dollar labels replaced the same day).** Every Websites gallery card in `casestudies.html` (both grids — 12 pills) shows a `.tcg-stat-pill` hours estimate ("EST 120-160 HRS"; ASCII hyphen in pills — VCR-font en-dash coverage untrusted), backs and detail pages cite the range with the **published $125/hr shop rate** (`website_pricing_plan.txt` §37), and a rate line sits above each gallery grid. Detail pages are hours-led but keep their named package's dollar anchor, and every disclosure includes "hours × rate is not a quote". Estimates: Naples ~120–160 (owner-raised above the derived 95–130), Elite ~70–80, JPSurette ~35–50, SeansAds ~60–80, AuctionBuddha ~280–440+, MetalsCalc ~15–20 hrs (others derived from the earlier dollar ranges at $125/hr so everything reconciles with the pricing page). Note: `portfolio-seansads.html` gets its Tailwind config from `assets/tailwind-nav-config.js`, so its section uses `font-['JetBrains_Mono']`-style arbitrary classes rather than the `font-label-mono` tokens the other detail pages define inline. If pricing changes in `website_pricing_plan.txt`, both surfaces must be updated together.

- **Site breakpoint unified at 880px (2026-07-16).** The mobile/desktop line is now 880 for *everything* — header, bottom bar, page layout, footer. Enforced in two places that must agree: `"screens": {"md": "880px"}` in every page's `tailwind.config` (+ `assets/tailwind-nav-config.js`), and `879.98/880` in all hand-written media queries. New pages must carry the screens override or they will flip layout at Tailwind's stock 768 while the nav flips at 880 (that mismatch is what put the mobile bar over a squeezed desktop grid). Portal pages without a nav loader and `client-portal.css` are deliberate exceptions.
- **Header breakpoint moved to 880px + hero height caps (2026-07-16).** The header used to half-collapse between 768–880px (CTA hidden, dead gutter, Contact unreachable from the header); it now switches to the hamburger at 880. Nav-only: page layouts still switch at 768, so 768–880 renders the wide layout under the hamburger header. Implemented with two `nav.css` rules on `.dm-standard-desktop-nav` / `.dm-standard-mobile-shell` (the latter class was added to the injected shell so page wrappers sharing `md:hidden` aren't affected); mobile-chrome CSS now runs to `879.98px` while page-layout rules stay at `767px`. Also capped both heroes so their flexible middle stage can't strand the CTA on tall windows: `#hero` → `min(100vh, 1000px)`, `#hero-mobile` → `min(74vh, 700px)`. Common phone and laptop sizes are unchanged by design. Audited all 28 pages at 768/834/879/880. `nav.css` + `standard-site-nav.js` → `v=20260716-nav-880`.

- **One shared footer site-wide (2026-07-16).** ~10 drifted footer variants across 27 pages replaced by `assets/standard-site-footer.js`, injected from a single tag per page (`data-variant="desktop"`/`"mobile"` on the two-layout pages; `../assets/...` under `services/`). No inline footer markup remains anywhere. Mirrors the homepage: `.dm-footer` ≥768px (tablet included), `.dm-footer-mobile` below. **Key constraint: injected markup cannot rely on Tailwind** — the CDN only builds utilities it finds in a page's static HTML, so the footer is styled with plain CSS in `nav.css`, and the hamburger's line colours are pinned there too (that bug had left `portfolio.html`'s menu button invisible). Verified with zero computed-style diffs against the homepage's real footers, then across all 27 pages at 390px and 834px.

- **Bottom tab bar was covering footers (2026-07-16).** Fixed in `nav.css` with one rule — `body.has-dm-standard-nav footer { margin-bottom: calc(5rem + env(safe-area-inset-bottom,0px)) }` under 768px — after an audit found 7 of 28 pages hiding real footer links behind the bar (the six portfolio detail pages were a regression from the nav migration, which removed the shim rule that had held them clear; the pricing page was broken independently). Must be a footer margin, not body/wrapper padding, or the footer lifts off the bar and exposes the fixed starfield. Removed the redundant `pb-24` from the two service pages' mobile wrappers. `nav.css` → `v=20260716-bottom-bar-clearance` on 33 pages.
- **Table scroll hint + pricing-page anchor offset (2026-07-16).** New `assets/table-scroll-hint.js` makes horizontally-scrollable tables announce themselves on tablet/mobile by nudging right and easing back the first time they scroll into view. It is generic by design — it resolves the scroll container by walking up from each `<table>`, so new tables need no opt-in. Loaded on `services/website-design-hosting.html`, `account-admin.html`, `account-users.html`. Also fixed the pricing page's jump pills landing with the teal eyebrow tucked under the fixed nav: `section[id]` scroll-margin-top is now 145px (≥768px) / 110px (<768px), because the nav is ~101px but the mobile header is 64px. Note for future browser checks: **the preview pane freezes `requestAnimationFrame` and IntersectionObserver** (screenshots time out at wider viewports), so animation/observer behavior has to be verified by re-evaluating the file under a stubbed clock rather than watched visually.

- **Nav single-source migration (2026-07-15).** All ~16 public pages that still carried hand-copied inline nav markup (index, contact, apps, app-catalog, casestudies, built-by, accessibility, process, terms, privacy, the five portfolio-* detail pages, and both remaining `services/*` pages) were converted to the `assets/standard-site-nav.js` loader; the loader now `../`-prefixes its links on `services/*` pages (they all 404'd before); `assets/unified-mobile-menu.js` (the legacy runtime shim) was deleted along with its 28 script tags and its nav.css shell rules; `app-pricing.html`'s broken `es/` lang toggle was removed; all loader references now use `v=20260715-nav-unify`. This closed several live bugs: wrong active-page highlight in the mobile menu/bottom bar on six pages, hardcoded-cyan Client Login on legacy pages, the homepage's structurally different hamburger menu, portfolio detail pages having no mobile nav in markup, and terms.html's empty nav logo. Portal utility pages (account-admin/-users/-settings/-created, app-checkout) intentionally keep their own minimal chrome.
- UX iteration pass on the pricing page: type-hierarchy split (`wp-h4`/`wp-label` tiers), scroll-reveal with stagger on all cards/tables, contrast/tint pass (purple pills with arrow affordance), hero simplified with jump-pills relocated below the "Two Ways" section, plain-English glossary + guidance links for non-technical visitors, and a sitewide reveal-trigger fix in `surette-logo.js` (now fires 320px below the fold; cache-busted on all 27 pages).
- Completed the Website Design &amp; Hosting pricing page (`services/website-design-hosting.html`) from the owner's `website_pricing_plan.txt` spec: hero, two purchasing paths, featured $0 Upfront Starter Website, 5-plan managed comparison + detail cards, after-12-months ownership/renewal rules, 8-package one-time build comparison + details, care plans, update/support/revision policies, lead capture, scheduling, 8-step process with fine-print accordions, ownership/handoff, add-on pricing, 36-question FAQ, and final CTA. Page uses native `<details>` accordions, scrollable comparison tables, and page-scoped `wp-*` styles; verified anchors/IDs/overflow/content in-browser and reran the validator (pre-existing noise only). Do not change plan names, prices, allowances, or policy wording without consulting `website_pricing_plan.txt`.
- Fixed the mobile Services popout (`assets/mobile-services-nav.js`) still linking to the three retired service pages; replaced with one "Website Design / Hosting" entry (EN + ES) and cache-busted all 28 referencing pages to `v=20260715-services-merge`.
- Repaired public header/nav consistency across desktop, tablet, and phone breakpoints. Added `assets/unified-mobile-menu.js`, updated `assets/standard-site-nav.js` hooks and shared `assets/nav.css` gutters/overflow behavior, and included the compatibility layer on legacy public routes. Verified Portfolio, Client Login, service pages, app pages, legal pages, and legacy portfolio details through a 150-case browser sweep (30 routes x 5 widths); all checks passed.
- Added `portfolio.html` as an intermediate Portfolio hub and changed hand-authored desktop/mobile navigation so Portfolio is the top-level work category. Existing Apps (`apps.html`) and Websites (`casestudies.html`) pages remain live downstream. `portfolio.html` now uses the shared `assets/standard-site-nav.js` generator with `data-active="portfolio"` instead of one-off nav markup. Verified desktop and 390px mobile preview; static validator still reports documented pre-existing noise plus the expected missing Spanish mirror for `portfolio.html`.
- Restored Netlify Forms capture for Contact, Apps consultation, App Checkout, and account-dashboard request forms while keeping Supabase/Admin Center message saves. Removed the Resend notification function path.
- Fixed the service-page header/menu right-shift by anchoring fixed top headers to the viewport in shared `assets/nav.css` and cache-busting `nav.css`. Verified all five service pages at mobile width and checked Website Design on desktop.
- Removed profile-level business/company and website URL fields from account settings, Admin Center subscriber/account-holder tables, and Supabase setup/schema cleanup. Verified preview headers/forms no longer expose those fields.
- Separated Admin Center Subscribers from Account holders: Subscribers now reads only `homepage_email_signups`, Account holders remains auth/profile driven, new account signups still mirror into subscriber rows, and the SQL setup no longer re-backfills all auth users into Subscribers on rerun.
- Widened Admin Center Subscribers and Account holders table name/email columns, especially subscriber email, and narrowed/wrapped the Subscribers source column. Verified the updated CSS/script cache keys and computed column widths in local preview.

- Made the mobile header account icon color state consistent across pages: grey when signed out, cyan/blue when signed in. Verified the logged-out state on a mobile service-page preview.
- Added consistent mobile header/menu chrome to all service umbrella pages and verified at 390px that the logo, account icon, promoted nav row, and Services popout render without horizontal overflow.
- Added compact mobile footers to all service umbrella pages and verified at 390px that the mobile footer is visible, contained, and does not create horizontal overflow; desktop still shows only the original desktop footer.
- Added a sitewide no-white-flash dark loading baseline: inline critical background CSS, shared `assets/nav.css` dark document/app-root defaults, normalized dark `theme-color`, and checked repeated desktop/mobile route loads and hard reloads for home, apps/pricing, contact, legal, accessibility, and SDMS compliance pages. Then corrected the account page regression by removing opaque backgrounds from visual shells so the animated portal hero remains visible.
- Removed Netlify Forms from Contact, Apps consultation, and App Checkout. Added shared Supabase form submission JS, public `submit_site_message()` SQL, optional photo attachment upload support, and Admin Center attachment rendering.
- Replaced the account dashboard request form's Netlify Form submission with a Supabase `submit_portal_message()` RPC. Added `client_messages` schema/RLS plus owner-only list/delete RPCs to `supabase/portal-role-setup.sql`, and added a Message Center tab to `account-admin.html`.
- Added Delete actions to the Admin Center Subscribers and Account holders tables. Each opens a confirmation modal before calling owner-only Supabase RPCs; the signed-in owner account row is protected from self-delete.
- Split Admin Center into its own `account-admin.html` page. `account.html` now contains no inline owner console; it only reveals an owner-only link to the separate admin page. The Admin Center itself is organized as a left-anchored tab workspace.
- Admin Center desktop layout is intentionally wide for ultra-wide use. The Subscribers and Account holders side tabs render/load their tables directly in the tab panel.
- Added `account-users.html` as an owner-only newsletter subscriber table linked from Admin Center. It treats portal account signups as subscribers and includes a pending placeholder/source for future homepage email sign-ups.
- Added homepage newsletter signup forms in desktop/mobile heroes, wired through `assets/newsletter-signup.js` to `homepage_email_signups`.
- Moved owner-only portal tools behind an "Admin Center" card so the super-admin account looks like a regular client dashboard until Admin Center is opened.
- Added canonical Supabase auth redirect base (`siteUrl`) and moved the `account.html` forgot-password handler into `assets/client-portal.js`; password reset links now target `https://surettesystems.com/account-settings.html` instead of local preview origins.
- Confirmed the project is wired to Supabase through browser-side config/project `axlszyssxyvehjatztwe`; frontend queries `client_profiles`, `client_services`, `client_invoices`, `client_documents`, `client_messages`, and `homepage_email_signups`. Starter SQL still contains some legacy table names and should be reconciled before fresh DB bootstrap.
- Smoothed mobile homepage scrolling for the Nova particle background by preventing WebGL renderer resize/reallocation during height-only browser toolbar changes. Verified at a 390x844 mobile viewport that the fixed layer and canvas size stay stable after scroll; no browser console errors.
- Fixed the mobile header account control so signed-in state no longer appends the visible word "Account"; cache-busted `account-nav.js` references to `v=20260702-mobile-account-icon`.
- Added **MetalsCalc** as the 5th Surette Data Systems app: `metalscalc-buying-calculator.html` app profile, tile #05 in `app-catalog.html`, screenshots in `assets/apps/metalscalc/`. Added `portfolio-metalscalc.html` Websites detail page as entry #06 in `casestudies.html`, screenshots in `assets/portfolio/metalscalc/`.
- Added portal support pages: `account-created.html` (post-signup email verification), `account-settings.html` (profile/password management), `account-ads-status.html` (Google Ads workspace stub).
- Rebuilt `app-catalog.html` gallery cards to `.portfolio-tile` design matching `casestudies.html` (2-up mobile, 3-up desktop). Removed "DepthFold" 3D card system.
- Removed "fly" WebGL canvas hero from `app-catalog.html`; restored cosmic-web background.
- Replaced top-level `Process` nav item with `Websites` → `casestudies.html` across all shared/hand-authored nav. Added "View Our Process" CTAs to Website Design, Managed Hosting, Care Plans, Custom Apps, In-Home Tech, and Office Network service pages.
- Added AuctionBuddha.com entry (`portfolio-auctionbuddha.html`) + screenshots to Websites gallery.
- Refreshed NaplesEstateJewelry.co portfolio content and screenshots.
- Refreshed Antique Mall profile around `thirdstreetauctions.com` with 7 new public captures and updated feature copy. Fixed overlapping marketplace-flow diagram.
- Fixed homepage hero tint to use a fixed `#hero-tint` scrim (sibling of `#nova-bg`) so it stays anchored while content scrolls over the Nova background.
- Replaced homepage black-hole Three.js hero with Nova particle-galaxy widget.
- Removed "Do Not Press" widget (`face-interactive.html` deleted, `assets/face/` removed).
- Added super-admin account-holder viewer to `account.html` + Supabase RPC.

## Validation

Run after broad edits:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\validate-site.ps1
```

Known validator noise (pre-existing, not blocking): missing `es/` mirrors and the node_modules HTML scan. Mojibake in site content was fully repaired 2026-07-15 — a clean run should show zero mojibake findings outside `node_modules`. Note: in restricted sessions run the validator as `& .\scripts\validate-site.ps1` (the `-ExecutionPolicy Bypass` form can be blocked).

## Do Not Forget

- Mirror English/Spanish changes when `es/` mirrors are restored.
- Do not store secrets in markdown.
- Browser-side portal gates are not true security.
- MetalsCalc OG URLs reference `darkmatterwebsites.com` — verify/correct domain.
- Deploy cleanup done 2026-07-21: stale captures/scripts deleted, `node_modules` gitignored (on disk only, `npm install` restores), git index reconciled with disk. `pokecard-dropin/app-template/` is the live "Tech Business Site Template" demo — never delete it as "vendor cruft". Netlify publishes the repo root as-is, so project docs, supabase SQL, and `website_pricing_plan.txt` are publicly fetchable URLs — owner-accepted tradeoff.
- Update compact memory docs before ending meaningful sessions.
