# Tasks

Last updated: 2026-08-27

## Active

- **DONE 2026-08-27: Self-Hosting Setup is surfaced.** `pricing.html` (new "Own your own hosting" route, new FAQ, rewritten ownership FAQ), `services/website-design-hosting.html` (full panel in `#one-time-builds`) and `websites.html`. The stale "manage hosting yourself" claim is gone from all three.
- **DONE 2026-08-29: all cache-busters verified and bumped to `20260829-apps-retired`.** A full mtime-vs-token audit found **8 stale assets**, not 3: the three from the 08-19 session, three that had been stale for weeks (`account-settings.js`, `client-portal.js`, `portfolio-title-fit.js`), and — critically — **`nav.css` and `standard-site-nav.js`, which were edited AFTER the 08-27 bump**, so the nav fix would not have reached returning visitors. **Bump tokens as the LAST step before deploy, never mid-session**, and re-run the mtime-vs-token check to confirm.
- **OPEN: signed-in portal views have never been audited** for contrast. Every sweep sees only the signed-out shell (12–26 text boxes on the account pages). Needs an authenticated session.
- **Apps are retired (2026-08-27).** Do not re-add app-catalog/app-pricing/app-checkout or a Software nav pill. The six profiles are case studies only. §29c defines what is a website vs custom software — check tier 3 scope against it before quoting.
- **Nav breakpoint is 1024px** with four pills (+41px). **0px clearance at 920 — never return to 880.**

- **`apps.html`'s physics wordmark does not run below 880px** (2026-08-27) — a static wordmark stands in. The letters used to settle on the hero copy; every softer fix was measured and failed (the canvas paints above the section whatever the stacking, so no chip or z-index helps). **If you re-enable it below 880, the contrast regression comes back** — and it is intermittent, so test 20+ loads, not 5.

- **Nav collapses to the hamburger at 1120px (was 880px), changed 2026-08-20.** The row needs ~1105px for the 7 items an interior page carries; below that it spilled and buried the theme toggle and login icon under the CTA. **Re-measure on an interior page before adding a 6th pill.** The breakpoint lives in 6 `@media` blocks in `nav.css` plus `isMobileNavViewport()` — and `.page`'s padding is split so only its vertical half follows the nav.

- **Full contrast sweep clean as of 2026-08-20: 52 pages x 2 themes x 4 widths (1280/1024/768/390) = 416 page-audits, BOTH auditors, 0 failures on every routable page.** That now includes `multichannel-commerce-website-test-4.html`, whose 761-800px coral-circle overlap was fixed on request. **Sweep all four widths — 1024/768 found defects that 1280/390 did not.** Re-run after adding pages. Scope is 49 root + 3 `services/`; it was 51 pages on 2026-08-18.
- **A sweep is only clean if BOTH auditors are clean.** `scripts/audit-contrast.js` now runs an ancestor walk *and* a rendered-pixel pass. The 2026-08-18 "0 failures" result was ancestor-walk only and missed five real defects, including whole pages washed near-black in light mode. **`getComputedStyle` cannot see sibling paint, fixed overlays, canvas, or images** — only a screenshot can.
- **Run the pixel pass like this:** hide every glyph, screenshot the *viewport* (not `fullPage` — it re-composites fixed layers), sample the exact line boxes from `Range.getClientRects()`. Keep only boxes that are topmost at their own coordinates (flipped card backs are laid out behind the front face and will otherwise be scored against front-face pixels), force `scroll-behavior: auto`, bracket the capture with two rect measurements and drop anything that moved, and keep the **best-centred** sample per box rather than the worst across scroll steps.
- **Score by contiguity, not by percentile.** Hairline decoration (apps.html's fractal) fails ~30% of the pixels under legible copy; a bright plume covering whole words (account.html) is the real failure. Fail at `maxChunk >= 0.75` over word-sized chunks.
- **Scroll the page before auditing.** `.reveal-up` sections stay at `opacity: 0` until `surette-logo.js`'s IntersectionObserver fires — 64 homepage elements were silently unmeasured without it.
- **Auditing requires waiting out the OBJECT REVEAL (~800ms).** nav.css holds ~19 selectors (.glass, .panel, .card, .pricing-banner, .sds-card ...) at opacity 0 until html gains page-content-ready. Audit sooner and those panels are measured mid-fade, which OVER-estimates contrast because the auditor reads the elements own color and ignores ancestor opacity; at exactly 0 they are skipped outright. Force the whole reveal list visible, add page-content-ready, settle >=700ms.
- **A screenshot taken inside that ~800ms window looks exactly like a contrast bug and is not one.** app-pricing.html was reported this way on 2026-08-18; measured after the reveal it is html opacity 1, banner opacity 1, and passes in both themes.
- **RULE: when a surface does not flip with the theme, nothing layered on it may flip either.** `portfolio-live-widget.css` broke this three ways and went to 1.21:1 in light. It now scopes its own `--lw-light` / `--lw-dark` locals and literal gradient stops. **Never use `--c-veil-rgb`, `--t-void-black`, `--t-electric-cyan` or a `--t-ink-*` token inside that file.**
- **Auditor caveat: drop gradient stops with ~0 alpha.** A transparent stop reveals the layer beneath; counting it as a ground scores text against the page body and invents failures. Cost 14 phantom hits on 2026-08-18.
- **Light accents are calibrated against the standard light surfaces.** Four separate components put them on darker tinted panels and landed at 4.19-4.42. If you add a tinted panel in light mode, re-check any accent text on it.

- **All 13 industry pages + the industries hub shipped (2026-08-18).** Generated by scripts/gen-industry-pages.js from one TRADES list, so the hub cannot drift out of sync with the pages. **The HTML is the source of truth after generation** — re-running the script OVERWRITES hand edits. Edit the HTML for one page; edit the generator only when all 13 should change together.
- **Industries is now the 5th nav pill.** The desktop bar was re-measured at 880px (narrowest desktop width) and still fits. **Re-measure there before adding a 6th.** Valid data-active gained industries.
- **None of the 13 pages carries a statistic, and none claims a client in its trade.** funnls leans on unsourced market figures; we do not. Every proof strip uses its real attribution (Naples medical office). If a real client lands in one of these trades, swap that pages strip for a genuine quote. Verified: zero percentage claims across all 14 pages.
- **Validator baseline is now 84** (71 + 12 new industry pages + 1 hub; HVAC was already counted).
- **Trades covered:** HVAC, plumbing, roofing, law firms, medical practices, dentists, real estate, restaurants, auto repair, contractors, home services, irrigation, plus a custom/catch-all page. **If the owner does not actually serve one of these, delete its page and its TRADES entry** — the pages describe our service and general trade observations, but publishing a trade page is an implicit claim of interest in that work.

- **HVAC page is the TEMPLATE for the other 12 industry pages (2026-08-18):** `web-design-for-hvac.html`. Swap points are commented at the top of its body, and repeated in `project-docs/FUNNLS_PARITY_PLAN.md` §8.
- **That page carries NO statistics, on purpose.** funnls' industry stat band ("46% of searches are local", "$15–45 CPC") is unsourced market claims — same class as the 1–2 week turnaround figure. Supply sourced figures and the band goes under the hero; otherwise clone as-is. Verified zero percentage claims in the body.
- **Nothing on it claims an HVAC client, because there isn't one.** The proof strip uses its real attribution (Naples medical office). Swap it if a real HVAC client lands.
- **No industries hub or nav route yet.** funnls uses a "Businesses" nav item → `/industries`. Not worth building for one page; revisit at 3+.
- **Validator baseline is now 71** (68 + `blog.html` + `blog-post-template.html` + `web-design-for-hvac.html`).

- **Blog is built but has ZERO posts and is NOT in the nav (2026-08-18).** `blog.html` + `blog-post-template.html`; authoring steps in `project-docs/BLOG.md`. **Add the nav item only once the first post ships** — a 5th pill was measured and fits at 880px. Until then the page is reachable only by direct URL, which is intentional.
- **Validator baseline is now 70** (68 + `blog.html` + `blog-post-template.html`).
- **Never put example `href=` markup inside an HTML comment.** `validate-site.ps1` does not parse comments, so it reports the example as a broken link. Cost one confusing validator delta on 2026-08-18; the blog card pattern lives in `project-docs/BLOG.md` for this reason.
- **Phase 2 of the funnls parity pass (industry pages) is still blocked on the owner** — which trades to target, and whether the industry statistics in funnls' template can be sourced. See `project-docs/FUNNLS_PARITY_PLAN.md`.
- **A3 residual risk still open:** cancellation, revision-round, post-launch bug-fix and ownership/handoff terms are now unpublished sitewide. `terms.html` does not cover them. Decide whether they move into `terms.html` or stay contract-only.

- **Homepage "Recent work" shows live sites only (2026-08-17):** Naples Estate Jewelry, SpotCalc.com, Deepfield Gallery. If any goes offline, **swap it out** rather than marking it offline — that section is proof and must only link to sites a visitor can open. It lives in **both** `index.html` trees.
- **Watch the weight of anything added to that section.** Its three cards were once 3.2 MB, were cut to ~566 KB, and now total 383 KB. Use a purpose-shot card image (`scripts/capture-deepfield.js --card` produces a 1200x750 q82 JPEG) rather than a raw `assets/portfolio/**` capture.

- **The Websites gallery is SEVEN cards as of 2026-08-17** (Deepfield Gallery, No. 007, live). **Both grids must stay in sync** — desktop and phone are hand-duplicated. `portfolio.html` reads 7 Websites / 7-card set.
- **Deepfield is the only gallery entry without a `portfolio-*.html` detail page** (owner chose card-only). Its foot has one `Live Site` button instead of `Live` + `Case Study`. Building the detail page later means adding the second button to **both** grids and matching the other six pages' structure (hero, `.portfolio-live-widget`, `#build-pricing`).
- **Card-back checks: do not trust a "true slack" child-sum.** It reads negative on every shipped card because `.poke-back-inner` is a flex column with `margin-top: auto`. Use `plate.scrollHeight - plate.clientHeight` plus the inner-vs-plate delta. `.poke-plate` is `overflow: hidden`, so an over-budget back **clips silently instead of scrolling**. Force `loading="eager"` on lazy art before measuring, or an offscreen iframe reports 0x0 images.
- **`scripts/capture-deepfield.js`** re-captures the Deepfield screenshots. It **hides the site's cookie notice via CSS rather than clicking it** — keep it that way; clicking Accept would be consenting on the owner's behalf.

- **Nav condensed to `Websites · Software · Pricing` (2026-08-17) — done; two follow-ups.** Plan and rationale: `project-docs/NAV_RESTRUCTURE_PLAN.md`. **Do not reintroduce a Work/Portfolio/Case-Studies nav item as a fourth sibling** — that recreates the exact ambiguity the owner asked to fix. Open items, neither blocking:
  1. `process.html` is still in **no menu**, reachable only from service-page CTAs. It is the standing candidate if a fourth nav slot is ever wanted (the local equivalent of funnls' "Why Funnls").
  2. Consider whether `websites.html` and `apps.html` should make their gallery links more prominent, now that they are the only route to `casestudies.html` and `app-catalog.html`. Both links exist and were verified; this is a strength-of-signal question, not a broken-link one.
- **Audit canvases by screenshot, not by computed style (2026-08-17).** `apps.html`'s hero wordmark sat at **1.12:1** in light mode through every previous "clean" sweep because it is a `<canvas>`. **Screenshots do work in the preview pane** — the old "pane never composites frames" note is wrong; it fails only when the pane is hidden. Any canvas/WebGL/image-backed text needs an eyeball or a pixel sample, and any new canvas needs an `sds-themechange` listener (see `fractal.js` and `surette-physics-wordmark-canvas.js` for the pattern).
- **Remaining canvas surfaces to spot-check in light mode:** the nav logo canvas (`surette-logo.js`) looked correct in this session's screenshots but was never pixel-sampled, and the `nova/` widget is gated off in light on the homepage.
- **`terms.html` loads `surette-physics-wordmark-canvas.js` but never instantiates it** — dead weight, same class as the deleted `rail.js`. Safe to drop the script tag; verify nothing else on the page expects the global first.
- **Validator baseline is now 68** (was 69, was 67). `process.html` was deleted 2026-08-17, removing one expected missing-`es`-mirror. Each new English page adds one back.
- **Homepage process block follows funnls' structure (2026-08-17). Keep it at three steps.** The ladder is now **3 → 4 → 8**: homepage (hook), `websites.html` (detail), `services/website-design-hosting.html` (full reference). `process.html` — the former fourth surface — is retired. Adding a fourth homepage card, or a fourth process page, recreates the overlap that produced both this and the nav problem.
- **`websites.html#how-it-works` is a load-bearing anchor id — do not rename it.** It is the Netlify 301 target for the retired `/process.html`, and both service pages' "View Our Process" CTAs. (It was also `rail.js`'s `launch` target before that file was deleted.)
- ~~**`assets/rail.js` is dead code — decide whether to delete it.**~~ **Closed 2026-08-17: deleted**, with its 13 script tags and 76 lines of orphaned `.rail-popover-*` CSS across 9 pages. **Two unrelated "rails" exist here — never sweep on the bare word:** the live Admin Center sidebar uses the `client-admin-*` prefix (`client-admin-tabs-rail__brand`, `client-admin-rail-link`, `client-portal.css`), and `nav.css`'s mentions are layout comments. A naive `rail` sweep would gut the Admin Center.
- **`.footer-meta` / `.footer-meta-sep` in `nav.css` are still dead** and remain the next candidate of this kind, alongside the unreferenced `assets/mobile-services-nav.js` (which must stay unreferenced — its regex would attach the retired popout to the Websites tab).
- **The 1–2 week turnaround is a published factual claim — handle it like a price, not like copy.** It appears on `index.html` (both trees) and `websites.html:167`, and **always with its qualifier**: One Page sites, once content and access are in hand, larger builds run longer. If the real turnaround changes, both surfaces must move together. Never restate it unqualified.
- **NEVER bulk-edit `.html` with PowerShell `Get-Content`/`Set-Content`/`Out-File` (2026-08-17).** PowerShell 5.1 reads UTF-8-without-BOM as Windows-1252, so a read-modify-write round-trip mojibakes every curly quote, em-dash and middot — **silently**. It hit 31 files this session during a cache-buster bump and was caught only because the validator jumped 69 → 271. Use `[IO.File]::ReadAllText/WriteAllText` with `UTF8Encoding($false,$true)`, the editor's edit tooling, or git-bash `sed`. **Re-run `scripts/validate-site.ps1` after every bulk pass** — the delta off the 69 baseline is the only cheap detector. Reversal recipe is in DECISIONS.
- **`DEVELOPER-NOTES.md` was rewritten 2026-08-17** after drifting since June. If you change the nav, the theme system, the breakpoint, or the validator baseline, update it — it is the first file a new contributor reads.
- **The docs trailed the code by a session when this one started** (docs stamped 2026-08-16, assets on `?v=20260817-cta-r5`). `pricing.html`'s plan names had changed to One Page / Starter / Growth / Pro without CURRENT_STATUS being updated — its "four cards: Starter $125 · Lead Capture $250 · Local Business $375–500 · Growth $750" description is stale. Someone should reconcile the pricing section against the live page.

- **Layout upgrade (funnls.com reference) — Phases 1 and 2 shipped, 3–5 remain.** Plan file: `C:\Users\rcman\.claude\plans\zazzy-dazzling-waterfall.md`.
  - ✅ Phase 1 — nav/IA: `Websites · Software · Work · Pricing` + Contact CTA.
  - ✅ Phase 2 — `pricing.html` front page over the existing detail page.
  - ✅ Phase 3 — `websites.html`, plus "Get started" adopted sitewide (21 anchors).
  - ✅ Phase 4 — homepage restructured websites-first; the local-services strip restored in-home/office-network discoverability.
  - ✅ Phase 5 — shared `.sds-*` layout system in `nav.css`; both pages migrated, 37 duplicated rules removed.

  **The layout upgrade is complete.** Possible follow-ups, none blocking: a proof/testimonial strip higher on the homepage (the plan sketched one but the existing testimonials section was left where it sits); tablet-band (1024px) contrast coverage; and signed-in portal content, which remains unaudited.
- **Validator baseline moved to 69** (was 67): `pricing.html` and `websites.html` each add one expected missing-`es/`-mirror. Each further new English page adds one more.

- **Theme rules for all new pages (2026-08-16).** A new page needs, or light mode breaks on it: (1) the inline `<script id="sds-theme-resolver">` as the **first** script in `<head>`; (2) `#dm-critical-dark-baseline` including its `html[data-theme="light"]` rules, with hex literals left alone; (3) the `tailwind.config` palette written as `rgb(var(--t-NAME) / <alpha-value>)`, never hex. Copy all three from any current page. Re-run `node scripts/add-theme-resolver.js` and `node scripts/tokenize-tailwind-config.js` — both are idempotent and will fill in anything missing.
- **Two traps that cost real debugging time (2026-08-16), worth reading before editing `index.html` or auditing contrast:**
  1. **Never write a literal script-closing tag inside JS, including in a comment.** The HTML parser ends the `<script>` element on that byte sequence regardless of JS context. Doing so truncated `index.html`'s Nova module from 7,816 to 2,496 chars and orphaned the engine, with only a generic `SyntaxError` to go on.
  2. **`scripts/audit-contrast.js` skips elements at `opacity: 0`,** and `hero-enter`/`reveal` entrance animations never complete in the preview pane because rAF is frozen there. A broken white `<h1>` passed the audit for that reason. Inject `.hero-enter,[class*="reveal"]{opacity:1 !important}` before auditing any page with entrance animations.
  3. Related: the preview pane's console buffer **persists across reloads and navigations within a tab**. Errors from an earlier broken load will look like they are recurring. Open a fresh tab to get a clean console before concluding an error is live.
- **Never hardcode a palette colour (2026-08-16).** Literals silently defeat the theme. Use `--t-*` for palette colours, `--c-veil-rgb`/`--c-shadow-rgb` for translucent overlays, `.sds-on-accent` for text on gradient fills, and `--t-ink-*` only for the 29 legacy one-off page colours (don't add to that block — it's a compatibility shim, not a design system). The five sweep scripts under `scripts/` are all idempotent and safe to re-run after adding content.
- **Light-mode contrast must be checked against the darkest light surface** (`#e5e2db`), not the page ground — the two differ by enough to move a value from pass to fail. Use `scripts/audit-contrast.js`; it composites translucent ancestors, which a naive check does not.
- **Cosmic-web light values are pre-inversion complements.** Editing that block by picking the colour you want will produce its opposite. Write `255 - channel`, and keep `invert(1)` alone in the filter chain (adding `hue-rotate` makes the complements no longer a simple subtraction).
- **Two contrast items remain open, both pre-existing** (they failed identically before the theme work, so they are a design call, not a regression): homepage nebula-purple decorative icons at 2.91:1 against the 3:1 large-text threshold, and `#fractal-gui-toggle` (the fractal dev-settings control) at 0.18 alpha in both themes.
- **ALWAYS audit at a phone width as well as desktop (2026-08-16).** This site renders a genuinely separate mobile DOM — `.dm-footer-mobile`, the injected phone account icon, the bottom tab bar, the phone gallery zigzag — none of which exists at 1280px. A desktop-only contrast sweep reported 0 failures while **169 elements were failing at 390px in both themes**, two of them shared chrome on nearly every page. Run `__scan` (or audit-contrast.js) at 1280 **and** 390 before calling a sweep complete.
- **Inline styles set from JS cannot be themed from CSS.** `account-nav.js` writes the account icon's colour as an inline style, which outranks every stylesheet rule. Anything set that way must reference a CSS variable (`var(--sds-acct-out)`) or it is frozen to one theme. Same class of problem as the `<html style="background:…">` attribute the pre-paint resolver has to overwrite.
- **Light-mode sweep is COMPLETE (2026-08-16): all 34 pages, 3,871 elements, 3 failures** — all the `#fractal-gui-toggle` grain control. The earlier assumption that unswept pages "share the same chrome so should be clean" was **wrong**: the full sweep found five systematic causes affecting 100+ elements that the five-page spot-check never touched. Do not extrapolate page health from shared chrome.
- **Dark-mode contrast is CLEAN as of 2026-08-16: 3,871 elements, 0 failures** (was 49). Two rules to preserve:
  - **`#7000FF` is a fill colour, not a text colour.** As a foreground it measures 2.91:1 on the site's near-blacks. Text and icons must use `--t-nebula-purple-on` (via the `:root .text-nebula-purple` override); the raw token stays for gradient stops. If you add a new purple-on-dark label, use the `-on` token.
  - **Don't dim small print with alpha.** `text-outline/40-60` and the app-credit pill's 58% label failed in *both* themes. Alpha reads as "subtle" in a design tool and as "illegible" in a contrast check. Use a dimmer token, not a transparent bright one.
- **Closed 2026-08-16: the multichannel 92px overflow.** Root cause was a decorative pseudo-element (`.mc-browser::before`, a halo at `inset:-18%`), fixed with `overflow-x: clip` on `.mc-page`. Two things worth carrying forward:
  - **`body{overflow-x:hidden}` does not stop viewport scrolling.** Pairing `hidden` with `overflow-y:visible` makes body its own scroll container instead of propagating to the viewport. If a page still scrolls sideways despite that rule, this is why. Use `overflow-x: clip` on a full-width wrapper instead — it is the only value that pairs with `visible` without creating a scroll container.
  - **Pseudo-elements are invisible to overflow hunting.** No `getBoundingClientRect()` exceeded the viewport because `::before`/`::after` are not in the DOM. Bisecting by hiding subtrees actively misleads (hiding a grid item re-sizes the tracks). The reliable probe is to disable all `::before`/`::after` in a suspect region and watch `documentElement.scrollWidth`.
- **Sweep technique worth reusing:** auditing 34 pages one navigation at a time is ~70 round trips. A same-origin iframe harness (see the `__sweep` helper pattern in CHANGELOG) audits all of them in three calls, because contrast only needs computed styles and layout, which work fine in an offscreen iframe. Only *timing*-dependent checks are unreliable there.

- **Portfolio live-status rule (2026-08-08): only `naplesestatejewelry.com` and `spotcalc.com` are live.** If a portfolio site comes back online, three surfaces must move together or they contradict each other: the gallery card's `status - ...` tag AND its outbound button (in **both** duplicated grids of `casestudies.html`), and the detail page's `.portfolio-live-widget` (drop the `--offline` modifier) or, for Sean's Ads, its hero eyebrow/status chip/closing section.
- **Do not lengthen the gallery cards' foot buttons past ~105px (2026-08-08).** `Ask to View` (84px) is deliberate. `Request Viewing` (108px) wraps the `.poke-links` row and puts AuctionBuddha's back face into a 24px scroll at 320px. Measure the row, not the button, and remember the desktop grid is `display:none` below 880px so it reports fake passes.
- **Confirm `spotcalc.com` and `app.spotcalc.com` actually resolve (2026-08-08).** The rename was applied from the owner's instruction; neither URL was reachability-tested from the build environment. SpotCalc is the only app still presented as live, so a dead link here is the one remaining way a visitor hits a wall.
- **App availability rule (2026-08-08): only SpotCalc is live.** If a new app profile page is added, or a retired app comes back online, keep these four surfaces in step or they will contradict each other: the profile page's CTA + `.app-access-notice`, the `.tcg-stat-pill` on `app-catalog.html`, any per-app CTA on `app-pricing.html` (a stale `benjipayroll.netlify.app` button was found there, outside the app pages), and the catalog's meta descriptions. The notice is **one shared rule** in `assets/surette-data-systems-app-profile.css` — add the `.app-access-notice` markup, never a page-local copy of the CSS.
- **Brand-rename rule for SpotCalc (2026-08-08):** brand strings are `MetalsCalc`/`metalscalc.com`; file and asset paths are lowercase `metalscalc-`/`metalscalc/` and are **kept on purpose**. A case-sensitive substitution therefore hits every brand string and no path. Do not "tidy" this with a case-insensitive replace — it would rewrite live URLs and every screenshot path.

- Logo rule for new public pages (2026-08-01): pages using the injected desktop and mobile nav must include the established forced-flex CSS selectors for both `#sds-logo` and `#sds-logo-mobile`, and initialize both mounts after `surette-logo.js`. The logo script emits one shared `#sds-styles` block scoped to the first mount it initializes; without the two-mount page styles, the second/mobile wordmark stacks below its canvas.
- Keep memory docs compact and current after meaningful work.
- Static validator known noise: missing `es/` mirrors and node_modules/pokecard template scans. New English-only pages also appear as missing Spanish mirrors until bilingual pages are restored. Mojibake was swept site-wide and repaired 2026-07-15 — the only remaining validator mojibake hit is inside `node_modules` (vendor file, ignore).
- Nav rule for all new pages: never hand-copy nav markup — add `<script src="assets/standard-site-nav.js?v=20260801-scroll-hide3" data-active="...">` (use `../assets/...` under `services/`; version current as of 2026-08-01). `assets/unified-mobile-menu.js` was deleted 2026-07-15; all public pages now use the generator.
- Reveal rule for all new pages (2026-07-21): any page that loads `surette-logo.js` must also carry the inline head stamp `document.documentElement.classList.add("sds-logo-loaded")` (sits just above `#dm-critical-dark-baseline` on every current page) — without it, heavy pages flash in at 0.2s via nav.css's fallback, hide when the bottom script stamps the class, and fade back in (double blink).
- Confirm Supabase Auth URL configuration in the hosted project: Site URL should be `https://surettesystems.com`, and redirect allowlist should include `https://surettesystems.com/account.html`, `https://surettesystems.com/account-settings.html`, `https://surettesystems.com/account-admin.html`, `https://surettesystems.com/account-users.html`, plus local preview URLs only for development.
- Run the updated `supabase/portal-role-setup.sql` in the hosted Supabase project so homepage newsletter submissions, account-signup mirroring into `homepage_email_signups`, public form message submission/photo attachment support, account-dashboard message submission/list/delete RPCs, and Admin Center delete RPCs work live. If Supabase reports a changed return type for `list_portal_messages()`, rerun the current file, which includes `drop function if exists public.list_portal_messages();` before the recreated function.
- Configure Netlify Forms notifications for the detected forms (`contact`, `apps-consultation`, `app-checkout-request`, and `client-request`) so submissions email `info@surettesystems.com`, then submit production tests and confirm each message also appears in Admin Center Messages.

## Near-Term

- **Consider raising the mobile tab bar's inactive label opacity.** They are `rgba(196,199,199,0.6)` in `assets/standard-site-nav.js`, so they composite onto the bar itself — as the bar darkens the label darkens too, which caps them at **4.69:1 even against a pure black bar** (AA wants 4.5, so they are marginal *everywhere*, not just over light content). The 2026-07-16 bar-tint fix took them from 1.17:1 to ~4.25:1 over a light card, which is as far as tint alone can go. Raising the label to ~0.85 opacity would reach ~6:1, at the cost of some active/inactive hierarchy. Owner's call.


- **The Websites gallery cards are duplicated across two layouts — keep them in sync.** `casestudies.html` renders the same six collectible cards twice: desktop (`.hidden md:block`, 2/3-up) and phone (`.md:hidden`, 1-up). Any card edit (copy, estimate, links, theme) must land in both or they drift. Worth revisiting if this page is ever unified into a single responsive layout, or if the cards move to a shared injected component — but note they are static HTML on purpose (SEO).
- **`app-pricing.html`'s `data-app-back-link` only handles three apps.** Its `?from=` map covers `auction`/`sdms`/`benji`; MetalsCalc, Antique Mall and MetalsPredictor fall through to the default "Back to Apps Gallery". Harmless, but stale since the catalog grew to six.
- **`.footer-meta` / `.footer-meta-sep` in `assets/nav.css` are dead.** They styled the pre-2026-07-16 hand-authored footers; the shared footer uses `.dm-footer__legal-line` / `.dm-legal-sep` instead. Safe to delete once confirmed no page still emits `.footer-meta` markup.
- **`assets/portfolio-mobile-fixes.css`'s tile-era rules may be partly dead — but the file itself is now load-bearing.** As of 2026-07-17 it owns the entire mobile card redesign for `casestudies.html` (zigzag rows/number chips, front declutter, pill/title/badge sizing, template-widget mobile reformat) alongside the old `.portfolio-tile__media`-era rules, which may still be dead (neither gallery renders tiles; only the open slots use `.portfolio-tile`). If auditing, the tile-era rules are the candidates — everything `.tcg-*`, `.md\:hidden .tcg-card`, and `.lab-poke-section`-scoped is current. It is loaded by several portfolio detail pages too.

- ~~**Fix MetalsCalc OG/meta URLs**~~ — **closed 2026-08-08 as already-done:** a repo-wide grep returns zero `darkmatterwebsites.com` hits in any HTML/JS/CSS; both pages already use `surettesystems.com`. The task had outlived the problem.
- ~~**Capture ThirdStreetAuctions admin/vendor dashboard screenshots**~~ — **dropped 2026-08-08: the site is offline**, so the logged-in admin/vendor screens can no longer be captured. The seven existing `assets/apps/antique-mall/thirdstreet-*.png` public captures are the permanent record for this profile.
- **Finish Supabase portal setup** (dedicated Dark Matter / Surette Data Systems project):
- **Reconcile Supabase starter SQL** with the frontend's live table config (`client_invoices`, `client_documents`, `client_messages`) before using it to bootstrap a fresh database.
  - Run `supabase/client-portal-schema.sql`
  - Run `supabase/portal-role-setup.sql`
  - Confirm `list_portal_account_holders()` RPC returns rows for super-admin
  - Confirm `submit_portal_message()`, `list_portal_messages()`, and `delete_portal_message()` work for the intended client/admin roles
  - Confirm `submit_site_message()` works for anon/public website forms and that owner accounts can open signed links for uploaded message attachments
  - Run the updated `homepage_email_signups` setup so homepage submissions insert and account signups mirror into the newsletter subscriber source
  - Confirm `delete_newsletter_subscriber()` and `delete_portal_account_holder()` RPCs work for the super-admin and reject non-owner users
  - Confirm `handle_new_portal_user()` trigger creates `client_profiles` rows on signup
  - Set `app_metadata.role`: `rcman12589@aol.com` → `super_admin`, `scochrane495@gmail.com` → `sean_ads_admin`
  - Configure Auth redirects and email templates (signup redirect → `account-created.html`)
  - Create test users/client rows and verify RLS
  - Confirm Contact, Apps consultation, App Checkout, and portal account request submissions all appear in Admin Center Messages
- **Review all high-value pages** on desktop and mobile before launch.
- **Add a secure backend/server-side payment layer** before real Stripe recurring billing or privileged admin operations.
- **Keep app screenshots current** — Auction, SDMS, and Antique Mall screenshots should stay in sync with hosted demos.
- **Review app pricing copy and rates** after real client feedback.
- **Keep portfolio build-time estimates in sync with pricing.** The hours shown across the portfolio were derived from `website_pricing_plan.txt`'s one-time build packages at the published **$125/hr** rate (spec §37). If the rate or plan prices change, re-derive and update: 12 card pills + 12 card backs + 2 rate lines in `casestudies.html`, the six detail-page `#build-pricing` sections, and `portfolio.html`'s "Est. 15–440 hrs" folder chip.

## Backlog

- Spanish (`es/`) mirrors are absent from this working copy; reconcile or regenerate before bilingual launch.
- Native Spanish review before major public launch.
- Add real client/operations references to `CLIENTS.md` when approved.
- Decide first analytics source for portal traffic summaries.
- Build real account settings and Google Ads activity/status workflows (currently `account-settings.html` and `account-ads-status.html` are stubs).
- Consider future migration from duplicated hand-authored HTML to generated static HTML with shared layouts (Astro candidate for marketing pages).

## Recently Completed

- **Marked four portfolio sites offline and renamed Naples to .com (2026-08-08).** Elite Yacht Detailing, JPSurette, Sean's Ads, and AuctionBuddha are down: 16 gallery links across both duplicated grids became `Ask to View` -> `contact.html`, status tags read `status - offline`, three detail pages got a new `.portfolio-live-widget--offline` variant, and `portfolio-seansads.html` had its hero eyebrow, status chip, section heading, closing section, and meta descriptions rewritten off the "live" premise. `NaplesEstateJewelry.co` became `.com` everywhere. Caught and fixed a 24px card-back scroll regression from the first (longer) button label before shipping. Validator: 67-item baseline, unchanged.
- **Retired app demos taken off the site; access is now by request (2026-08-08).** Five of six apps (Auction House, SDMS, Benji, Third Street / Antique Mall, MetalsPredictor) lost their public instances. Removed every outbound link to the dead hosts, replaced each primary CTA with `Request Access` → `contact.html`, added a shared `.app-access-notice` panel to all five profiles, updated the "Demo Login" / "Try It Free" sections that were premised on self-serve access, switched the catalog's stat pills to `By Request`, and rewrote the catalog meta descriptions that advertised "live-demo" apps. Also caught a stray `benjipayroll.netlify.app` button on `app-pricing.html` outside the named scope. Verified at 320px and 1280px: zero dead external links, zero page overflow, notice styled on all five. Validator: 67-item baseline, unchanged.
- **MetalsCalc renamed to SpotCalc site-wide (2026-08-08).** 44 brand strings across `app-catalog.html`, the app profile, both duplicated `casestudies.html` grids, and `portfolio-metalscalc.html`; app link now `app.spotcalc.com`, marketing link `spotcalc.com`; dev capture script URLs updated. Filenames and asset directories deliberately unchanged. Card palette class renamed to `.tcg-card--app-spotcalc` with a matching `collectible-card.css` cache bust. Re-ran the never-scroll check on all 18 card backs at 320px — zero vertical and horizontal overflow.

- **Smoothed the mobile bottom-nav hide/reveal animation (2026-08-01).** Slowed the outgoing transition to 460ms and the return to 520ms, using separate easing curves and longer opacity fades so the bar flows rather than snaps while retaining the existing directional thresholds.
- **Added directional hide/reveal behavior to the site-wide mobile bottom nav (2026-08-01).** Downward scrolling hides the bar after a short threshold; upward scrolling restores it quickly. Added jitter protection, top-of-page reset, keyboard recovery, reduced-motion handling, and clean Services-popout interaction in the shared nav source.
- **Improved the spatial concept hero paragraph contrast (2026-08-01).** Added a dark translucent reading panel, lime rule, and stronger white text so the perspective grid no longer competes with the AI Listing Assistant description.
- **Built an unlinked spatial-style Multichannel Commerce test page (2026-08-01).** Added `multichannel-commerce-website-test-4.html` as a noindex experiment inspired by the requested Treasure Island reference, while retaining the full live-channel and AI Listing Assistant product story. No production navigation or inbound links were added.
- **Fixed the multichannel gallery overflowing narrow phones (2026-08-01).** The first gallery figure's 16:10 aspect ratio and 250px minimum height were forcing a 400px width inside smaller containers. Phone CSS now constrains all figures to the grid width and disables the conflicting aspect ratio on the first figure.
- **Apps hub now links to Multichannel Commerce (2026-08-01).** Added a featured commerce-platform banner to `apps.html` with the list-once positioning, a short AI Listing Assistant explanation, and a direct CTA to `multichannel-commerce-website.html`.
- **AI Listing Assistant detailed on the multichannel sales page (2026-08-01).** Added the image-analysis and natural-speech workflow to metadata and hero copy, rebuilt the first two workflow steps around AI form completion plus human review/approval, added a highlighted package feature, and added a detailed FAQ explaining how the assistant turns photos and spoken details into a structured listing draft.
- **Multichannel marketplace and social publishing marked live (2026-08-01).** Updated the standalone commerce page so its metadata, hero, channel availability strip, automatic-publishing workflow, feature list, social section, and FAQ consistently present eBay, Etsy, Instagram, and Facebook publishing as live. Removed all rollout/coming-next language from the page.
- **Complete Multichannel Commerce Website page (2026-08-01).** Added a full standalone sales page derived from the live Naples Estate Jewelry commerce model, then refocused the page on "List it once. Sell everywhere." One product record explicitly powers the branded store, eBay, Etsy, Instagram, and Facebook product/ad posts without duplicate entry. Replaced the dated storefront image with a fresh clean capture from the live site and updated the matching Open Graph card. Page serves successfully from the local preview; validator is 66 items because the new English page adds one expected missing-Spanish-mirror report to the previous 65-item baseline.
- **Homepage responsive symmetry and card-interaction pass (2026-08-01).** Centered every section-heading group, replaced the Services four-card 3+1 desktop layout with a balanced 2x2 tier from 880-1279px and a single 4-card row at 1280px+, made every desktop and compact-layout service card a full-surface anchor, and equalized repeated card/quote heights. Audited 320, 375, 414, 768, 879, 880, 1024, 1279, 1280, 1440, 1536, and 1920px; all card edges resolve to their links, and no orphaned groups, misaligned headers, unequal repeated-object heights, or horizontal overflow remain. Validator remains at the documented 65-item baseline.

- **Choppy contact-page load fixed (2026-07-21).** supabase-js is now lazy-imported in `site-message-forms.js` (was a static top-level import executing at DCL, mid-reveal; now first-submit + post-load idle warm-up — measured fetching after the load event), and `surette-logo.js`'s reveal waits for fonts (500ms cap) so swaps can't re-layout mid-fade. All three form pages verified bound; validator 65 baseline. → `site-message-forms.js?v=20260721-lazy-supabase`, `surette-logo.js?v=20260721-fonts-gate`.
- **Double-blink load fix + total knock-loose removal (2026-07-21).** Head-stamped `sds-logo-loaded` before first paint on all 28 surette-logo.js pages (kills the 0.2s-fallback flash→hide→reveal race that hit apps + the pricing page); apps.html pre-hides content until `is-apps-ready` with a 5s CSS safety (kills the loader's 50ms race); `#wm-canvas` pointer-events none at every width + dead handler code removed (terms.html wordmark unaffected). Verified end-states + zero console errors; validator 65 baseline.
- **Deploy cleanup (2026-07-21).** ~11MB of stale/orphaned files deleted (Elite/Naples/antique-mall/MetalsCalc old captures, darkmatter OG images, 5 dead code files, one-off root scripts, empty `netlify/`); `node_modules` untracked + gitignored (kept on disk; `package.json` added for reinstall); `.claude/settings.local.json` untracked; stale git index reconciled with disk (`git add -A` — whole pages and load-bearing assets were untracked). Closes the antique-mall screenshot and `_enum_out.txt` cleanup items. Validator 65 baseline.
- **Background-canvas scroll stutter fixed site-wide (2026-07-21).** `fractal.js` resize handler debounced + skips mobile height-only resizes (the toolbar-collapse reallocation stutter); `#canvas-container` composited (`translate3d`/`backface`/`contain`) on apps/process/account; wordmark canvas `_resize()` guarded against no-op reallocation (apps + terms). Verified with instrumented buffer-write counters: zero writes on height-only resizes, correct single resize on width change. → `fractal.js?v=20260721-scroll-stutter`, `surette-physics-wordmark-canvas.js?v=20260721-resize-guard`.
- **Nav-bar hairline borders pinned in plain CSS (2026-07-21).** Portfolio.html showed a solid light-gray line under the mobile header and above the bottom tab bar — the injected `border-starlight-white/10` utilities missed the CDN's DOM observer there and fell back to preflight `#e5e7eb`. All three bars' borders now pinned in `nav.css` next to the bar-tint rule (`rgba(255,255,255,0.1)`). All 28 public pages verified consistent. → `nav.css?v=20260721-nav-border-pin` (33 pages).
- **Apps wordmark knock-loose removed on mobile + scroll dead-zone fixed (2026-07-21).** `#wm-canvas { pointer-events: none }` in apps.html's mobile block; intro unaffected, desktop unchanged. Also unblocked touch scrolling over the hero (the engine's `touch-action:none` canvas covered the top ~430px). Loader scroll-lock reviewed, kept as is.
- **Mobile dropdown contact dedupe (2026-07-21).** Plain "Contact" row removed from the generator; gradient "Contact Us" CTA kept. All menu surfaces audited — zero intra-menu duplicate destinations site-wide. → `standard-site-nav.js?v=20260721-contact-dedupe` (28 pages).
- **Mobile gallery card polish (2026-07-17 session, after the zigzag).** Stat pill to 70%, titles/badges shrunk, back text and back titles enlarged (with a new measurement rule: back "fullness" needs true-slack math, not the scroll check — and backs must be checked for horizontal overflow too, now that titles are large enough to hit the edge). Card fronts decluttered on mobile (stage line + status tag hidden, full-width buttons, art reclaims the height). The Tech Business Site Template widget got the same mobile reformat — and the audit caught its back **scrolling** (148px over at 320px; it isn't a `.tcg-card`, so none of the gallery no-scroll protection covered it) plus `â†’` mojibake in all three of its copies. All CSS-scoped; desktop and app-catalog verified untouched each round.
- **Phone gallery numbered zigzag on `casestudies.html`, plus a container-query fix for the back-scroll rule it regressed (2026-07-17).** Cards in the `.md:hidden` grid shrink and stagger against a number chip (odd right/chip-left, even left/chip-right) via new `.tcg-mobile-row`/`.tcg-mobile-num` CSS in `assets/portfolio-mobile-fixes.css`. The shrink also shrinks each card's back face (fixed aspect ratio, one 3D object) and reopened the zero-slack "never scroll" budget from the day before — caught by re-running the same overflow check, not assumed safe. Fixed by moving the back-face tightening in `assets/collectible-card.css` from a viewport media query to a `@container` query keyed to the card's own rendered width, since the zigzag makes card width diverge from viewport width. Re-verified 0px overflow on all 18 card backs (both galleries) across the full mobile width range, not just the usual 320/375/1440 spot-checks — the failure this time was at a tier boundary a narrower sweep would have missed.
- **Portfolio surfaces rebuilt (2026-07-16 session).** Both galleries now render a shared **collectible card** (`assets/collectible-card.css` + `pokecard-dropin`): `casestudies.html` at every width (desktop + phone grids, hand-duplicated — edit both) and `app-catalog.html` (single responsive layout, 6 cards, its own coral/lime/green/teal/periwinkle/magenta palette). `portfolio.html`'s two category cards were rebuilt as non-flipping **"collection folders"** in the same visual family. Card overlays must stay **in-flow** — see DECISIONS; this cost four device round-trips of mirrored bleed-through and flicker before the simple structure won.
- **Legibility fixes forced by the new light cards (2026-07-16):** the three fixed nav bars went from a 10% tint (bar labels measured **1.17:1** over a light card) to `rgba(8,10,14,0.9)` in `nav.css`, and the bar tint moved off a Tailwind utility on injected markup into plain CSS. Both galleries' chips got opaque backings for the same reason.
- **Footer meta repaired (2026-07-16):** phrase-atomic legal/tagline text (the phone number was splitting as "404-"/"8505" at **1440px**, not just on phones), the `tel:` link restored, and the wide footer's legal block reshaped into a three-line stack — both regressions from the footer consolidation.
- **Dead code removed (2026-07-16):** `casestudies.html`'s referrer back-link script (dead three ways: retired referrer target, no target element, no incoming link) and the gallery pages' orphaned `.portfolio-tile` media/img/badge/price rules.
- **Build-cost estimates on the Websites gallery + all six portfolio detail pages (2026-07-16):** gold "EST. $X+" pills on every gallery tile and a `#build-pricing` estimate section on every detail page, priced against `website_pricing_plan.txt` one-time build packages with the required estimate-only disclosure. Naples $12K–$16K, Elite $8.5K–$10K, JPSurette $4.5K–$6.5K, SeansAds $7.5K–$10K, AuctionBuddha $35K–$55K+ (repriced later the same day once the full ecosystem scope was known), MetalsCalc $2K–$2.5K. Verified in-browser (pill styles, no collisions at 375px, sections on all six pages, no overflow, no console errors); validator baseline unchanged. *(Superseded 2026-07-16: all public labels converted to hours at the published $125/hr rate — see the sync item under Near-Term.)*

- **Nav single-source migration (2026-07-15):** migrated all 16 remaining public pages with inline/hand-copied nav markup to the `assets/standard-site-nav.js` generator, fixed the generator to emit `../`-prefixed links on `services/*` pages (all its injected links there 404'd before), deleted `assets/unified-mobile-menu.js` + its 28 script tags + dead nav.css shell rules, removed `app-pricing.html`'s broken `es/` lang toggle, and unified every loader reference to `v=20260715-nav-unify`. Fixes wrong active-page highlights (Portfolio shown active on apps/app-catalog/casestudies/built-by/accessibility/process), hardcoded-cyan Client Login on legacy pages, homepage's divergent hamburger structure, missing mobile nav markup on portfolio detail pages, and terms.html's missing nav logo. Verified in-browser at mobile + desktop across all migrated page types; no console errors; validator baseline unchanged.
- Completed `services/website-design-hosting.html` as a full public pricing page implementing `website_pricing_plan.txt` (project root, source of truth for plan names/prices/policies): managed plans ($0 upfront Starter/Mini/Lead Capture + Local Business + Growth), one-time builds (8 packages), hosting/care plans, update-allowance/support/revision policies, ownership/renewal/handoff, add-on price list, and a 36-question FAQ. Verified anchors, overflow (375px clean), and content coverage in-browser; validator noise unchanged.
- Fixed `assets/mobile-services-nav.js` mobile Services popout still linking to the three retired service pages; now a single "Website Design / Hosting" entry (EN + ES), cache-busted on all 28 referencing pages (`v=20260715-services-merge`).
- Unified public navigation behavior across legacy, standard-nav, service, Portfolio, and client-login pages. Added `assets/unified-mobile-menu.js` as a compatibility layer, corrected shared fixed-header gutters/overflow rules, and verified 30 public routes at 1440px, 834px, 768px, 767px, and 390px with zero header/navigation failures.
- Added `portfolio.html` as the top-level Portfolio hub and folded the header/mobile nav Apps + Websites entries into one Portfolio item. The hub links to the existing Apps (`apps.html`) and Websites (`casestudies.html`) pages with short category explanations and existing visual assets. Verified desktop/mobile preview and reran the static validator; remaining failures are documented noise plus the expected missing Spanish mirror for the new page.

- Updated Sean's Ads portfolio (`portfolio-seansads.html`, `casestudies.html`) to reflect live production at SeansAds.com; aligned `portfolio-seansads.html` with the standard sitewide top nav.
- Fixed tablet overflow/clipping sitewide: shared `assets/nav.css` tablet band, casestudies hero stack, contact page `lg:flex-row`, portfolio live-widget sizing, and nav `#sds-logo` flex treatment. Playwright tablet audit clean on 16 pages × 3 viewports.

- Restored Netlify Forms capture for Contact, Apps consultation, App Checkout, and account-dashboard request forms while keeping Supabase/Admin Center message saves.
- Fixed service-page header/menu alignment by anchoring fixed top headers to the viewport in shared `assets/nav.css`; verified all service pages on mobile and the Website Design service page on desktop.
- Removed profile-level business/company and website URL fields from account settings, Admin Center subscriber/account-holder tables, and Supabase setup/schema cleanup.
- Separated Admin Center Subscribers from Account holders: Subscribers now reads only `homepage_email_signups`, account holders stay in their own auth/profile table, new account signups still mirror into subscriber rows, and subscriber deletion no longer reappears from account-holder fallback data.
- Widened Admin Center Subscribers and Account holders table name/email columns, especially subscriber email, and narrowed/wrapped the Subscribers source column.

- Made the mobile header account icon color state consistent sitewide: grey when signed out, cyan/blue when signed in, while keeping the control icon-only on mobile.
- Added the standard fixed mobile logo/header and promoted mobile nav row to all service umbrella pages so their mobile menu matches the rest of the site and the Services tab opens the shared services popout.
- Added compact mobile footers to all service umbrella pages (`services/website-design.html`, `managed-hosting.html`, `website-care-plans.html`, `in-home-services.html`, and `office-network-setup.html`) while keeping the existing desktop footer behavior unchanged.
- Added a sitewide no-white-flash dark loading baseline: critical inline head CSS on HTML pages, shared `assets/nav.css` dark document/root defaults, normalized `theme-color` to `#050505`, and verified repeated desktop/mobile route loads across home, apps/pricing, contact, legal, accessibility, and SDMS compliance pages. Follow-up fixed the account page by keeping visual shells transparent so the animated portal background remains visible.
- Added the shared desktop site footer to the remaining service-page outliers, `services/in-home-services.html` and `services/office-network-setup.html`.
- Removed stale standalone demo pages that were not linked from the live site: `surette-logo-demo.html`, `blackhole-icon.html`, and `singularity.html`.
- Added a sitewide object-level reveal for repeated cards, panels, pricing tiles, glass blocks, and portfolio tiles so whole objects fade in together after page-ready instead of text appearing before the background/border treatment.
- Added a same-site outbound navigation loader: link clicks now show the small black spinner overlay briefly before navigation so page changes feel intentional instead of frozen.
- Stabilized page reveal after the white-screen regression: restored the `html`-based fade path, kept the root/background black, added a CSS safety fallback for logo pages if JS reveal is interrupted, and updated `surette-logo.js` cache keys across pages.
- Updated public/site contact email links and visible contact text to `info@SuretteSystems.com` across contact, portfolio, case study, process, and service pages. Form placeholder examples and Supabase/admin role setup emails were intentionally left unchanged.
- Audited representative desktop/mobile page transitions and header consistency. Confirmed core marketing/app pages settle to `html` opacity 1 with stable desktop nav height and no leftover homepage loader markup after reveal.
- Normalized `account.html` desktop nav and `assets/client-portal.js` signed-out state so the account page shows the same "Client Login" label/account icon as the rest of the site, with cache-busted script reference.
- Matched the homepage newsletter email field more closely to the account-page inputs: transparent wrapper, thin cyan outline, darker subtle interior, clearer sample-email placeholder, and unchanged Supabase signup wiring.
- Removed remaining Netlify Forms wiring from Contact, Apps consultation, and App Checkout. Public forms now submit into the Admin Center Message Center through Supabase `submit_site_message()`, with optional photo/screenshot attachment support on Contact and Checkout.
- Replaced the account dashboard "Request a change or ask a question" Netlify Form flow with Supabase `client_messages` submission and added an owner-only Message Center tab in Admin Center to list/delete those portal messages.
- Added a Subscribers table copy/export modal in Admin Center with line-list, comma-list, and CSV formatting plus quick-copy/manual-selection fallback.
- Added Admin Center Delete actions to the Subscribers and Account holders tables, each routed through a confirmation modal and owner-only Supabase RPCs. The signed-in owner account is protected from self-delete in the UI and RPC.
- Smoothed the homepage Nova WebGL background on mobile by making the fixed canvas layer use stable viewport sizing/compositing and ignoring height-only mobile browser-toolbar resize events during scroll.
- Kept the mobile header account control icon-only in signed-in state and cache-busted `assets/account-nav.js` references.
- Added MetalsCalc — Buying Calculator as a 5th Surette Data Systems app: `metalscalc-buying-calculator.html` app profile, catalog tile #05 (PWA badge) in `app-catalog.html`, screenshots in `assets/apps/metalscalc/`, and a portfolio/website entry (`portfolio-metalscalc.html`, tile #06 in `casestudies.html`) with screenshots in `assets/portfolio/metalscalc/`.
- Added `account-settings.html` (profile/password management), `account-created.html` (email verification landing page after signup), and `account-ads-status.html` (Google Ads workspace stub for Sean).
- Rebuilt `app-catalog.html` gallery cards to match the Websites/Case Studies `.portfolio-tile` design (2-up mobile, 3-up desktop). Removed old "DepthFold" 3D card system.
- Removed "fly" canvas hero background from `app-catalog.html` after `assets/fly/` was deleted; restored cosmic-web background.
- Replaced top-level `Process` nav item with `Websites` link to `casestudies.html` across all navigation. Added "View Our Process" CTAs to six service pages.
- Added AuctionBuddha.com to Websites gallery with detail page and public screenshots.
- Refreshed NaplesEstateJewelry.co portfolio with current live-site screenshots.
- Refreshed Antique Mall profile around ThirdStreetAuctions.com with seven new public captures and updated feature copy.
- Fixed homepage hero tint so it stays fixed over the Nova background while content scrolls.
- Replaced homepage black-hole hero with Nova particle-galaxy widget.
- Added super-admin account-holder viewer to `account.html` backed by Supabase RPC.
- Added owner-only `account-users.html` subscriber table and linked it from Admin Center; homepage email sign-ups are represented as a pending backend source until the capture form is wired.
- Reorganized `account-admin.html` into a left-anchored tab workspace for Overview, Subscribers, Account holders, and Sean Ads.
- Expanded `account-admin.html` for ultra-wide desktop use and embedded the Subscribers / Account holders tables directly inside their side-tab panels.
- Added homepage desktop/mobile newsletter signup forms wired to Supabase `homepage_email_signups` through `assets/newsletter-signup.js`.
- Removed "Do Not Press" widget and `face-interactive.html` from the site.
- Removed defunct English/Spanish nav toggle from Auction, SDMS, and Benji app detail pages.
- Rebranded app/software line to Surette Data Systems site-wide.
- Added lossless WebP Surette Data Systems brand assets.
- Ran mobile sweep; fixed pricing CTA containment, two-wide pricing tiers, SDMS mobile overflow, and several other overflow issues.
